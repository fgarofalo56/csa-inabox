azure-functions>=1.18.0
openai>=1.30.0
azure-cosmos>=4.7.0
azure-identity>=1.17.1
opencensus-ext-azure>=1.1.13
# Microsoft Learn MCP fallback grounding — see ms_learn.py
mcp>=1.0.0,<2.0.0
httpx>=0.27.0,<1.0.0
# CI unblock (latent break, found 2026-07-15): mcp pulls sse-starlette
# transitively; sse-starlette 3.x requires starlette>=1.0, which silently
# upgrades starlette past the <0.42 cap of fastapi 0.115.x (pinned by
# platform/runners/script-runner/requirements.txt in the SAME shared CI env).
# starlette 1.x removed the deprecated `on_startup` kwarg fastapi<0.116 still
# passes to Router, breaking EVERY FastAPI() construction (portal tests:
# "Router.__init__() got an unexpected keyword argument 'on_startup'").
# mcp 1.x accepts sse-starlette 2.x + starlette>=0.27, so these caps are safe.
# TODO: drop both caps when the repo migrates to fastapi>=0.139 / starlette 1.x
# (lifespan pattern) everywhere.
sse-starlette>=2.1.0,<3.0.0
starlette>=0.40,<0.42
