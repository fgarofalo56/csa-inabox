{
  "_": "A SNAPSHOT of `main`'s required status checks, so the declaration in policy.json (receipts.ci_green_rule.substantive_steps) can be checked for SET EQUALITY offline. Refresh with `python tools/drain/merge_gate.py --refresh-required-contexts`; __tests__/test_ci_green_declared.py compares it to the live API whenever `gh` is reachable and skips when it is not, so a stale snapshot fails in CI rather than sitting unnoticed.",
  "_why_not_live_only": "gates.DATA_NOT_NAMESPACE names this test as the instrument that checks the rows of `substantive_steps`, which is what lets the policy walk stop there. An instrument that only runs when a network is up is not one a stop can rest on.",
  "measured": "2026-09-13",
  "source": "repos/fgarofalo56/csa-inabox/branches/main/protection/required_status_checks",
  "contexts": [
    "Python Lint",
    "Python Tests (3.10)",
    "Python Tests (3.11)",
    "Python Tests (3.12)",
    "PowerShell Lint",
    "Secret Scan",
    "Repo Hygiene",
    "dbt Compile (shared)",
    "dbt Compile (finance)",
    "dbt Compile (inventory)",
    "dbt Compile (sales)",
    "next build (node 20)",
    "guardrails",
    "vitest (node 20)",
    "changelog parser can read every commit message"
  ]
}
