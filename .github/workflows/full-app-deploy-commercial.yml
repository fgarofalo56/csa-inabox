name: full-app-deploy-commercial

# CSA Loom — end-to-end deploy of container apps into a provisioned
# Admin Plane. Unblocks the chicken-and-egg ACR private-endpoint
# problem by temporarily enabling public access, building + pushing
# all images, then locking it back down. Finally re-deploys the
# Bicep with deployAppsEnabled=true so the Container Apps come up
# pointing at the new images.
#
# REQUIRES: a successful first deploy (run_mode=full) has already
# provisioned the Admin Plane RG with ACR + Container Apps Env.
#
# Spend warning: each app build runs ~3-8 min in ACR Tasks; the
# Container Apps stay up after the deploy (~$10-30/hr at F8).

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Image tag (default v0.1)'
        type: string
        default: v0.1
      skip_build:
        description: 'Skip build phase (images already in ACR)'
        type: boolean
        default: false
      enable_apps_after:
        description: 'Re-run deploy with deployAppsEnabled=true after build'
        type: boolean
        default: true
      region:
        description: 'Azure region of the target Admin Plane (e.g. centralus). LEAVE EMPTY to discover the estate — the resolve job asks Resource Graph which subscription-wide rg-csa-loom-admin-*/acrloom* exists and targets that. There is deliberately NO default: a default of eastus2 pointed run 31028909702 at a resource group that does not exist while the live estate is centralus (#3029). Supply a value only to disambiguate a subscription that holds more than one admin plane.'
        type: string
        required: false
      subscription:
        description: 'Target subscription id (defaults to AZURE_SUBSCRIPTION_ID secret).'
        type: string
        required: false
      skip_supply_chain:
        description: 'EMERGENCY VALVE — skip the SC1 Trivy CRITICAL gate + cosign sign/verify (mirrors skip_uat semantics). Images ship WITHOUT supply-chain verification.'
        type: boolean
        default: false

permissions:
  contents: read
  issues: write
  # SC1: keyless cosign signing exchanges the Actions OIDC token for a
  # short-lived Fulcio certificate.
  id-token: write

# This workflow holds a 180-minute ACR firewall lease (open-acr) for the whole
# build matrix. A SECOND dispatch inside that window cannot get the lease and
# dies after `acquire`'s bounded 25-minute wait — which is exactly what killed
# run 30600323509 (dispatched two minutes after 30600236939):
#     [acr-lease] TIMED OUT after 25m … holder = run 30600236939
# and its `Roll Container Apps` then ran anyway against a half-built registry
# and failed the console health gate. Serialise instead of colliding.
# cancel-in-progress is deliberately FALSE: cancelling a holder mid-push is the
# other half of the #2603 incident this lease exists to prevent.
#
# The group is a CONSTANT (refs #3029). It used to be keyed on
# `inputs.region || 'auto'`, so a run that named `centralus` and a run that left
# the field blank landed in DIFFERENT concurrency groups — and both then raced
# for the same ACR firewall lease on the same registry, which is precisely what
# the group exists to serialise. There is one Loom admin plane per subscription
# and the resolve job discovers it, so there is nothing for the region to
# partition.
concurrency:
  group: full-app-deploy-commercial
  cancel-in-progress: false

env:
  # NO DEFAULT REGION (refs #3029). Empty is the normal, correct value: the
  # resolve job below DISCOVERS the admin plane rather than constructing its
  # name from a region. A default of eastus2 is what made run 31028909702 look
  # for rg-csa-loom-admin-eastus2, which does not exist, and then report an
  # unrelated "Registry names may contain only alpha numeric characters" from
  # the cleanup step running with an empty registry name.
  AZURE_LOCATION: ${{ inputs.region }}

jobs:
  resolve:
    name: Resolve ACR + admin RG
    runs-on: ubuntu-latest
    outputs:
      acr_name: ${{ steps.acr.outputs.acr_name }}
      acr_login_server: ${{ steps.acr.outputs.acr_login_server }}
      rg: ${{ steps.acr.outputs.rg }}
      admin_sub: ${{ steps.acr.outputs.admin_sub }}
      region: ${{ steps.acr.outputs.region }}
      contract_refs: ${{ steps.contract.outputs.contract_refs }}
    steps:
      - uses: actions/checkout@v4
      - name: Azure login
        uses: Azure/login@v2
        with:
          creds: |
            {
              "clientId": "${{ secrets.AZURE_CLIENT_ID }}",
              "clientSecret": "${{ secrets.AZURE_CLIENT_SECRET }}",
              "tenantId": "${{ secrets.AZURE_TENANT_ID }}",
              "subscriptionId": "${{ inputs.subscription || secrets.AZURE_SUBSCRIPTION_ID }}"
            }
      - id: acr
        run: |
          # DISCOVER the admin RG + ACR; never CONSTRUCT them from a region
          # default (auto-bind-by-default.md 5 - the platform figures it out).
          #
          # On run 31028909702 (2026-08-05) a DEFAULT dispatch resolved
          # rg-csa-loom-admin-eastus2 because `region` still defaults to eastus2
          # while the live Commercial estate is centralus. That RG does not
          # exist, `az acr list -g` exited 3, and the step died right there.
          # The run headline error then became an unrelated "Registry names may
          # contain only alpha numeric characters" from the cleanup step running
          # with an EMPTY registry name - a message about the wrong thing.
          #
          # NOTE on -e: GitHub runs `run:` under `bash -e`, and `set -uo
          # pipefail` does NOT clear it. A previous fallback in this repo was
          # unreachable for exactly that reason. Every capture below therefore
          # uses the `if ! VAR=$(...)` form, which -e does not abort on.
          set -uo pipefail
          # AZURE_LOCATION is now EMPTY unless the operator named a region
          # (#3029 removed the eastus2 default). With no region there is no
          # "preferred" RG to try, so go straight to discovery rather than
          # querying `rg-csa-loom-admin-` and reading its 404 as a fact.
          ACR=""; RG=""; SERVER=""; REGION="$AZURE_LOCATION"
          if [[ -n "$AZURE_LOCATION" ]]; then
            PREFERRED_RG="rg-csa-loom-admin-${AZURE_LOCATION}"
            if ACR=$(az acr list --resource-group "$PREFERRED_RG" --query "[?starts_with(name, 'acrloom')]|[0].name" -o tsv 2>/dev/null); then
              ACR=$(echo "$ACR" | tr -d '\r')
              [[ "$ACR" == "None" ]] && ACR=""
              [[ -n "$ACR" ]] && RG="$PREFERRED_RG"
            fi
          else
            echo "::notice::No region supplied — discovering the Loom admin plane across the subscription (this is the normal path; the workflow no longer defaults to a region)."
          fi
          if [[ -z "$ACR" ]]; then
            echo "::notice::Discovering the Loom admin plane across the subscription with Resource Graph rather than assuming the region${AZURE_LOCATION:+ (nothing matched rg-csa-loom-admin-$AZURE_LOCATION)}."
            az extension add --name resource-graph --upgrade --only-show-errors
            Q="Resources | where type =~ 'microsoft.containerregistry/registries' | where name startswith 'acrloom' | where resourceGroup startswith 'rg-csa-loom-admin' | project name, resourceGroup, location, loginServer = properties.loginServer | order by resourceGroup asc"
            if ! ROWS=$(az graph query -q "$Q" --first 50 -o json 2>&1); then
              echo "::error::Could NOT query Azure Resource Graph, so nothing is known about whether a Loom admin registry exists in this subscription. This is NOT evidence that the estate is missing. Raw error follows."
              echo "$ROWS" >&2
              exit 1
            fi
            COUNT=$(printf "%s" "$ROWS" | jq -r ".data | length")
            if [[ "$COUNT" == "0" ]]; then
              echo "::error::Resource Graph ANSWERED and this subscription contains no rg-csa-loom-admin-*/acrloom* registry. Provision the admin plane first: deploy-fiab-commercial with deployAppsEnabled=false (phase 1 of the two-phase image path)."
              exit 1
            fi
            # Ambiguity is an ERROR, not a coin flip (carried over from #3001).
            # Picking .data[0] out of several Admin Plane RGs would silently
            # build into whichever one sorted first.
            if [[ "$COUNT" != "1" ]]; then
              NAMES=$(printf "%s" "$ROWS" | jq -r '[.data[].resourceGroup] | join(", ")')
              echo "::error::Multiple Loom admin planes found ($NAMES). Ambiguous — re-dispatch with the 'region' input set to the one you mean."
              exit 1
            fi
            SEL=".data[0]"
            ACR=$(printf "%s" "$ROWS" | jq -r "$SEL.name")
            RG=$(printf "%s" "$ROWS" | jq -r "$SEL.resourceGroup")
            SERVER=$(printf "%s" "$ROWS" | jq -r "$SEL.loginServer")
            REGION=$(printf "%s" "$ROWS" | jq -r "$SEL.location")
            if [[ -n "$AZURE_LOCATION" && "$REGION" != "$AZURE_LOCATION" ]]; then
              echo "::warning::The requested region ($AZURE_LOCATION) has no Loom admin plane. Discovered the estate in $RG ($REGION) and targeting that."
            else
              echo "::notice::Discovered the Loom admin plane in $RG ($REGION)."
            fi
          fi
          if [[ -z "$SERVER" ]]; then
            if ! SERVER=$(az acr show --name "$ACR" --query loginServer -o tsv 2>&1); then
              echo "::error::Resolved registry $ACR but could not read its login server. Refusing to continue with an empty value - that is what produces the misleading \"Registry names may contain only alpha numeric characters\" error downstream. Raw error: $SERVER"
              exit 1
            fi
            SERVER=$(echo "$SERVER" | tr -d '\r')
          fi
          if [[ -z "$ACR" || "$ACR" == "null" || -z "$RG" || "$RG" == "null" || -z "$SERVER" ]]; then
            echo "::error::Registry coordinates came back empty or null (acr=$ACR rg=$RG server=$SERVER). Not proceeding blind."
            exit 1
          fi
          # Coordinates for the chained post-deploy bootstrap (rel-T34).
          #
          # ADMIN_SUB is READ below. It must be ASSIGNED here, and it must be
          # assigned BEFORE the outputs block: #3013 rewrote this step to
          # discover the RG/ACR via Resource Graph, declared ACR/RG/SERVER/
          # REGION, and dropped the `az account show` that used to produce
          # ADMIN_SUB inline -- while leaving the reader on the line below.
          # With `set -u` that is an unbound variable, so the step died at the
          # very first output and took the ENTIRE app-image path with it
          # (runs 31049648941, 31063434824, 31065363893).
          if ! ADMIN_SUB=$(az account show --query id -o tsv 2>&1); then
            echo "::error::Resolved the registry ($ACR in $RG) but could NOT read the current subscription id, which the chained post-deploy bootstrap needs. This is not a missing subscription -- it is an unreadable one. Raw error: $ADMIN_SUB"
            exit 1
          fi
          ADMIN_SUB=$(echo "$ADMIN_SUB" | tr -d '\r')
          if [[ -z "$ADMIN_SUB" ]]; then
            echo "::error::az account show returned success but an EMPTY subscription id. Not emitting a blank admin_sub for the bootstrap to fail on later."
            exit 1
          fi
          echo "acr_name=$ACR" >> "$GITHUB_OUTPUT"
          echo "acr_login_server=$SERVER" >> "$GITHUB_OUTPUT"
          echo "rg=$RG" >> "$GITHUB_OUTPUT"
          echo "admin_sub=$ADMIN_SUB" >> "$GITHUB_OUTPUT"
          echo "region=$REGION" >> "$GITHUB_OUTPUT"
          echo "::notice::ACR=$ACR login=$SERVER rg=$RG region=$REGION"

      # ── The DEPLOY CONTRACT (refs #2958) ──────────────────────────────────
      # THE bug this workflow shipped for months: the build stamped
      # `--image $APP:$TAG` where TAG is the `tag` INPUT, so a dispatch that
      # passed an explicit tag (0.79.0 on 2026-07-28, 0.80.0 on 2026-07-31 —
      # both real) produced NO `:v0.1`. But `params/commercial.bicepparam`
      # resolves every appImageTags key through
      # `readEnvironmentVariable('LOOM_<APP>_TAG', 'v0.1')`, and
      # scripts/ci/reconcile-resolve.mjs only exports a pin for apps that are
      # ALREADY RUNNING — so a not-yet-deployed app (loom-duckdb, which
      # admin-plane/main.bicep now deploys BY DEFAULT) falls back to a `v0.1`
      # nothing had ever pushed. `loom-duckdb` itself BUILT green in both runs;
      # only the tag was missing, and that one missing tag is what fails the
      # image preflight in deploy-fiab-commercial and blocks the whole
      # apps-enabled deploy.
      #
      # So the contract set is derived here from the two files that actually
      # decide it (reconcile-policy's APP_IMAGE_TAGS + the paramfile defaults)
      # using the SAME resolver the deploy lane preflights with, and every
      # matrix build stamps its contract tag in addition to :$TAG. Deriving it
      # rather than hard-coding 'v0.1' is the property that makes it survive a
      # paramfile edit — a hard-coded list is how the duckdb-only Gov preflight
      # missed four other images.
      - name: Derive the deploy-contract image tags
        id: contract
        run: |
          set -euo pipefail
          node scripts/ci/resolve-image-preflight-refs.mjs \
            --param-file platform/fiab/bicep/params/commercial.bicepparam \
            --boundary commercial > /tmp/contract-refs.txt
          REFS=$(tr '\n' ' ' < /tmp/contract-refs.txt | sed 's/  */ /g; s/^ *//; s/ *$//')
          if [[ -z "$REFS" ]]; then
            echo "::error::resolve-image-preflight-refs.mjs produced NO refs. Either the paramfile moved or the resolver broke — refusing to build a set of images nobody can prove the deploy will be able to pull."
            exit 1
          fi
          echo "contract_refs=$REFS" >> "$GITHUB_OUTPUT"
          echo "::notice::deploy contract: $REFS"

  open-acr:
    name: Temporarily open ACR public access + grant AcrPush
    needs: resolve
    if: ${{ !inputs.skip_build }}
    runs-on: ubuntu-latest
    steps:
      # Needed for scripts/csa-loom/acr-firewall-lease.sh (#2603).
      - uses: actions/checkout@v4
      - name: Azure login
        uses: Azure/login@v2
        with:
          creds: |
            {
              "clientId": "${{ secrets.AZURE_CLIENT_ID }}",
              "clientSecret": "${{ secrets.AZURE_CLIENT_SECRET }}",
              "tenantId": "${{ secrets.AZURE_TENANT_ID }}",
              "subscriptionId": "${{ inputs.subscription || secrets.AZURE_SUBSCRIPTION_ID }}"
            }
      - name: Enable ACR public access + admin user + disable quarantine/trust
        run: |
          ACR_NAME="${{ needs.resolve.outputs.acr_name }}"
          # #2603: the network open is a LEASE ACQUISITION, not a bare toggle.
          # The close-acr job below only re-locks if this run is still the
          # recorded holder, so it can no longer slam the door on a concurrent
          # build's in-flight push. This job is the from-scratch deploy path and
          # holds the registry for the whole matrix build, so it takes a long
          # lease. See docs/fiab/acr-firewall-lease.md.
          LOOM_ACR_LEASE_TTL_MINUTES=180 \
            bash scripts/csa-loom/acr-firewall-lease.sh acquire --acr "$ACR_NAME"
          az acr update --name "$ACR_NAME" --admin-enabled true -o none
          # Disable quarantine + trust policies — they block reads of
          # newly-pushed unsigned/unscanned images. NOTE (SC1): ACR
          # content-trust is Docker Content Trust (Notary v1) — NOT the
          # signing scheme Loom enforces. Supply-chain enforcement on this
          # deploy path is cosign keyless sign (in the build job below) +
          # cosign verify-before-roll (verify-images job); these registry
          # policy toggles stay disabled deliberately and are a tracked
          # drift item for the V5 scheduled what-if lane.
          az acr config content-trust update --registry "$ACR_NAME" --status disabled 2>&1 | tail -3 || true
          # quarantinePolicy can't be toggled via az acr config; via REST:
          ACR_ID=$(az acr show --name "$ACR_NAME" --query id -o tsv)
          az rest --method patch --uri "https://management.azure.com${ACR_ID}?api-version=2023-07-01" \
            --body '{"properties":{"policies":{"quarantinePolicy":{"status":"disabled"}}}}' \
            --output none 2>&1 || true
          echo "::warning::ACR $ACR_NAME public access + admin user OPENED; quarantine/trust DISABLED for build."
          # ACR network-rule changes propagate asynchronously (~30-90s). The
          # parallel build jobs were getting 'client IP not allowed' because
          # they started pushing before the open took effect. Poll the registry
          # token endpoint until it authorizes from this runner, then give the
          # change extra slack before the build matrix fans out.
          echo "Waiting for ACR public access to propagate…"
          ok=false
          for i in $(seq 1 24); do
            if az acr login --name "$ACR_NAME" >/dev/null 2>&1; then ok=true; echo "ACR reachable after ${i} attempt(s)."; break; fi
            sleep 10
          done
          $ok || echo "::warning::ACR login still failing after ~240s; builds may need a retry."
          sleep 20
      - name: Grant AcrPush to deploy SP + AcrPull to all app UAMIs (idempotent)
        run: |
          set +e
          ACR_ID=$(az acr show --name "${{ needs.resolve.outputs.acr_name }}" --query id -o tsv)
          # Use the RG the resolve job actually adopted — AZURE_LOCATION may be
          # the (stale) `region` input default while the estate is elsewhere.
          RG='${{ needs.resolve.outputs.rg }}'

          # AcrPush to deploy SP (push images)
          SP_OID=$(az ad sp show --id "${{ secrets.AZURE_CLIENT_ID }}" --query id -o tsv)
          az role assignment create --assignee-object-id "$SP_OID" \
            --assignee-principal-type ServicePrincipal \
            --role 8311e382-0749-4cb8-b61a-304f252e45ec \
            --scope "$ACR_ID" 2>&1 | grep -vi "already" || true

          # AcrPull (7f951dda-4ed3-4680-a7ca-43fe172d538d) to every Loom UAMI
          for UAMI in $(az identity list --resource-group "$RG" \
              --query "[?starts_with(name, 'uami-loom-')].principalId" -o tsv); do
            az role assignment create --assignee-object-id "$UAMI" \
              --assignee-principal-type ServicePrincipal \
              --role 7f951dda-4ed3-4680-a7ca-43fe172d538d \
              --scope "$ACR_ID" 2>&1 | grep -vi "already" || true
          done

          echo "::notice::AcrPush + AcrPull roles granted."
          # 90s for Azure RBAC propagation across all assignments
          sleep 90

      # ── Mirror third-party upstream images into the estate ACR (#2682) ───────
      # NO Loom deploy path may pull a third-party container image from a public
      # registry. `az acr import` is a server-side registry-to-registry copy, so
      # the ONLY egress to Docker Hub is here (a one-time build-lane import into
      # the ACR the firewall lease has open), NOT from the Container Apps at
      # runtime. The bicep then pulls <acr>/<repo>:<tag> via the app UAMIs.
      #
      # WHAT to mirror is NOT written here any more. Both cloud lanes read the
      # SINGLE manifest platform/fiab/images/upstream-images.json and import BY
      # DIGEST, then verify the digest that landed. The old hard-coded array in
      # this file, the identical array in gov-provision-dataplane-images.yml and
      # the refs in the bicep modules were three copies of one fact: bump the
      # bicep ref alone and the ACR never receives that tag, so the Container App
      # points at an image that does not exist and cannot activate a revision.
      # scripts/ci/check-upstream-image-mirror.mjs (MIR0) fails the build if a
      # bicep-pinned ref has no manifest entry or if either lane stops calling
      # the shared script. LIC0 still reviews each image's LICENCE separately.
      - name: Mirror third-party images into ACR (#2682)
        run: bash scripts/ci/mirror-upstream-images.sh --acr "${{ needs.resolve.outputs.acr_name }}"

  build:
    name: Build ${{ matrix.app }} via Docker push
    needs: [resolve, open-acr]
    if: ${{ !inputs.skip_build }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        include:
          - app: loom-console
            ctx: ./apps/fiab-console
          - app: loom-setup-orchestrator
            ctx: ./apps/fiab-setup-orchestrator
          - app: loom-mcp
            ctx: ./apps/fiab-mcp-config
          - app: loom-mcp-bridge
            ctx: ./apps/fiab-mcp-bridge
          - app: loom-activator
            ctx: ./apps/fiab-activator-engine
          - app: loom-mirroring
            ctx: ./apps/fiab-mirroring-engine
          - app: loom-direct-lake-shim
            ctx: ./apps/fiab-direct-lake-shim
          - app: loom-copilot-maf
            ctx: ./apps/copilot-maf
          - app: loom-wrangler-host
            ctx: ./apps/fiab-wrangler-host
          # The sandboxed R/Python script-visual executor behind the report
          # designer's Power-BI-parity script visual. `params/commercial.bicepparam`
          # declares `scriptRunner: readEnvironmentVariable('LOOM_SCRIPT_RUNNER_TAG','v0.1')`,
          # so an apps-enabled deploy on an estate where it is not already
          # running pulls `loom-script-runner:v0.1` — but as of 2026-08-05
          # `grep -rn script-runner .github/workflows/*.yml` found NO workflow
          # that builds this image at all. It exists in the live Commercial
          # registry only because it was pushed out of band, which means a
          # genuine from-scratch estate could never produce it and the
          # "two-phase image path" in no-vaporware.md was incomplete. Added
          # here, where the rest of the Commercial image set is produced.
          - app: loom-script-runner
            ctx: ./platform/runners/script-runner
          # dbt execution runtime for the Synapse / opt-in Fabric dbt-job targets.
          - app: loom-dbt-runner
            ctx: ./apps/fiab-dbt-runner
          # N4 — dual-engine transformation runtime (dbt-core + SQLMesh + ODBC 18)
          # behind the transformation-project item. Both runner images must be in
          # ACR before the admin-plane activates them (dbtRunnerImageReady).
          - app: loom-transform-runner
            ctx: ./apps/loom-transform-runner
          # N2b/N3 — the DuckDB serving tier + Arrow Flight SQL wire. The image
          # bakes the azure/httpfs/delta/iceberg extensions in at build time (plus
          # `postgres`/`ducklake`, which the DuckLake catalog ATTACH needs), so the
          # running container needs no extension repository (IL5). admin-plane/
          # main.bicep now deploys this tier BY DEFAULT (duckdbTierActive) and binds
          # LOOM_DUCKDB_URL, so the image MUST be in ACR before the app phase.
          - app: loom-duckdb
            ctx: ./apps/loom-duckdb
          # V1 — the Playwright runner image the DEFAULT-ON loom-synthetic-monitor
          # scheduled job executes (`node e2e/run-synthetic.mjs`). The console
          # image is slimmed of e2e/ (.dockerignore), so this second image out of
          # the console context is the only one carrying the journeys. Without it
          # every scheduled execution fails on an unresolvable image and the
          # Health hub's Journeys tab has nothing to list.
          - app: loom-uat
            ctx: ./apps/fiab-console
            file: ./apps/fiab-console/Dockerfile.uat
          # Loom Unity — the Unity-Catalog-compatible OSS metastore. Deployed by
          # the orchestrator on EVERY boundary since svc-loom-unity-authz wired
          # compute/loom-unity-app.bicep (authMode=entra, IP-pinned ingress) +
          # data-plane/loom-unity-postgres.bicep into admin-plane/main.bicep, so
          # the image MUST be in ACR before the admin-plane deploy runs.
          - app: loom-unity
            ctx: ./apps/loom-unity
          # N7e — the DEFAULT-ON Federated SQL engine. Single-node Trino OSS as a
          # scale-to-zero internal-ingress Container App
          # (data-plane/loom-trino-aca.bicep, wired from admin-plane/main.bicep).
          # The image MUST be in ACR before that app can start, otherwise
          # LOOM_TRINO_URL would be wired to an endpoint that never serves.
          - app: loom-trino
            ctx: ./apps/loom-trino
          # M1 — estate-assessment reader (inbound-migration on-ramp). DEFAULT-ON:
          # admin-plane/main.bicep deploys data-plane/loom-migrate-aca.bicep on
          # every apps-enabled deploy and wires LOOM_MIGRATE_URL, so this image is
          # a hard prerequisite of phase 2 (a missing manifest fails the Container
          # App PUT with MANIFEST_UNKNOWN, not just the feature).
          - app: loom-migrate
            ctx: ./apps/loom-migrate
          # N7a — stateful streaming-SQL tier (single-node RisingWave, Apache-2.0).
          # Also DEFAULT-ON (LOOM_RISINGWAVE_URL), same hard prerequisite. The
          # image is a thin pin over the upstream engine binary.
          - app: loom-risingwave
            ctx: ./apps/loom-risingwave
    steps:
      - uses: actions/checkout@v4
      - name: Azure login
        uses: Azure/login@v2
        with:
          creds: |
            {
              "clientId": "${{ secrets.AZURE_CLIENT_ID }}",
              "clientSecret": "${{ secrets.AZURE_CLIENT_SECRET }}",
              "tenantId": "${{ secrets.AZURE_TENANT_ID }}",
              "subscriptionId": "${{ inputs.subscription || secrets.AZURE_SUBSCRIPTION_ID }}"
            }
      # Build + push via ACR Tasks (`az acr build`) — the image is built and
      # pushed SERVER-SIDE inside Azure by ACR, which is a trusted Azure service
      # (the registry has networkRuleBypassOptions: AzureServices). This works
      # even though the registry is publicNetworkAccess=Disabled, so it does NOT
      # depend on the runner's public IP being allow-listed — eliminating the
      # recurring "client with IP '…' is not allowed access" push failures that
      # an external private-by-default Azure Policy kept re-triggering by
      # reverting the temporary public-access toggle mid-build. SHA is stamped so
      # /build-marker.txt matches what loom-roll-and-validate probes. Retried 3×
      # for transient ACR Task scheduling hiccups.
      - name: Stage Copilot RAG corpus (loom-console only)
        if: matrix.app == 'loom-console'
        shell: bash
        run: |
          # Repo-root docs/ + PRPs/ are outside the apps/fiab-console build
          # context; stage a markdown copy in so the loom-docs Copilot index is
          # populated (otherwise it's empty — "No corpus chunks discovered").
          bash scripts/csa-loom/stage-copilot-corpus.sh

      - name: Build + push via ACR Tasks (az acr build)
        shell: bash
        env:
          CONTRACT_REFS: ${{ needs.resolve.outputs.contract_refs }}
        run: |
          ACR_NAME="${{ needs.resolve.outputs.acr_name }}"
          APP="${{ matrix.app }}"; CTX="${{ matrix.ctx }}"; TAG="${{ inputs.tag || 'v0.1' }}"
          # The tag the apps-enabled DEPLOY will pull for this app, derived in
          # the resolve job from params/commercial.bicepparam. Stamped IN
          # ADDITION to :$TAG so a dispatch that passes an explicit tag still
          # leaves the deploy able to pull. Empty for images with no
          # appImageTags key (loom-uat — the jobs pull :latest, which is always
          # stamped; loom-unity — deployed out-of-band, not by admin-plane).
          CONTRACT_TAG=""
          for REF in $CONTRACT_REFS; do
            case "$REF" in
              "$APP":*) CONTRACT_TAG="${REF#*:}" ;;
            esac
          done
          CONTRACT_ARGS=""
          if [ -n "$CONTRACT_TAG" ] && [ "$CONTRACT_TAG" != "$TAG" ]; then
            CONTRACT_ARGS="--image $APP:$CONTRACT_TAG"
            echo "::notice::$APP — also stamping the deploy-contract tag :$CONTRACT_TAG (params/commercial.bicepparam) so an apps-enabled deploy can pull it whatever tag this dispatch used."
          fi
          # All tags in ONE `az acr build` push the SAME manifest, so the cosign
          # signature (which is on the digest) covers the contract tag too — no
          # second signing pass is needed for it to satisfy verify-before-roll.
          # Most apps build ./<ctx>/Dockerfile; loom-uat is a second image out of
          # the console context (Dockerfile.uat), so the matrix may override it.
          FILE="${{ matrix.file || '' }}"; [ -n "$FILE" ] || FILE="$CTX/Dockerfile"
          # loom-uat carries the Playwright specs, but apps/fiab-console/.dockerignore
          # excludes e2e/ + tests/ to keep the CONSOLE image lean — and ACR Tasks
          # does not honour a per-Dockerfile <Dockerfile>.dockerignore. Drop those
          # two exclusions for this build only. Each matrix job has its own
          # disposable checkout, so this cannot leak into the console image.
          if [ "$APP" = "loom-uat" ]; then
            grep -vxE 'e2e|tests' apps/fiab-console/.dockerignore > /tmp/di && mv /tmp/di apps/fiab-console/.dockerignore
            echo "::notice::loom-uat build — e2e/ + tests/ un-ignored for this context"
          fi
          # 2026-07-16: the console compile outgrew the DEFAULT ACR Task agent
          # (8 GB) — consecutive builds were cgroup-OOM-SIGKILLed (runs
          # cjdq..cjdv) with no code change. loom-console builds on the
          # dedicated 16 GB S3 pool `loombuild` (scaled 0↔1 around the build;
          # proven on run cjdy). Other apps stay on default agents.
          POOL_ARGS=""
          if [ "$APP" = "loom-console" ]; then
            az acr agentpool show -r "$ACR_NAME" -n loombuild -o none 2>/dev/null \
              || az acr agentpool create -r "$ACR_NAME" -n loombuild --tier S3 -o none
            CNT=$(az acr agentpool show -r "$ACR_NAME" -n loombuild --query count -o tsv | tr -d '\r')
            [ "${CNT:-0}" -ge 1 ] || az acr agentpool update -r "$ACR_NAME" -n loombuild --count 1 -o none
            POOL_ARGS="--agent-pool loombuild"
          fi
          scale_down_pool() {
            [ -z "$POOL_ARGS" ] && return 0
            q=$(az acr agentpool show -r "$ACR_NAME" -n loombuild --queue-count -o tsv 2>/dev/null | tr -d '\r' || echo 1)
            [ "${q:-1}" = "0" ] && az acr agentpool update -r "$ACR_NAME" -n loombuild --count 0 --no-wait -o none || true
          }
          # RETRY WITH CLASSIFICATION (deploy-integrity.md R6).
          #
          # This used to be `for attempt in 1 2 3; do build; sleep 30; done`
          # with no classification. On run 31022950740 that retried a
          # deterministic `QuotaExceeded: standardDDSv5Family Cores, Location:
          # centralus, Current Limit: 200, Current Usage: 196, Additional
          # Required: 8` three times over 90 seconds and then emitted
          # "$APP ACR build failed after 3 attempts" — a sentence with no cause
          # in it. Retrying a quota denial cannot succeed; those 90 seconds
          # bought nothing but a less accurate error.
          #
          # deploy-retry.mjs retries ONLY the classes named here, fails closed
          # on exhaustion / wall-clock / an unclassifiable failure, and writes
          # deploy-failure.json for the failure notifier to render. The harness is
          # handed the argv directly (spawn, no shell), so $POOL_ARGS is
          # word-split by THIS shell and arrives as real arguments.
          set +e
          node scripts/ci/deploy-retry.mjs \
            --class-allow transient,eventual-consistency \
            --max-attempts 3 --backoff 30 --jitter 0.3 --wall-clock 90m \
            --step "az acr build ($APP)" \
            --artifact "$GITHUB_WORKSPACE/deploy-failure.json" \
            -- az acr build --registry "$ACR_NAME" $POOL_ARGS \
                 --image "$APP:$TAG" --image "$APP:${{ github.sha }}" --image "$APP:latest" \
                 $CONTRACT_ARGS \
                 --build-arg "LOOM_BUILD_SHA=${{ github.sha }}" \
                 --build-arg "LOOM_BUILD_TIMESTAMP=${{ github.run_id }}" \
                 --file "$FILE" "$CTX"
          STATUS=$?
          set -e
          scale_down_pool
          if [[ $STATUS -eq 0 ]]; then echo "✓ $APP built+pushed by ACR Tasks"; fi
          exit $STATUS

      # ── SC1: supply-chain gate — Trivy CRITICAL scan + keyless cosign sign ──
      # Runs while the ACR is still OPEN (open-acr ran; close-acr runs after
      # the whole build matrix + verify-images). The verify-images job below
      # re-verifies every image signature before the roll job may proceed.
      - name: Supply-chain — resolve image digest
        id: sc
        if: ${{ !inputs.skip_supply_chain }}
        shell: bash
        run: |
          set -euo pipefail
          ACR_NAME="${{ needs.resolve.outputs.acr_name }}"
          az acr login --name "$ACR_NAME" >/dev/null
          DIGEST=$(az acr repository show --name "$ACR_NAME" --image "${{ matrix.app }}:${{ github.sha }}" --query digest -o tsv | tr -d '\r')
          if [[ -z "$DIGEST" ]]; then
            echo "::error::Could not resolve the manifest digest for ${{ matrix.app }}:${{ github.sha }} — cannot scan/sign."
            exit 1
          fi
          echo "digest=$DIGEST" >> "$GITHUB_OUTPUT"
          echo "::notice::${{ matrix.app }}:${{ github.sha }} → $DIGEST"

      - name: Supply-chain — Trivy gate (fail on CRITICAL CVEs)
        if: ${{ !inputs.skip_supply_chain }}
        uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1 # 0.35.0
        with:
          image-ref: ${{ needs.resolve.outputs.acr_login_server }}/${{ matrix.app }}:${{ github.sha }}
          severity: CRITICAL
          exit-code: '1'
          ignore-unfixed: true
          # Vuln gating only + reviewed baseline (see .trivyignore) — same
          # posture as build-fiab-images-acr-tasks; secret-scan FPs on minified
          # bundles are excluded, source secret hygiene has its own lane.
          scanners: vuln
          trivyignores: .trivyignore
          format: table

      - name: Supply-chain — install cosign
        if: ${{ !inputs.skip_supply_chain }}
        uses: sigstore/cosign-installer@v3.7.0

      - name: Supply-chain — cosign sign (keyless OIDC)
        if: ${{ !inputs.skip_supply_chain }}
        shell: bash
        run: |
          set -euo pipefail
          REF="${{ needs.resolve.outputs.acr_login_server }}/${{ matrix.app }}@${{ steps.sc.outputs.digest }}"
          echo "Signing $REF keylessly (GitHub Actions OIDC → Fulcio/Rekor) ..."
          if ! cosign sign --yes "$REF"; then
            echo "::error::cosign signing FAILED for $REF. The image is built but UNSIGNED — the verify-images gate will reject the roll. Re-run (transient Sigstore outage), or (emergency only) re-dispatch with skip_supply_chain=true."
            exit 1
          fi
          echo "::notice::Signed $REF."

      - name: Supply-chain — skip notice
        if: ${{ inputs.skip_supply_chain }}
        shell: bash
        run: |
          echo "::warning::skip_supply_chain is set — SKIPPING the SC1 Trivy CRITICAL gate + cosign signing for ${{ matrix.app }}. This image is UNSCANNED and UNSIGNED."

  # ── SC1: verify-before-roll — every image the roll will ship must carry a
  # valid keyless cosign signature from a trusted build workflow before the
  # roll job may proceed. Runs while the ACR is still open (before close-acr).
  # `always()` preserves the pre-SC1 lenient behavior for PARTIAL build
  # failures (fail-fast is off; the roll ships whatever :TAG images exist) —
  # but each existing :TAG image must verify. When skip_build=true the deploy
  # rolls pre-existing images and this job is skipped — the roll step emits a
  # loud warning for that path.
  verify-images:
    name: Verify image signatures (cosign)
    needs: [resolve, build]
    if: ${{ always() && !inputs.skip_build && needs.resolve.result == 'success' }}
    runs-on: ubuntu-latest
    steps:
      # Needed for scripts/ci/deploy-classify.mjs and
      # scripts/ci/assert-acr-image-tags.sh — the gates below distinguish
      # "the registry answered: absent" from "I could not read the registry",
      # and must never skip an image on the strength of the second.
      - uses: actions/checkout@v4
      - name: Azure login
        uses: Azure/login@v2
        with:
          creds: |
            {
              "clientId": "${{ secrets.AZURE_CLIENT_ID }}",
              "clientSecret": "${{ secrets.AZURE_CLIENT_SECRET }}",
              "tenantId": "${{ secrets.AZURE_TENANT_ID }}",
              "subscriptionId": "${{ inputs.subscription || secrets.AZURE_SUBSCRIPTION_ID }}"
            }
      - name: Install cosign
        if: ${{ !inputs.skip_supply_chain }}
        uses: sigstore/cosign-installer@v3.7.0
      - name: Verify every image the roll ships is signed by a trusted workflow
        env:
          CONTRACT_REFS: ${{ needs.resolve.outputs.contract_refs }}
        run: |
          set -uo pipefail
          if [[ "${{ inputs.skip_supply_chain }}" == "true" ]]; then
            echo "::warning::skip_supply_chain is set — SKIPPING the SC1 verify-before-roll gate. The roll ships images WITHOUT supply-chain verification."
            exit 0
          fi
          ACR_NAME="${{ needs.resolve.outputs.acr_name }}"
          ACR_LOGIN="${{ needs.resolve.outputs.acr_login_server }}"
          TAG="${{ inputs.tag || 'v0.1' }}"
          if ! az acr login --name "$ACR_NAME" >/dev/null; then
            echo "::error::Cannot reach the ACR data plane on $ACR_NAME even though open-acr ran this run — cannot verify image signatures, refusing to roll blind. Check the ACR network toggle (an Azure Policy may have reverted it), or (emergency only) re-dispatch with skip_supply_chain=true."
            exit 1
          fi
          IDENTITY_RE="^https://github\\.com/${{ github.repository }}/\\.github/workflows/(build-fiab-images-acr-tasks\\.yml|full-app-deploy-commercial\\.yml|gov-console-roll\\.yml)@"
          # ── WHAT THIS GATE COVERS — DERIVED, NOT HAND-COPIED (refs #3035) ──
          # This used to be a hand-maintained 17-name array under the claim
          # "that is exactly what the redeploy-with-apps job rolls". That claim
          # was FALSE (deploy-integrity.md R7): the roll ships SIX Container
          # Apps. Ten of the other eleven are still roll-blocking for a real
          # reason — the apps-enabled deploy PULLS them (they have appImageTags
          # keys) — but `loom-uat` is neither rolled nor pulled by tag, and on
          # 2026-08-05 run 31066164706 a Trivy CRITICAL in that Playwright test
          # image left it unsigned, failed this gate, and SKIPPED the roll of
          # all six Container Apps. A finding in a test image stopped the
          # console deploying.
          #
          # The set is now derived as ROLL_APPS ∪ the deploy contract, from
          # scripts/ci/deploy-image-roles.mjs — so adding a Container App to
          # the roll, or an appImageTags key to the paramfile, extends this gate
          # automatically. Exclusions are declared in that module WITH a reason,
          # printed on every run (stderr, below), and
          # scripts/ci/check-full-app-deploy-contract.mjs fails if a declaration
          # ever tries to exempt an image that IS rolled or IS in the contract.
          #
          # Nothing here excuses an image from being scanned or signed: every
          # matrix entry still runs the Trivy CRITICAL gate + cosign sign in its
          # own build job, and a failure there still fails that job and this run.
          if [[ -z "${CONTRACT_REFS// /}" ]]; then
            echo "::error::the resolve job produced no deploy contract, so the roll-blocking verify set cannot be derived. Refusing to verify a narrowed set — that is a gate silently measuring less."
            exit 1
          fi
          VERIFY_APPS=$(node scripts/ci/deploy-image-roles.mjs --print verify-set --contract-refs "$CONTRACT_REFS")
          if [[ -z "${VERIFY_APPS// /}" ]]; then
            echo "::error::scripts/ci/deploy-image-roles.mjs produced an EMPTY verify set. Refusing to roll images whose signatures nothing checked."
            exit 1
          fi
          read -ra APPS <<< "$VERIFY_APPS"
          echo "::notice::SC1 verify set (${#APPS[@]} images): ${APPS[*]}"
          FAILED=()
          UNPROVEN=()
          for APP in "${APPS[@]}"; do
            # R7: capture stderr and CLASSIFY it. This used to be
            #     DIGEST=$(az … 2>/dev/null || echo "")
            #     if [[ -z "$DIGEST" ]]; then "…not found… Skipping its verification."
            # which collapsed an RBAC denial, a throttle, a network failure and
            # a genuine 404 into one empty string and then stated the 404 as
            # fact — and, worse, SKIPPED the SC1 supply-chain gate for that
            # image on the strength of a claim it had not established.
            #
            # Absence is now claimed ONLY when the registry actually answered
            # ManifestUnknown/TagNotFound. Anything else is UNPROVEN and FAILS
            # the gate: an unverifiable image must never ship.
            set +e
            SHOW_OUT=$(az acr repository show --name "$ACR_NAME" --image "$APP:$TAG" --query digest -o tsv 2>&1)
            SHOW_RC=$?
            set -e
            if [[ $SHOW_RC -eq 0 ]]; then
              DIGEST=$(printf '%s' "$SHOW_OUT" | tr -d '\r')
            else
              DIGEST=""
              # --query always exits 0: this is a QUERY for the human-readable
              # diagnosis, not a gate. The gate is the --assert-signal below.
              WHY=$(node scripts/ci/deploy-classify.mjs --text "$SHOW_OUT" --query)
              if node scripts/ci/deploy-classify.mjs --text "$SHOW_OUT" \
                   --assert-signal config.image-tag-absent >/dev/null 2>&1; then
                echo "::warning::$APP:$TAG — the registry ANSWERED and the tag is absent (its build leg produced nothing). The roll skips apps with no image; nothing to verify."
                continue
              fi
              echo "::error::$APP:$TAG — could NOT read $ACR_NAME, so the image's existence and signature are UNPROVEN (not disproven). Failing the SC1 gate rather than skipping it. $WHY"
              UNPROVEN+=("$APP")
              continue
            fi
            if [[ -z "$DIGEST" ]]; then
              echo "::error::$APP:$TAG — az exited 0 but returned an EMPTY digest. That is not an answer; the image is UNPROVEN."
              UNPROVEN+=("$APP")
              continue
            fi
            REF="$ACR_LOGIN/$APP@$DIGEST"
            if cosign verify "$REF" \
                --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
                --certificate-identity-regexp "$IDENTITY_RE" \
                --output text >/dev/null 2>&1; then
              echo "✓ $REF signature verified"
            else
              echo "::error::$REF ($APP:$TAG) has NO valid cosign signature from a trusted build workflow."
              FAILED+=("$APP")
            fi
          done
          if (( ${#UNPROVEN[@]} > 0 )); then
            echo "::error::SC1 SUPPLY-CHAIN GATE FAILED — could not READ the registry for: ${UNPROVEN[*]}. Their signatures are UNPROVEN, not disproven (deploy-integrity.md R7). A gate that skips what it cannot read is not a gate. Re-run once the ACR data plane is reachable (scripts/csa-loom/acr-firewall-lease.sh), or (emergency only) re-dispatch with skip_supply_chain=true."
            exit 1
          fi
          if (( ${#FAILED[@]} > 0 )); then
            echo "::error::SC1 SUPPLY-CHAIN GATE FAILED — unsigned image(s): ${FAILED[*]}. The roll job is BLOCKED. These :$TAG images were not produced by the gated CI path (or predate SC1, e.g. a failed build leg left a stale pre-SC1 :$TAG behind). Fix: re-run this workflow so the build job scans+signs them (check the per-app 'Cosign sign' step logs), or (emergency only) re-dispatch with skip_supply_chain=true."
            exit 1
          fi
          echo "::notice::SC1 supply-chain gate PASSED — every existing :$TAG image is cosign-signed by a trusted workflow."

      # ── The image contract (refs #2958) ───────────────────────────────────
      # The cosign gate above is deliberately LENIENT about a missing image: it
      # `continue`s past any :$TAG it cannot find so a partial build failure
      # still rolls whatever DID build. That leniency is what let run
      # 30600323509 print "SC1 supply-chain gate PASSED" after verifying 4 of
      # 16 images, and it is structurally unable to notice the defect that
      # matters here — a tag that was never stamped at all.
      #
      # This step is the opposite and it is NOT optional: every ref the
      # apps-enabled deploy will pull must exist in the registry NOW. Missing
      # means the next `deploy-fiab-commercial` dies at its own image preflight
      # (or, worse, an older deploy adopts the live estate and takes the
      # running Container Apps down with it). Reporting THIS run green while
      # leaving that landmine is exactly the false-green class.
      #
      # Runs even when the cosign step failed (both problems visible in one
      # run) and even under skip_supply_chain — this is a "did you produce what
      # the deploy needs" gate, not a supply-chain gate.
      - name: Image contract — every tag the apps-enabled deploy pulls must now exist
        if: ${{ !cancelled() }}
        env:
          CONTRACT_REFS: ${{ needs.resolve.outputs.contract_refs }}
        run: |
          set -uo pipefail
          if [[ -z "${CONTRACT_REFS// /}" ]]; then
            echo "::error::the resolve job produced no deploy contract — cannot prove this build made the images the deploy needs."
            exit 1
          fi
          # The ACR is still open (open-acr ran this run, close-acr needs this
          # job), so no second firewall lease is taken. assert-acr-image-tags.sh
          # owns the three outcomes: present / absent / unprovable — and an
          # unreadable registry FAILS rather than passing as "not found".
          # shellcheck disable=SC2086
          bash scripts/ci/assert-acr-image-tags.sh \
            --acr "${{ needs.resolve.outputs.acr_name }}" \
            --rg "${{ needs.resolve.outputs.rg }}" \
            $CONTRACT_REFS

  close-acr:
    name: Re-lock ACR (private endpoint only)
    # verify-images is in `needs` so the re-lock only happens after the
    # signature pushes + verification reads are done; `always()` still
    # guarantees the ACR is re-locked even when a gate fails.
    needs: [resolve, build, verify-images]
    # `always()` still guarantees the re-lock when a gate FAILS. But if resolve
    # never produced an ACR, open-acr (which needs resolve) never ran, so there
    # is nothing open and nothing to close — releasing a lease for an empty
    # registry name just adds a confusing red job to an already-failed run.
    if: always() && !inputs.skip_build && needs.resolve.result == 'success'
    runs-on: ubuntu-latest
    steps:
      # Needed for scripts/csa-loom/acr-firewall-lease.sh (#2603).
      - uses: actions/checkout@v4
      - name: Azure login
        uses: Azure/login@v2
        with:
          creds: |
            {
              "clientId": "${{ secrets.AZURE_CLIENT_ID }}",
              "clientSecret": "${{ secrets.AZURE_CLIENT_SECRET }}",
              "tenantId": "${{ secrets.AZURE_TENANT_ID }}",
              "subscriptionId": "${{ inputs.subscription || secrets.AZURE_SUBSCRIPTION_ID }}"
            }
      - name: Disable ACR public access + admin user
        run: |
          set -euo pipefail
          ACR_NAME="${{ needs.resolve.outputs.acr_name }}"
          # The admin user is this run's to turn off either way — it is not part
          # of the shared network toggle. C24 (#3088): the write is VERIFIED
          # rather than `|| true`'d. An admin user left enabled is a live
          # username/password on the registry; reporting that as done without
          # reading it back is the same class of lie as the unverified re-lock
          # below.
          if ! az acr update --name "$ACR_NAME" --admin-enabled false -o none; then
            echo "::warning::az acr update --admin-enabled false returned non-zero for $ACR_NAME; verifying the actual state before deciding."
          fi
          ADMIN="$(az acr show --name "$ACR_NAME" --query adminUserEnabled -o tsv | tr -d '\r')"
          if [ "$ADMIN" != "false" ]; then
            echo "::error::ACR $ACR_NAME still has adminUserEnabled=${ADMIN:-<unreadable>} after the disable. That is a live username/password on the registry. Disable it by hand: az acr update --name $ACR_NAME --admin-enabled false"
            exit 1
          fi
          echo "::notice::ACR $ACR_NAME adminUserEnabled=false (verified)."

          # #2603: `release` re-locks the network ONLY if this run still holds
          # the firewall lease. If another run holds it (a build that started
          # while this deploy was finishing), the registry is left open for
          # them and they re-lock on their own release. If NOBODY holds it, this
          # re-locks anyway — fail closed.
          #
          # C24 (#3088): THIS IS THE STEP FROM THE INCIDENT. After run
          # 31143181962 this job concluded SUCCESS while `az acr show` read
          # publicNetworkAccess=Enabled / defaultAction=Allow on three probes
          # across a minute. `release` now reads the registry back and exits
          # non-zero when it cannot confirm the locked state, so a no-op re-lock
          # turns this job RED instead of green.
          bash scripts/csa-loom/acr-firewall-lease.sh release --acr "$ACR_NAME"

  redeploy-with-apps:
    name: Roll Container Apps to new image
    needs: [resolve, close-acr, verify-images]
    # SC1: the roll additionally requires the verify-images gate to have
    # PASSED (it exits 0 with a loud warning when skip_supply_chain is set,
    # so the emergency valve still rolls). skip_build rolls pre-existing
    # images with no verification — the roll step warns loudly for that path.
    if: always() && inputs.enable_apps_after && (needs.close-acr.result == 'success' || inputs.skip_build) && (needs.verify-images.result == 'success' || inputs.skip_build)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Azure login
        uses: Azure/login@v2
        with:
          creds: |
            {
              "clientId": "${{ secrets.AZURE_CLIENT_ID }}",
              "clientSecret": "${{ secrets.AZURE_CLIENT_SECRET }}",
              "tenantId": "${{ secrets.AZURE_TENANT_ID }}",
              "subscriptionId": "${{ inputs.subscription || secrets.AZURE_SUBSCRIPTION_ID }}"
            }
      # Roll each Container App directly to the freshly-built image. This
      # deliberately does NOT run `az deployment sub create` (full main.bicep):
      # the live env is already provisioned, and re-running the template fights
      # pre-existing drift (RoleAssignmentExists, private-DNS vnet-link Conflict,
      # firewall-policy churn) — none of which has anything to do with rolling
      # an image. `az containerapp update --image` creates a new revision that
      # pulls the updated tag, which is all the app-roll needs.
      - name: Roll Container Apps
        run: |
          set -e
          if [[ "${{ inputs.skip_build }}" == "true" ]]; then
            echo "::warning::skip_build=true — rolling PRE-EXISTING images that were NOT signature-verified by this run (the SC1 verify-images gate only covers images built in-run). Use loom-roll-and-validate for a verified roll of an existing tag."
          fi
          RG='${{ needs.resolve.outputs.rg }}'
          ACR='${{ needs.resolve.outputs.acr_login_server }}'
          TAG='${{ inputs.tag || 'v0.1' }}'
          # Unique per run+attempt so the roll ALWAYS creates a fresh revision.
          # Critical: the image tag (:v0.1) is mutable and overwritten in place,
          # so on a redeploy the only change is the image *digest* behind the
          # unchanged tag. `az containerapp update --image …:v0.1` with an
          # otherwise-identical template is then a NO-OP — no new revision is
          # created and the running revision stays pinned to the OLD digest
          # (Container Apps pins digest at revision-creation). The deploy goes
          # green but Front Door serves stale code. A unique --revision-suffix
          # forces a new revision that re-pulls :v0.1 and gets the new digest.
          SUFFIX="r${{ github.run_id }}-${{ github.run_attempt }}"
          # loom-uat is deliberately NOT here: it is a Container App JOB image
          # (loom-synthetic-monitor), not a Container App, so each scheduled
          # execution pulls :latest on its own — there is no revision to roll.
          #
          # THIS ARRAY IS THE ROLL SET, and the SC1 verify-before-roll gate is
          # derived from it: scripts/ci/check-full-app-deploy-contract.mjs
          # asserts it matches ROLL_APPS in scripts/ci/deploy-image-roles.mjs
          # exactly, both directions. Add an app here and you MUST add it there
          # — otherwise the roll would ship an image no signature gate covered.
          APPS=(loom-console loom-mcp loom-setup-orchestrator loom-activator loom-mirroring loom-direct-lake-shim)
          # RETRY WITH CLASSIFICATION + AN HONEST EXISTENCE PROBE.
          #
          # This block used to hand-roll its own retry (`for attempt in 1 2 3 4
          # 5 6` + `grep -qi OperationInProgress`) - which was the ONE correct,
          # classified retry in the repo, and is exactly what
          # scripts/ci/deploy-retry.mjs generalises. It now uses the shared
          # harness so every deploy path gets the same fail-closed budget, the
          # same taxonomy, and the same deploy-failure.json for the notifier.
          #
          # The existence probe was `az containerapp show ... 2>/dev/null`, so an
          # auth failure, a throttle or a network error all produced
          # "container app $app not found in $RG - skipping", and the roll could
          # report success having rolled a SUBSET (deploy-integrity.md R7).
          # Absence is now claimed only when ARM answered ResourceNotFound.
          for app in "${APPS[@]}"; do
            set +e
            SHOW_OUT=$(az containerapp show -g "$RG" -n "$app" -o none 2>&1)
            SHOW_RC=$?
            set -e
            if [[ $SHOW_RC -ne 0 ]]; then
              if node scripts/ci/deploy-classify.mjs --text "$SHOW_OUT" \
                   --assert-signal config.resource-not-found >/dev/null 2>&1; then
                echo "::warning::ARM answered ResourceNotFound for container app $app in $RG - this estate does not carry it. Skipping."
                continue
              fi
              echo "::error::Could not establish whether container app $app exists in $RG. $(node scripts/ci/deploy-classify.mjs --text "$SHOW_OUT" --query)"
              echo "::error::Refusing to skip it: a roll that silently omits an app reports success having deployed only a subset."
              exit 1
            fi
            echo "::group::roll $app -> $ACR/$app:$TAG"
            node scripts/ci/deploy-retry.mjs \
              --class-allow transient,eventual-consistency \
              --max-attempts 6 --backoff 30 --jitter 0.3 --wall-clock 15m \
              --step "az containerapp update ($app)" \
              --artifact "$GITHUB_WORKSPACE/deploy-failure.json" \
              -- az containerapp update -g "$RG" -n "$app" --image "$ACR/$app:$TAG" --revision-suffix "$SUFFIX" -o none
            echo "::endgroup::"
          done
          CONSOLE_FQDN=$(az containerapp show -g "$RG" -n loom-console --query "properties.configuration.ingress.fqdn" -o tsv 2>/dev/null || echo "")
          echo "::notice::Apps rolled to :$TAG. Console: https://$CONSOLE_FQDN"
      - name: From-cluster app health
        run: bash .github/scripts/fiab-post-provision-validate.sh
        env:
          RG_NAME: ${{ needs.resolve.outputs.rg }}
          BOUNDARY: Commercial

  # dlz-brownfield: publish the COMPILED main.json to the admin storage account
  # and wire LOOM_DLZ_TEMPLATE_URI / LOOM_SETUP_TEMPLATE_URI on the running apps.
  # This is what makes the DAY-ONE in-product "Add landing zone" deploy work: ARM
  # REST needs a compiled template (it can't compile .bicep), and the Console's
  # user-ARM tier only runs when LOOM_DLZ_TEMPLATE_URI is set — otherwise it falls
  # to the honest copy-paste gate. Wiring it here (not just a live patch) means a
  # FRESH from-scratch deploy stands the day-one path up. Idempotent + best-effort:
  # every step is guarded so it never fails the app roll (no-vaporware).
  publish-dlz-template:
    name: Publish DLZ template + wire deploy env
    needs: [resolve, redeploy-with-apps]
    if: ${{ always() && inputs.enable_apps_after && needs.redeploy-with-apps.result == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Azure login
        uses: Azure/login@v2
        with:
          creds: |
            {
              "clientId": "${{ secrets.AZURE_CLIENT_ID }}",
              "clientSecret": "${{ secrets.AZURE_CLIENT_SECRET }}",
              "tenantId": "${{ secrets.AZURE_TENANT_ID }}",
              "subscriptionId": "${{ inputs.subscription || secrets.AZURE_SUBSCRIPTION_ID }}"
            }
      - name: Setup Bicep
        run: az bicep install
      - name: Compile + publish main.json, wire LOOM_DLZ_TEMPLATE_URI / LOOM_SETUP_TEMPLATE_URI
        run: |
          set -e
          RG='${{ needs.resolve.outputs.rg }}'
          CONTAINER=arm-templates
          # 1) Compile the full template ARM REST will fetch via templateLink.
          az bicep build -f platform/fiab/bicep/main.bicep --outfile main.json
          # 2) Discover the admin storage account (deployment artifacts). Prefer a
          #    Loom-named account; fall back to the first account in the RG.
          SA=$(az storage account list -g "$RG" --query "[?starts_with(name,'stloom')||starts_with(name,'saloom')||starts_with(name,'stcsaloom')].name | [0]" -o tsv 2>/dev/null || echo "")
          if [[ -z "$SA" ]]; then SA=$(az storage account list -g "$RG" --query "[0].name" -o tsv 2>/dev/null || echo ""); fi
          if [[ -z "$SA" ]]; then
            echo "::warning::No storage account in $RG — cannot publish main.json; the day-one deploy stays on the honest copy-paste gate."; exit 0
          fi
          # Sovereign-safe blob base (handles Gov endpoints automatically).
          BLOB_BASE=$(az storage account show -g "$RG" -n "$SA" --query "primaryEndpoints.blob" -o tsv)
          BLOB_URL="${BLOB_BASE}${CONTAINER}/main.json"
          # 3) Upload (AAD auth; the deploy SP holds Storage Blob Data Contributor
          #    or the account key path below covers it).
          az storage container create --account-name "$SA" --name "$CONTAINER" --auth-mode login -o none 2>/dev/null || true
          if ! az storage blob upload --account-name "$SA" --container-name "$CONTAINER" --name main.json --file main.json --auth-mode login --overwrite -o none 2>/dev/null; then
            KEY=$(az storage account keys list -g "$RG" -n "$SA" --query "[0].value" -o tsv 2>/dev/null || echo "")
            az storage container create --account-name "$SA" --name "$CONTAINER" --account-key "$KEY" -o none 2>/dev/null || true
            az storage blob upload --account-name "$SA" --container-name "$CONTAINER" --name main.json --file main.json --account-key "$KEY" --overwrite -o none || { echo "::warning::main.json upload failed — day-one deploy stays gated."; exit 0; }
          fi
          # 4) Read SAS so ARM's templateLink can fetch a PRIVATE blob (long-lived).
          EXPIRY=$(date -u -d '+3 years' '+%Y-%m-%dT%H:%MZ' 2>/dev/null || date -u -v+3y '+%Y-%m-%dT%H:%MZ')
          KEY=${KEY:-$(az storage account keys list -g "$RG" -n "$SA" --query "[0].value" -o tsv 2>/dev/null || echo "")}
          SAS=""
          if [[ -n "$KEY" ]]; then
            SAS=$(az storage blob generate-sas --account-name "$SA" --account-key "$KEY" --container-name "$CONTAINER" --name main.json --permissions r --expiry "$EXPIRY" -o tsv 2>/dev/null || echo "")
          fi
          # 5) Wire the apps (idempotent). LOOM_DLZ_TEMPLATE_URI on the Console
          #    (day-one user-ARM tier) + LOOM_SETUP_TEMPLATE_URI on the orchestrator.
          CONSOLE_VARS="LOOM_DLZ_TEMPLATE_URI=$BLOB_URL LOOM_SETUP_TEMPLATE_URI=$BLOB_URL"
          if [[ -n "$SAS" ]]; then CONSOLE_VARS="$CONSOLE_VARS LOOM_DLZ_TEMPLATE_QUERY_STRING=?$SAS"; fi
          az containerapp update -g "$RG" -n loom-console --set-env-vars $CONSOLE_VARS -o none || echo "::warning::could not set loom-console template env"
          if az containerapp show -g "$RG" -n loom-setup-orchestrator -o none 2>/dev/null; then
            ORCH_VARS="LOOM_SETUP_TEMPLATE_URI=$BLOB_URL"
            if [[ -n "$SAS" ]]; then ORCH_VARS="$ORCH_VARS LOOM_DLZ_TEMPLATE_QUERY_STRING=?$SAS"; fi
            az containerapp update -g "$RG" -n loom-setup-orchestrator --set-env-vars $ORCH_VARS -o none || echo "::warning::could not set loom-setup-orchestrator template env"
          fi
          echo "::notice::Published main.json → $BLOB_URL and wired LOOM_DLZ_TEMPLATE_URI / LOOM_SETUP_TEMPLATE_URI (day-one Add-landing-zone deploy enabled)."

  # rel-T34: chain the post-deploy bootstrap after the apps are LIVE. This is the
  # true Commercial from-scratch completion — infra deploy (apps off) → this
  # workflow builds images + brings the Container Apps up → bootstrap wires
  # sign-in (MSAL) + data-plane grants. Runs the ONE reusable bootstrap
  # (secrets: inherit) so the logic isn't copy-pasted per cloud.
  post-deploy-bootstrap:
    name: Post-deploy bootstrap (Commercial)
    needs: [resolve, redeploy-with-apps]
    if: ${{ inputs.enable_apps_after && needs.redeploy-with-apps.result == 'success' }}
    uses: ./.github/workflows/csa-loom-post-deploy-bootstrap.yml
    secrets: inherit
    with:
      boundary: Commercial
      region: ${{ needs.resolve.outputs.region }}
      admin_subscription: ${{ needs.resolve.outputs.admin_sub }}

  # E4 (loom-next-level ws-copilot-cost): fire a post-deploy Copilot quality
  # eval run so EVERY roll re-baselines quality against the freshly-staged
  # corpus. Fire-and-forget + report: one short POST per surface (the HTTP
  # response has a ~230s ALB ceiling and a full run far exceeds it; the
  # Function keeps executing after the client disconnects, one surface per
  # invocation stays inside the Y1 execution window). Scores land in Cosmos
  # `loom-copilot-evals` (gating + sticky comments live in
  # copilot-quality-evals.yml; browse on /admin/copilot-quality once E5 lands).
  # Token spend is bounded by E2's LOOM_COPILOT_EVAL_JUDGE_DAILY_CAP. Never
  # fails the deploy (continue-on-error).
  post-deploy-evals:
    name: Post-deploy Copilot quality evals (fire-and-forget)
    needs: [resolve, redeploy-with-apps]
    if: ${{ always() && inputs.enable_apps_after && needs.redeploy-with-apps.result == 'success' }}
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Azure login
        uses: Azure/login@v2
        with:
          creds: |
            {
              "clientId": "${{ secrets.AZURE_CLIENT_ID }}",
              "clientSecret": "${{ secrets.AZURE_CLIENT_SECRET }}",
              "tenantId": "${{ secrets.AZURE_TENANT_ID }}",
              "subscriptionId": "${{ inputs.subscription || secrets.AZURE_SUBSCRIPTION_ID }}"
            }
      - name: Dispatch per-surface eval runs on the copilot-evaluator Function
        run: |
          set -uo pipefail
          RG='${{ needs.resolve.outputs.rg }}'
          FUNC=$(az functionapp list -g "$RG" --query "[?starts_with(name,'func-cpeval-')].name | [0]" -o tsv 2>/dev/null | tr -d '\r' || true)
          if [ -z "$FUNC" ]; then
            echo "::warning::copilot-evaluator Function (func-cpeval-*) not found in $RG — svc-copilot-evaluator gate unresolved; post-deploy eval re-baseline skipped."
            exit 0
          fi
          HOST=$(az functionapp show -g "$RG" -n "$FUNC" --query defaultHostName -o tsv | tr -d '\r')
          KEY=$(az functionapp keys list -g "$RG" -n "$FUNC" --query "functionKeys.default || masterKey" -o tsv | tr -d '\r')
          if [ -z "$KEY" ]; then echo "::warning::no function key readable for $FUNC — eval dispatch skipped."; exit 0; fi
          echo "::add-mask::$KEY"
          DISPATCHED=""
          for F in content/evals/*.jsonl; do
            S=$(basename "$F" .jsonl)
            case "$S" in _*) continue ;; esac
            # fire-and-forget: a 15s client timeout only abandons the RESPONSE;
            # the invocation continues server-side to completion.
            curl -sS --max-time 15 -o /dev/null \
              -X POST "https://$HOST/api/copilotEvaluatorHttp?code=$KEY" \
              -H 'content-type: application/json' \
              -d "{\"surfaces\":[\"$S\"],\"trigger\":\"corpus\"}" || true
            DISPATCHED="$DISPATCHED $S"
          done
          echo "::notice::post-deploy eval runs dispatched for:$DISPATCHED — scores land in Cosmos loom-copilot-evals (runId trigger=corpus); the copilot-quality-evals workflow + /admin/copilot-quality read them."

  notify:
    name: Notify final state
    needs: [resolve, build, close-acr, redeploy-with-apps]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Was: a comment on `issue_number: 279` - a CLOSED roadmap issue with 289
      # comments. Every run, success or failure, was appended there, which is
      # why nobody noticed this workflow had been failing for weeks
      # (deploy-integrity.md). A SUCCESS now writes a job-summary line and
      # opens nothing; a FAILURE opens/updates one dedicated OPEN issue.
      - name: Summarise
        run: |
          echo "full-app-deploy-commercial run ${{ github.run_id }}: build=${{ needs.build.result }}, close-acr=${{ needs.close-acr.result }}, redeploy=${{ needs.redeploy-with-apps.result }}" >> "$GITHUB_STEP_SUMMARY"
      - name: Notify on failure (dedicated, OPEN issue)
        if: ${{ needs.redeploy-with-apps.result != 'success' || needs.build.result == 'failure' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          node .github/scripts/deploy-notify-failure.mjs \
            --workflow full-app-deploy-commercial \
            --failure-json "$GITHUB_WORKSPACE/deploy-failure.json"
