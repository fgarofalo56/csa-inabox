# CSA Loom — Trivy CRITICAL-gate baseline (SC1)
#
# The blocking build gate (build-fiab-images-acr-tasks.yml "Trivy gate") reads
# this file. Every entry is a KNOWN, REVIEWED finding that the gate ignores,
# with the reason and the burn-down path. Adding an entry requires the same
# review a guard-allowlist entry gets: an ignore is a LOAN, not a fix — it must
# name the upstream that would clear it and be removed the moment that ships.
#
# ── BASELINE IS EMPTY (B-SC1', loom-apex, 2026-07-28) ──────────────────────
# All three baselined CVEs were burned down by fixing the images, not by
# widening this file. Kept below as the audit trail.
#
#   CVE-2026-59873 — node-tar gzip-bomb DoS, in the copy of node-tar VENDORED
#     inside npm in the node base images (loom-console, loom-copilot-maf,
#     loom-mcp-bridge). Fixed in tar >= 7.5.19, first bundled by npm 11.18.0 —
#     and NO node base image ships a patched npm yet (node 22.23.1 -> npm
#     10.9.8 -> tar 7.5.11; node 26.5.0 -> npm 11.17.0 -> tar 7.5.16, both read
#     out of the published npm tarballs on registry.npmjs.org). A base bump
#     alone would NOT have cleared it, so it was resolved per image:
#       - loom-console / loom-copilot-maf / loom-onelake / lineage-extractor:
#         the runtime never invokes a package manager, so npm + npx are DELETED
#         from the final layer — the vulnerable code is gone, not muted.
#       - loom-mcp-bridge: it SPAWNS npx at runtime, so npm is upgraded in
#         place to the patched 11.18.0 (ARG NPM_VERSION in its Dockerfile).
#
#   CVE-2024-47561 — Apache Avro deserialization, in avro-1.11.3.jar vendored
#     by the upstream Debezium Connect base (loom-mirroring). Base bumped
#     quay.io/debezium/connect 3.0.0.Final -> 3.5.2.Final, which carries
#     avro-1.12.0.jar via apicurio-registry 2.6.13.Final (both converter
#     tarballs listed off Maven Central before the bump).
#
#   CVE-2026-33845 — libgnutls30 DTLS reassembly OOB read in the loom-mcp-bridge
#     OS layer. Debian fixed it in gnutls28 3.7.9-2+deb12u7 (bookworm) /
#     3.8.9-3+deb13u4 (trixie) per the Debian security tracker. The bridge
#     Dockerfile's leading `apt-get update && apt-get dist-upgrade -y` (landed
#     in #2527) pulls deb12u7 ahead of the pinned base tag, so the CVE is moot
#     in the built image — the entry was stale, not deferred.
#
# Node majors: node 20 "Iron" hit EOL 2026-04-30 and Docker Hub froze every
# node:20* tag on 2026-04-22, so every Loom node image moved to node 22 "Jod"
# (LTS through 2027-04-30). Next base-image review is due before that date.
#
# ── 2026-07-28: loom-risingwave + loom-unity burned down, STILL EMPTY ──────
# The two newest images were the last CRITICAL holdouts (they blocked the
# svc-loom-risingwave and svc-loom-unity-authz gates). Both were fixed in the
# image; nothing was added to this file. Recorded here for the audit trail.
#
#   loom-risingwave (risingwavelabs/risingwave:v2.1.3): 105 CRITICAL -> 0.
#     - 84 kernel CVEs across linux-libc-dev + linux-tools-{6.8.0-52,
#       6.8.0-52-generic,common}, all 6.8.0-52.53. `dist-upgrade` alone does NOT
#       clear the linux-tools set (ABI-pinned: it installs a parallel 6.8.0-136
#       set and leaves 6.8.0-52 behind), so the Dockerfile PURGES linux-tools-*
#       first and then dist-upgrades, taking linux-libc-dev to 6.8.0-136.136.
#     - 19 jackson-databind 2.4.0 CVEs (the polymorphic-deserialization gadget
#       family) SHADED inside htrace-core-3.2.0-incubating.jar. HTrace is in the
#       Apache Attic with no 3.x successor and 4.x renamed the API, so there is
#       no upgrade; the jar is DELETED. It sits in a dead 15-jar island — a
#       constant-pool sweep of the other 460 jars in connector-node/libs finds
#       ZERO references to org/apache/htrace, org/apache/hadoop/hbase or
#       co/cask/tephra, and no META-INF/services entry names them.
#     - CVE-2024-47561 in avro-1.11.3.jar (the same CVE burned down on
#       loom-mirroring above): UPGRADED in place to 1.11.4 with a pinned sha256.
#     - CVE-2025-30065 in parquet-avro-1.12.3.jar: DELETED, not upgraded. Every
#       parquet-avro >= 1.11 needs org.apache.parquet.schema.LogicalTypeAnnotation
#       and the only parquet core on that classpath is parquet-hadoop-bundle
#       1.10.0, so the class cannot load in the unmodified base either — shipping
#       1.15.x would be a newer version string on the same unloadable jar.
#     A build-time JVM link check (apps/loom-risingwave/scripts) proves every
#     affected class still resolves; it is what rejected the first htrace fix.
#
#   loom-unity (unitycatalog/unitycatalog:v0.5.0): 3 CRITICAL -> 0.
#     - CVE-2026-31789 (openssl heap overflow) in libcrypto3 + libssl3 3.3.6-r0.
#       alpine v3.20 already publishes 3.3.7-r0, so the base tag was simply
#       behind: `apk upgrade --no-cache` — the alpine analogue of the
#       dist-upgrade above — pulls it ahead of the pin.
#     - CVE-2025-14813 in bcprov-jdk18on-1.80.jar, one of the 554 jars the upstream
#       image bakes into its sbt/coursier cache that NO classpath file names.
#       Removed with the rest of the unreferenced set; the keep-set is derived
#       from the image's own eight classpath files at build time and every
#       surviving entry is asserted, so the prune cannot cut live code.
#
# ── 2026-09-11: CVE-2026-75595 (io.netty:netty-handler), STILL EMPTY ──────────
# #4429. The image-build lane failed 9 consecutive runs, 2026-09-09T02:22Z
# onward, on four jobs (loom-mirroring, loom-risingwave, loom-trino, loom-unity);
# the Gov lane (gov-build-images.yml) failed alongside it on loom-unity. Nothing
# in Loom changed: the last green run was 2026-09-09T00:24Z, two hours earlier,
# with no commit to any of the four Dockerfiles. A Trivy DB entry landed.
# Upstream fixed the CVE in netty 4.1.137.Final (2026-08-06) and 4.2.17.Final
# (2026-08-04); every image takes the MINIMAL version on its own netty line
# (4.2.18.Final exists and was deliberately not taken).
#
# A BASE BUMP WAS CHECKED FIRST FOR ALL FOUR AND IS NOT THE ANSWER:
#   - loom-trino:      trinodb/trino:483 IS :latest (identical digest); tags
#                      484..495 all 404. Nothing to bump to.
#   - loom-mirroring:  debezium/connect 3.6.2.Final exists, but its Oracle
#                      connector plugin tarball on Maven Central STILL ships
#                      netty-handler-4.1.128.Final -- the bump would change the
#                      connector runtime and NOT clear the CVE.
#   - loom-unity:      unitycatalog/unitycatalog:v0.6.0 exists, but moving the
#                      base trips this image's own base-drift assertion and
#                      invalidates the narrowed three-class #1603 overlay. That
#                      is a separately-validated upgrade, not a CVE patch.
#   - loom-risingwave: the pinned v2.1.3 is already a deliberate pin against a
#                      MAJOR engine bump (see the 2026-07-28 entry above).
#
#   loom-trino (trinodb/trino:483): 1 reported CRITICAL, FIFTEEN physical copies.
#     Trivy de-duplicates per (package, version) and printed only
#     plugin/cassandra; the image actually carries byte-identical copies of
#     io.netty_netty-handler-4.2.16.Final.jar in 15 plugin directories, each its
#     own isolated classloader. All 15 UPGRADED in place to 4.2.17.Final with a
#     pinned sha256, and the count is ASSERTED so a base that grows or loses a
#     plugin fails the build instead of being half-patched.
#
#   loom-mirroring (quay.io/debezium/connect:3.5.2.Final): 1 CRITICAL, 1 copy
#     (whole-filesystem find, not inferred from Trivy's table). UPGRADED in place
#     4.1.128.Final -> 4.1.137.Final with a pinned sha256.
#
#   loom-risingwave (risingwavelabs/risingwave:v2.1.3): 1 CRITICAL, and the ONE
#     image where upgrading the flagged jar alone BREAKS. netty-handler 4.1.137's
#     LazyX509Certificate$CertFactoryHandle references
#     io.netty.util.Recycler$EnhancedHandle, which netty-common 4.1.77 does not
#     carry. So the SIX-MODULE core (common, buffer, codec, resolver, transport,
#     handler) was lifted 4.1.77 -> 4.1.137 and the 4.1.100 leaf modules left
#     alone -- a 4.1.100 leaf on a 4.1.137 core is the safe direction, and the
#     lift also REPAIRS 182 references that were already dangling in the
#     unmodified base.
#
#   loom-unity (unitycatalog/unitycatalog:v0.5.0): TWO CRITICALs under the same
#     CVE id, and the second is easy to miss -- `Total: 2 (CRITICAL: 2)` while
#     the table wraps the second row onto a continuation line.
#     - netty-handler 4.1.112.Final on the SERVER classpath: UPGRADED in place to
#       4.1.137.Final, with every classpath file repointed and asserted.
#     - netty-handler 4.1.115.Final SHADED inside the 641 MB awssdk fat jar
#       bundle-2.29.52.jar: REMOVED. Of the image's eight classpath files exactly
#       one names it -- examples/cli/target/classpath, the upstream demo CLI --
#       and the Loom entrypoint runs bin/start-uc-server, never bin/uc. Same
#       disposition, same reason, as the npm/npx deletions above: the runtime
#       never invokes the tool. The examples tree and bin/uc go with it, so the
#       image does not ship a launcher that would now fail. Upgrading instead
#       would mean AWS SDK 2.29.52 -> 2.54.x (the first line whose netty.version
#       is 4.1.137.Final) and a 641 MB download per build, for a demo CLI.
#
#   Every swap is proved twice. At AUTHORING time by scripts/ci/netty_link_check.py
#   (forward / backward / callers / swap, each against a CONTROL, over the jars
#   extracted from the pinned images). At BUILD time by NettyLinkCheck.java in
#   each app's scripts/ dir, which re-runs the class-level half inside the image
#   against its REAL classpath -- also against a control taken before the swap,
#   so the verdict is the DIFFERENCE and not a raw unresolved set. Each script
#   additionally pins the digest of the jar it replaces, so a re-pushed base tag
#   fails the build rather than being patched on top of something the analysis
#   never saw.
#
# Do NOT add an entry here to make a build go green. Fix the image, or open the
# gate's emergency valve (skip_supply_chain) with an incident note.
