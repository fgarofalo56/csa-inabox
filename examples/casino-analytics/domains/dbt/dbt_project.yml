name: 'csa_casino'
version: '1.0.0'
config-version: 2
require-dbt-version: [">=1.7.0", "<2.0.0"]

profile: 'casino_analytics'

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["../../../domains/shared/dbt/macros"]

vars:
  bronze_database: "bronze"
  silver_database: "silver"
  gold_database: "gold"
  file_format: "delta"
  incremental_strategy: "merge"
  freshness_warn_hours: 24
  freshness_error_hours: 48

  # Casino-specific variables
  gaming_day_start_hour: 6          # Gaming day starts at 6:00 AM local
  ctr_threshold: 10000              # Title 31 CTR threshold ($10,000)
  structuring_threshold: 8000       # Structuring detection lower bound
  lookback_days: 90                 # Default analysis window

  # Player tier thresholds (ADT-based)
  tier_thresholds:
    bronze: 0
    silver: 50
    gold: 150
    platinum: 400
    diamond: 1500

  # Slot machine configuration
  target_hold_pct: 8.0              # Target hold percentage
  hold_variance_alert: 2.0          # Alert if actual hold deviates > 2% from theoretical

  # Floor zones
  floor_zones: ['A1', 'A2', 'B1', 'B2', 'C1', 'C2', 'D1', 'VIP']

  # Churn risk thresholds
  churn_high_risk_days: 45          # Days since last visit = high churn risk
  churn_medium_risk_days: 30        # Days since last visit = medium churn risk

models:
  csa_casino:
    bronze:
      +materialized: incremental
      +file_format: delta
      +schema: bronze
      +tags: ['bronze', 'casino', 'raw']
      +unique_key: ['source_system', 'ingestion_timestamp', 'record_hash']
    silver:
      +materialized: incremental
      +file_format: delta
      +schema: silver
      +tags: ['silver', 'casino', 'cleaned']
      +incremental_strategy: merge
      +on_schema_change: "sync_all_columns"
    gold:
      +materialized: table
      +file_format: delta
      +schema: gold
      +tags: ['gold', 'casino', 'analytics']
      +post-hook: "OPTIMIZE {{ this }} ZORDER BY (reporting_date)"

seeds:
  csa_casino:
    +schema: seeds
    +tags: ['seed', 'casino', 'synthetic']

# sources: moved to models/schema.yml where dbt expects them.

snapshots:
  csa_casino:
    +target_schema: snapshots
    +strategy: timestamp
    +updated_at: processed_timestamp
