// apex A2: segment loading state for /data-agent (shared centered spinner -
// see lib/components/route-loading.tsx).
import { RouteLoading } from '@/lib/components/route-loading';

export default function DataAgentLoading() {
  return <RouteLoading section="the Data agent" />;
}
