// apex A2: segment loading state for /external-shares (shared centered spinner -
// see lib/components/route-loading.tsx).
import { RouteLoading } from '@/lib/components/route-loading';

export default function ExternalSharesLoading() {
  return <RouteLoading section="External shares" />;
}
