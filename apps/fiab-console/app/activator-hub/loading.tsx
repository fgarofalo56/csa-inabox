// apex A2: segment loading state for /activator-hub (shared centered spinner -
// see lib/components/route-loading.tsx).
import { RouteLoading } from '@/lib/components/route-loading';

export default function ActivatorHubLoading() {
  return <RouteLoading section="the Activator hub" />;
}
