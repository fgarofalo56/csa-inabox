// apex A2: segment loading state for /developer (shared centered spinner -
// see lib/components/route-loading.tsx).
import { RouteLoading } from '@/lib/components/route-loading';

export default function DeveloperLoading() {
  return <RouteLoading section="the Developer hub" />;
}
