// apex A2: segment loading state for /activator (shared centered spinner -
// see lib/components/route-loading.tsx).
import { RouteLoading } from '@/lib/components/route-loading';

export default function ActivatorLoading() {
  return <RouteLoading section="Activator" />;
}
