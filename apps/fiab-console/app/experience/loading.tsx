// apex A2: segment loading state for /experience (shared centered spinner -
// see lib/components/route-loading.tsx).
import { RouteLoading } from '@/lib/components/route-loading';

export default function ExperienceLoading() {
  return <RouteLoading section="this experience" />;
}
