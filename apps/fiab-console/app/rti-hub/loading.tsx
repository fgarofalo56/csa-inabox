// apex A2: segment loading state for /rti-hub (shared centered spinner -
// see lib/components/route-loading.tsx).
import { RouteLoading } from '@/lib/components/route-loading';

export default function RtiHubLoading() {
  return <RouteLoading section="the RTI hub" />;
}
