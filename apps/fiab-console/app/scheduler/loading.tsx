// apex A2: segment loading state for /scheduler (shared centered spinner -
// see lib/components/route-loading.tsx).
import { RouteLoading } from '@/lib/components/route-loading';

export default function SchedulerLoading() {
  return <RouteLoading section="the Scheduler" />;
}
