// apex A2: segment loading state for /workload-hub (shared centered spinner -
// see lib/components/route-loading.tsx).
import { RouteLoading } from '@/lib/components/route-loading';

export default function WorkloadHubLoading() {
  return <RouteLoading section="the Workload hub" />;
}
