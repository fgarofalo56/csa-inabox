// apex A2: segment loading state for /business-events (shared centered spinner -
// see lib/components/route-loading.tsx).
import { RouteLoading } from '@/lib/components/route-loading';

export default function BusinessEventsLoading() {
  return <RouteLoading section="Business events" />;
}
