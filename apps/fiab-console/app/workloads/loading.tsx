// apex A2: segment loading state for /workloads (shared centered spinner -
// see lib/components/route-loading.tsx).
import { RouteLoading } from '@/lib/components/route-loading';

export default function WorkloadsLoading() {
  return <RouteLoading section="Workloads" />;
}
