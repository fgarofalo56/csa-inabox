{
  "//": "Registration example for the M1 loom-catalog MCP server. Copy the mcpServers entry into your client's .mcp.json (Claude Code / Cursor / Windsurf) or project config. Build first: see apps/loom-mcp/README.md. Replace LOOM_TOKEN with a loom_pat_<id>_<secret> from Settings -> Developer -> API tokens, and LOOM_API_URL with your estate. Do NOT commit a real token.",
  "mcpServers": {
    "loom-catalog": {
      "command": "node",
      "args": ["./apps/loom-mcp/dist/servers/loom-catalog/bin.js"],
      "env": {
        "LOOM_API_URL": "https://csa-loom.limitlessdata.ai",
        "LOOM_TOKEN": "loom_pat_REPLACE_ME"
      }
    },
    "loom-catalog-docker": {
      "command": "docker",
      "args": ["run", "--rm", "-i", "-e", "LOOM_API_URL", "-e", "LOOM_TOKEN", "loom-catalog-mcp"],
      "env": {
        "LOOM_API_URL": "https://csa-loom.limitlessdata.ai",
        "LOOM_TOKEN": "loom_pat_REPLACE_ME"
      }
    }
  }
}
