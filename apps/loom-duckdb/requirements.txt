fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
# --- the engine ---------------------------------------------------------------
# DuckDB is a single embedded OSS binary; the azure/httpfs/delta/iceberg
# extensions are INSTALLed into the image at build time (see Dockerfile) so a
# disconnected IL5 enclave never reaches an extension repository at runtime.
duckdb==1.5.5
# pyarrow provides both the Arrow IPC serialization for the HTTP tier and the
# Flight SQL server for the ADBC/JDBC serving wire (N3).
pyarrow==25.0.1
azure-identity==1.25.3
