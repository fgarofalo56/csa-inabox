> **Title:** Copilot & AI appendix (G1-G6) — 2026-07-08 code re-verification
> **Date:** 2026-07-08 · **Status:** verified addendum to `PRPs/completed/fabric-parity/appendix-copilot-ai.md`
> **Method:** per-gap code verification against origin/main (post-Wave-7, post-rel-T85) — the 06-26 appendix understates shipped progress.

# Copilot-AI gap re-verification (G1-G6)

These items feed the wave plan in `WAVES.md` alongside the five PRPs. Resolved-since-appendix work is noted so it is never re-proposed.

## G1 — **PARTIAL** (effort L)

G1 — Copilot-builder primitive (inline NL builder pane) on every design surface (~10 per-pane personas: warehouse, notebook, lakehouse, pipeline, eventstream, kql-database/queryset, kql-dashboard, semantic-model, dataflow, plus MLV/mirrored-db/automl/graph called out in the appendix's "missing" list)

**Evidence (current code):**
- apps/fiab-console/lib/azure/copilot-personas.ts:29-34 — persona registry (client-safe types) only defines 'notebook'|'warehouse'|'pipeline'|'lakehouse'|'default'; no eventstream/stream-analytics/semantic-model/mlv/mirrored-db/graph persona type
- apps/fiab-console/lib/azure/ — dedicated per-workload persona files exist for notebook, pipeline, dataflow, kql (copilot-personas-kql.ts KQL_COPILOT_PERSONA), dax, sql, activator, slash — i.e. 8 personas total are wired end-to-end, not the full ~10-14 the appendix targets
- apps/fiab-console/lib/editors/phase3-editors.tsx:3420-3658 KqlDatabaseEditor — real NL2KQL/explain/fix Copilot wired to POST /api/items/kql-database/[id]/assist (real AOAI, schema-grounded) — RESOLVED beyond appendix's stale gap #8
- apps/fiab-console/lib/editors/phase3-editors.tsx:6310-7230 KqlDashboardEditor — 'Add tile with Copilot' NL→KQL tile generator wired to real POST /api/items/kql-dashboard/[id]/generate-tile (validates by executing KQL against ADX) — RESOLVED beyond appendix's gap #9
- apps/fiab-console/lib/editors/phase3-editors.tsx:11165-11380 SemanticModelCopilotPane + app/api/items/semantic-model/[id]/copilot-structure/route.ts — real AOAI propose/apply/checkpoint/restore for NL model-structure edits (audit-T82, dated after appendix) — RESOLVED beyond appendix's gap #6/#28 partial
- apps/fiab-console/lib/editors/phase3-editors.tsx:7445-8826 EventstreamEditor — 0 matches for Copilot/Sparkle/assist across the full ~1380-line editor body — STILL MISSING
- apps/fiab-console/lib/editors/stream-analytics-editor.tsx, lakehouse-editor.tsx (tabs Files/Tables/History/Schemas/Preview/SQL/Shortcuts/Security, no Copilot tab), materialized-lake-view-editor.tsx, mirrored-database-editor.tsx, ml-experiment-editor.tsx, automl-editor.tsx (one decorative Sparkle24Regular icon only, no builder), graph-editors.tsx — all 0 matches for copilot/assist/NL2 — STILL MISSING
- No reusable `<CopilotBuilderPane>` primitive exists anywhere in the repo (0 grep hits) — each surface that IS built (kql-database, kql-dashboard, semantic-model, activator) is a bespoke one-off implementation rather than the shared component the appendix's design proposed

**Remaining work:**
- [x] Eventstream inline Copilot builder: new persona (lib/azure/copilot-personas-eventstream.ts) + new app/api/items/eventstream/[id]/assist/route.ts (real AOAI grounded on live topology/sources/transforms via eventhubs-client) + wire a Copilot dock into lib/editors/phase3-editors.tsx EventstreamEditor (~line 7445)
- [x] Stream Analytics NL→SAQL builder: persona + app/api/items/stream-analytics/[id]/assist route (real ARM query validate) + Copilot pane in lib/editors/stream-analytics-editor.tsx
- [x] Lakehouse editor Copilot tab: add a 'Copilot' tab alongside Files/Tables/History/Schemas/Preview/SQL/Shortcuts/Security in lib/editors/lakehouse-editor.tsx, reusing the existing 'lakehouse' persona type in lib/azure/copilot-personas.ts (defined but not surfaced) wired to a real ADLS/Synapse-grounded assist route
- [x] Materialized Lake View NL builder (view-definition authoring): persona + route + pane in lib/editors/materialized-lake-view-editor.tsx (backend: materialized-lake-view-engine.ts)
- [x] Mirrored Database Copilot (explain mirroring status / NL query over source): persona + route + pane in lib/editors/mirrored-database-editor.tsx
- [x] ML Experiment / AutoML Copilot (NL experiment config + run explanation): persona + route + pane in lib/editors/ml-experiment-editor.tsx and lib/editors/automl-editor.tsx (backend: aml-automl-client.ts)
- [x] Graph editors NL2GQL builder: persona + route (kusto-client make-graph/graph-match/openCypher) + pane in lib/editors/graph-editors.tsx
- [x] Optional but recommended: extract a shared `<CopilotBuilderPane contextSlug contextPayload>` primitive (matching the appendix's original design) instead of 7 more bespoke implementations, to cut duplication across the remaining builds
- [x] Verify/complete kql-dashboard 'explore-data in view mode' (read-only NL exploration distinct from the already-built tile generator) if still absent

**Notes:** The appendix (2026-06-26) is stale in the direction of understating progress: 3 of the ~10 target surfaces (kql-database/queryset NL2KQL, kql-dashboard tile-Copilot, semantic-model NL-structure-edit) have since been fully built with real AOAI + real backend execution (kusto-client/AAS-mirror), each with checkpoint/approval-diff safety per no-vaporware.md — these should be marked resolved, not open. However 7 of the originally-listed surfaces (eventstream, stream-analytics, lakehouse, materialized-lake-view, mirrored-database, ml-experiment/automl, graph) still have zero Copilot/assist/Sparkle wiring — confirmed by direct grep across each editor file. No shared `<CopilotBuilderPane>` primitive was ever built; each resolved surface is a bespoke implementation, which is fine functionally but means the remaining 7 surfaces each need their own persona + BFF route + UI, following the now-proven pattern (3 solid templates to copy from) rather than novel design work — hence L rather than XL.

## G2 — **PARTIAL** (effort L)

G2 — AI Functions at scale (table / T-SQL / Dataflow / multimodal)

**Evidence (current code):**
- IMPORTANT: local repo main (HEAD 703bfe54) is stale vs origin/main (2d462aa8, 0.57.0) — all evidence below is from origin/main.
- apps/fiab-console/lib/azure/ai-functions-client.ts (origin/main) — AI_FN_NAMES now has 9 entries: summarize, classify, sentiment, extract, translate, fix_grammar, generate_response, embed, similarity. The 4 'missing functions' the appendix flagged are now shipped (PR #1658 'complete AI functions 5→9 + per-call cost stats (rel-T85)', merged 2026-07-06). embed/similarity call aoaiEmbed (real AOAI embeddings data-plane); fix_grammar/generate_response are chat-completions prompts.
- apps/fiab-console/lib/azure/__tests__/ai-functions-client.test.ts (origin/main) — 9-function isAiFn() test list + dedicated tests for fix_grammar/generate_response/embed/similarity, real usage/cost assertions.
- apps/fiab-console/app/api/ai-functions/route.ts + app/api/items/[type]/[id]/ai-function/route.ts — real BFF routes, no mock arrays; honest 501 not_configured gate with exact env var when no AOAI deployment (no-vaporware compliant).
- apps/fiab-console/app/api/items/[type]/[id]/ai-function/route.ts — DOES support a genuine table/column batch surface: 'SELECT col, ai_summarize(col) AS ai_result FROM table LIMIT N' executed in-database via Databricks SQL warehouse (executeStatement); this is real at-scale execution over a live table, not single-text-only as the appendix claims for gap item 21 — but it is Databricks SQL, not Synapse T-SQL/dedicated-pool as the design spec called for, and it is wired ONLY into the Databricks item family (lib/editors/databricks/sql-warehouse-editor.tsx, cluster-editor.tsx, databricks-notebook-editor.tsx, job-editor.tsx, uc-dialogs.tsx via AiFunctionsHelper) — grep for AiFunctionsHelper/'Add AI column' in the canonical Synapse-native lib/editors/warehouse-editor.tsx, lib/editors/phase3/warehouse-editor.tsx, and lib/editors/lakehouse-editor.tsx returns ZERO hits, so the spec'd 'Add AI column' action on the warehouse+lakehouse table grid (item 20/22 of the appendix) is still not built for those item types.
- apps/copilot/ai_functions/ (origin/main, PR #1011 'notebook AI functions') — real Python package (functions.py, _batch.py, _client.py, _auth.py, _gate.py, tests/) exposing ai.summarize/classify/sentiment/extract/translate over str, pandas.Series (ThreadPoolExecutor batch), and pyspark.sql.Column (vectorized pandas_udf) against live AOAI via pool MSI/UAMI — this is the genuine DataFrame/at-scale primitive the appendix said didn't exist. BUT it still only has the original 5 functions (__init__.py __all__ has no embed/similarity/fix_grammar/generate_response) — it was not updated when the BFF client went 5→9, so notebook DataFrame parity now lags the SQL/BFF surface. Also concurrency default is 8 (LOOM_AI_FN_WORKERS env, tunable) not the spec'd 200.
- No 'Dataflow Gen2 AI column' step exists: apps/fiab-console/lib/editors/dataflow-gen2-editor.tsx (origin/main) has zero AI/prompt/openai references — grep confirms the appendix's item 23 gap is still fully open.
- No multimodal support: repo-wide grep for column_type/multimodal/'classify image'/'summarize PDF' in apps/fiab-console/lib and apps/copilot/ai_functions returns zero hits — appendix item 24 gap (text-only today) still fully open.
- Cost stats real and wired: ai-functions-client.ts emits a 'copilot.usage' App Insights event (persona ai-function) with real promptTokens/completionTokens/totalTokens feeding the existing usage-chargeback + copilot-usage admin panels — this sub-claim (Wave-6 T85) is genuinely built, not vaporware.
- Bicep/day-one for embeddings: platform/fiab/bicep/modules/admin-plane/main.bicep:3899 and modules/ai/foundry-project.bicep wire LOOM_AOAI_EMBED_DEPLOYMENT day-one via the Foundry hub (agentFoundryEnabled path or BYO), satisfying the appendix's 'AOAI embeddings deployment added to Foundry hub by default' bicep-sync ask for the embed function.

**Remaining work:**
- [x] Add 'Add AI column' UI action + wiring to the canonical Synapse-native table grids. DONE (PR `feat/copilot-g2-ai-columns`): new reusable `lib/editors/components/add-ai-column-dialog.tsx` + an "Add AI column" action wired into the shared `DeltaPreviewGrid` (lib/editors/components/delta-preview-grid.tsx) — the grid used by the lakehouse Delta preview and the dataset preview. It enriches the loaded rows via the new `/api/ai-functions/table` endpoint and appends real produced column(s). (Kept the shared grid as the single wiring point rather than editing the giant phase3 warehouse/lakehouse editor bodies.)
- [x] Build the dedicated BFF batch endpoint `/api/ai-functions/table`. DONE: `app/api/ai-functions/table/route.ts` — takes `{rows|inputs, inputColumns, fn, outputColumn, modelTier, options, schema?, inputType?}`, maps over `callAiFnBatch` with bounded concurrency + honest partial-failure, honors the model-tier router, emits a usage receipt, and honest-501-gates when no AOAI is deployed. (Synapse dedicated-pool scalar-UDF init deferred; the batch endpoint is the Azure-native at-scale path, reused by grid + dataflow + notebook.)
- [x] Add a Dataflow Gen2 'AI column' transform step. DONE: `lib/components/pipeline/dataflow/dataflow-ai-step.tsx` (function/column/tier picker + live preview) wired into `dataflow-gen2-editor.tsx`; calls the same `/api/ai-functions/table` endpoint and appends a real Power Query `Table.AddColumn` lookup step. Honest note surfaced: ADF-runtime AOAI invocation over unseen rows is not yet wired (values default to null until re-run).
- [x] Update apps/copilot/ai_functions/ to the full 9-function set. DONE: added `fix_grammar`, `generate_response` (chat) + `embed`, `similarity` (new `_embed.py` AOAI embeddings client) across `functions.py`/`__init__.py`/`_prompts.py`/`_config.py`, with tests. Notebook pandas/Series surface now matches the BFF's 9 functions. (Concurrency default LOOM_AI_FN_WORKERS kept at 8 — documented deviation from ~200 to stay within default AOAI TPM.)
- [x] Add multimodal support (seam + honest gate + real vision path). DONE (scoped): `inputType: 'text'|'image'|'document'` on the table endpoint + a real AOAI vision `image_url` content path in `ai-functions-client.ts` for summarize/classify/extract, honest-501-gated on `LOOM_AOAI_VISION_DEPLOYMENT` (opt-in env, bicep-wired). DEFERRED: ADLS `path` resolution (PDF/doc bytes) + OSS Qwen-VL Gov fallback.
- [x] Wire a schema-driven field builder + live preview into the AI-column wizard. DONE: the Add-AI-column dialog has a structured-extraction schema builder (field/type/prompt → one column per field via `callCustomPromptBatch`) + a live preview grid + model-tier control (server-side bounded concurrency).

**Notes:** Local git main (HEAD 703bfe54) is stale/behind origin/main (2d462aa8, release 0.57.0) by ~30 commits including the PR that fixed most of this gap (#1658, rel-T85, merged 2026-07-06) — evaluated against origin/main throughout. The appendix (dated 2026-06-26) is materially stale on the '4 missing functions' sub-claim: that part is now fully built and real (9/9 functions, real AOAI chat + embeddings calls, honest 501 gates, cost-stat telemetry wired into existing chargeback panels — no mocks). What remains genuinely open matches the appendix's other three sub-claims almost exactly: (1) the Synapse-native warehouse/lakehouse table grids still have no 'Add AI column' batch action (only the separate Databricks-item family got a real Databricks-SQL in-database batch path), (2) Dataflow Gen2 has no AI column step at all, (3) multimodal (image/PDF) is fully unbuilt. The notebook pandas/PySpark package (apps/copilot/ai_functions) is real and DataFrame-scale but has drifted out of sync with the BFF (still 5 functions, not 9). Net: roughly half of G2's sub-items are now solidly built; the higher-effort at-scale/T-SQL/Dataflow/multimodal pieces remain. Recommend re-running this gap check after next audit since this is clearly an area under active iteration (rel-T85 branch name suggests a Wave-6 fix cycle specifically targeting it).

## G3 — **BUILT (2026-07-15, `feat/g3-operations-agent`)**

> All seven remaining-work items below are DONE. The operations-agent editor is a
> rule canvas (typed ADX/Ontology pickers + WHEN condition-builder + THEN
> action-kind picker + approval-channel toggle) creating real Azure Monitor
> scheduledQueryRules; an `operations-agent` Copilot persona authors triggers from
> NL via the real activator ARM tools; the deploy route makes Azure Monitor the
> primary target with Foundry as an optional reasoning companion; a timer-trigger
> evaluator Function (`azure-functions/ops-agent-evaluator`) reasons over fired
> triggers with AOAI and dispatches the Teams approval Logic App; bicep ships the
> default (`monitor-ops-agent.bicep`) + OSS/Gov ACA+KEDA fallback
> (`monitor-ops-agent-aca.bicep`); tests cover rule creation, condition/action
> config, approval channel, and the persona.

G3 — Operations Agent (autonomous monitor-over-ADX/Ontology -> reasoning -> Logic App/Teams action), appendix-copilot-ai.md lines 233-253

**Evidence (current code):**
- apps/fiab-console/lib/catalog/fabric-item-types.ts:941-943 — 'operations-agent' item exists in catalog (preview:true), described as monitor+recommend via Activator/Power Automate.
- apps/fiab-console/app/api/items/operations-agent/[id]/route.ts — real CRUD (GET/PATCH/DELETE) via loadOwnedItem/updateOwnedItem/deleteOwnedItem against Cosmos; genuinely wired, no mocks.
- apps/fiab-console/app/api/items/operations-agent/[id]/deploy/route.ts:1-160 — deploy route is real but targets Azure AI Foundry Agent Service (createOrUpdateAgent, a prompt/model/tools conversational agent) — NOT the Azure Monitor scheduled-query-rule / Logic App architecture G3 specifies. Honest 501 gate when Foundry isn't configured (no-vaporware compliant), but wrong backend for this gap.
- apps/fiab-console/lib/editors/phase4-editors.tsx:3761-3856 (OperationsAgentEditor) — UI is a flat form (systemPrompt textarea, model input, free-text 'tools' CSV, free-text eventhouse/ontology item-id inputs) + 'Deploy to Foundry' button. NO rule canvas, NO ADX/KQL trigger query builder, NO condition builder, NO action picker (Teams/email/Logic App), NO approval channel, NO Copilot NL rule authoring — none of the G3 UI requirements are present on this item.
- apps/fiab-console/lib/editors/phase4-editors.tsx:3806-3809 — editor's own MessageBar states: 'Playbook generation, 5-minute polling, Activator + Power Automate handshake, and Teams notifications are tracked in docs/fiab/operations-agent-parity-spec.md for follow-up sessions' — self-documents these as NOT built.
- docs/fiab/operations-agent-parity-spec.md:56-119 — parity spec (still current against today's code) explicitly lists 13 parity gaps (goals/instructions split, actions list, Activator+Power Automate handshake, playbook viewer, Start/Stop, recipient, Teams preview, capacity meters) and estimates ~13 hrs / 4 sessions remaining; grades current state 'D (stubbed)'.
- apps/fiab-console/lib/azure/monitor-client.ts:1645-1795 — upsertScheduledQueryRule / listScheduledQueryRules / deleteScheduledQueryRule / patchScheduledQueryRule ALL exist and hit real ARM (Microsoft.Insights/scheduledQueryRules); upsertActionGroup:1490 supports Logic App/webhook/SMS receivers. This is real, working Azure Monitor infra — but it backs the sibling 'activator' item, not 'operations-agent'.
- apps/fiab-console/lib/editors/phase3-editors.tsx:8826-8891 (ActivatorEditor) — the FULL G3 UI already exists here: condition builder (property/operator/value), action-kind picker (TeamsMessage/Email/Webhook/SMS/LogicApp/AdfPipelineRun/NotebookRun/PowerAutomateFlow), KQL/EventHub source picker, eval frequency/window/severity, trigger-now, run history — but it is not reused by/wired into the 'operations-agent' item.
- apps/fiab-console/lib/azure/copilot-personas.ts:284-318 — ACTIVATOR_PERSONA_ID Copilot tools (activator_author_rule, activator_suggest_threshold [real KQL percentile query against Log Analytics], activator_create_rule [real ARM scheduledQueryRule], activator_list_rules, activator_describe_history) already deliver 'Copilot authors the KQL trigger from NL' — again scoped to 'activator', not exposed for 'operations-agent'.
- apps/fiab-console/lib/copilot/ops-tools.ts:1-42 — a DIFFERENT 'ops' surface (admin-plane Ops Copilot: scale SQL pool/ADX, toggle OAP, workspace create) — confirms appendix's 'ops-tools.ts' citation refers to platform-admin ops, not a Fabric Operations Agent, i.e. still not the same thing.
- platform/fiab/bicep/modules/admin-plane/monitoring.bicep:192-210 — Console UAMI already granted Monitoring Contributor (749f88d5…) day-one generically (covers metric-alert/scheduled-query authoring subscription-wide) — satisfies part of G3's bicep RBAC ask, but no operations-agent-specific monitor.bicep / Teams-connector / evaluator Function module exists.
- platform/fiab/bicep/modules/integration/approval-logicapp.bicep + apps/fiab-console/lib/azure/plan-approval-client.ts — a Consumption Logic App + O365 'send approval email' pattern exists for ADF pipeline Approval activities, but it's email-based and ADF-scoped, not a Teams-adaptive-card approval wired to operations-agent.
- No Azure Function timer-trigger evaluator found (grep for 'ops-agent-evaluator' / 'ops_agent_evaluator' — 0 hits) — the 5-minute poll-and-reason loop the parity spec calls for does not exist.
- apps/fiab-console/lib/editors/__tests__/operations-agent.test.tsx — only a mount/smoke test (renders chrome + asserts >=1 ribbon button); no coverage of condition/action/approval flow because none exists to test.

**Remaining work:**
- [x] Rebuild apps/fiab-console/lib/editors/phase4-editors.tsx::OperationsAgentEditor as a rule canvas: reuse ActivatorEditor's (phase3-editors.tsx:8826+) condition-builder + action-kind-picker (Teams/Email/LogicApp/Webhook) state/UI, plus a typed ADX/Eventhouse + Ontology source picker (replace the current free-text eventhouse/ontology inputs) and an approval-channel toggle.
- [x] Extend apps/fiab-console/lib/azure/copilot-personas.ts with an 'operations-agent' persona (or generalize ACTIVATOR_PERSONA_ID's tools) so the Copilot builder can author the ADX/KQL trigger + condition from NL for this item type, reusing activator_author_rule/activator_suggest_threshold/activator_create_rule against monitor-client's real scheduledQueryRule ARM calls.
- [x] Rewrite apps/fiab-console/app/api/items/operations-agent/[id]/deploy/route.ts to call monitor-client.upsertScheduledQueryRule + upsertActionGroup (Teams/Logic App receivers) as the primary deploy path (Azure-native default), keeping the existing Foundry Agent Service call as the 'reasoning' companion (AOAI interprets the fired event + recommends), not the sole deploy target.
- [x] Add a timer-trigger evaluator (new Azure Function, e.g. func-csa-loom-<region>/ops-agent-evaluator per docs/fiab/operations-agent-parity-spec.md:95,108) that on a fired scheduled-query rule calls AOAI to interpret + recommend, then dispatches the bound Logic App -> Teams adaptive-card approval (and optional downstream ERP/CRM connector call).
- [x] Add platform/fiab/bicep/modules/admin-plane/monitor-ops-agent.bicep (or extend monitoring.bicep) with the operations-agent-specific scheduled-query rule template, Logic App + Teams connector, the evaluator Function App, and role assignments (Database Viewer on bound Eventhouse/ADX, Microsoft Graph Chat.ReadWrite for Teams cards) — wire into admin-plane/main.bicep.
- [x] Add an OSS/air-gapped-Gov fallback rules engine on ACA + KEDA (cron/queue) as an alternate evaluator for the same operations-agent item when Logic Apps/Teams aren't available.
- [x] Expand apps/fiab-console/lib/editors/__tests__/operations-agent.test.tsx beyond the mount smoke test to cover rule creation, condition/action config, and the approval-channel flow once built.
- [x] Update docs/fiab/operations-agent-parity-spec.md and PRPs/completed/fabric-parity/appendix-copilot-ai.md G3 status once the rule-canvas + Monitor/Logic-App backend land (currently self-documented as deferred).

**Notes:** The appendix's citation is accurate in spirit but slightly stale in one respect: an 'operations-agent' catalog item + editor + CRUD/deploy BFF DOES now exist (post-dating the appendix), so it's not a flat MISSING. However the built item deploys to Azure AI Foundry Agent Service (a conversational prompt agent) rather than the Azure Monitor scheduled-query + Logic App + Teams/email rule architecture G3 specifies, and has none of the required rule-canvas UI (ADX trigger builder, condition builder, action picker, approval channel, NL Copilot authoring). Critically, ALL the underlying Azure-native primitives G3 needs already exist and are proven live on the sibling 'activator' item: real scheduledQueryRule CRUD (monitor-client.ts), real action-group Teams/Email/Logic-App receivers, and a working NL-to-rule Copilot persona (activator_author_rule/activator_suggest_threshold/activator_create_rule, backed by real KQL percentile queries + real ARM rule creation). So this is best framed as an integration/composition gap, not a from-scratch build — which is why effort is L rather than XL despite the acceptance bar (fire a real ADX condition, receive a real Teams/email action) not yet being met by the operations-agent item itself.

## G4 — **MISSING** (effort L)

G4 — Data Wrangler AI (Fabric parity appendix, apps/fiab-console)

**Evidence (current code):**
- PRPs/completed/fabric-parity/appendix-copilot-ai.md:102 and :255-263 — the appendix itself grades G4 ❤ MISSING ("no Data Wrangler AI surface") with a P2 gap spec; nothing since 2026-06-26 supersedes it.
- apps/fiab-console/lib/editors/components/delta-preview-grid.tsx (383 lines) is the only file in the repo matching /wrangler/i — it is a read-only Fluent v9 DataGrid (sort/filter/resize/multi-select/copy-CSV/cell-dialog) plus a Spark-computed column-statistics card (count/min/max/mean/stddev/nulls/histogram via /api/lakehouse/table-stats -> Livy). Zero AI content: no tab, no suggestion cards, no code-generation control, no apply/preview-transform button.
- docs/fiab/parity/lakehouse-preview.md — the checked-in parity doc for this exact surface — enumerates every capability as built ✅ and explicitly scopes coverage to ‘Fabric Lakehouse preview + Data Wrangler column-statistics panel’ only; no row for AI functions, cleaning suggestions, or NL-to-code.
- apps/fiab-console/lib/azure/ai-functions-client.ts:12-49 — AI Functions today is 5 single-text ops (summarize/classify/sentiment/extract/translate) called by a single-value BFF (app/api/ai-functions/route.ts). No table/DataFrame batch mode exists (matches appendix gap G2, which G4 depends on for the 'per-column AI functions in the grid' sub-claim).
- grep across apps/fiab-console for 'AI column|ai-column|cleaning suggestion|AI suggestion|generate the transform|nl-to-pandas|transform code' returns zero real hits (one false-positive substring match inside 'AOAI suggestions' in notebook-editor.tsx) — confirms no per-column AI tab, no suggestion engine, and no NL→pandas/PySpark codegen control anywhere in the app.
- apps/fiab-console/lib/copilot/notebook-tools.ts and notebook-persona-tools.ts have no codegen/fix/transform helper that a Wrangler AI tab could already be wired to; the notebook Copilot persona (row 1 of the appendix's own surface map) is a separate, unconnected surface.

**Remaining work:**
- [x] Add an 'AI' tab to DeltaPreviewGrid (apps/fiab-console/lib/editors/components/delta-preview-grid.tsx) alongside the existing File/Table toggle and column-statistics panel — new tab strip + panel state.
- [x] Build the batch AI-Functions backend this tab needs: extend app/api/ai-functions/route.ts (or new app/api/ai-functions/table/route.ts) to accept a column + function name, read N rows via synapse-sql-client / lakehouse preview, map over lib/azure/ai-functions-client.ts with bounded concurrency, and return per-row results — this is gap G2's table/DataFrame API and is a hard prerequisite for G4's 'AI Functions applied per-column'.
- [x] Add a rule-based / AOAI-driven 'cleaning suggestion' generator: a new BFF route (e.g. app/api/lakehouse/ai-clean-suggest/route.ts) that profiles the previewed columns (reusing table-stats output: nulls, min/max, type) and calls AOAI to produce suggestion cards (trim, cast, dedupe, fill-null, outlier-flag) with a preview-before-apply diff, wired into the new AI tab.
- [x] Add NL-to-code generation: a control in the AI tab that POSTs a natural-language transform request to a notebook-persona-style Copilot route (reuse/extend lib/copilot/notebook-persona-tools.ts + app/api/copilot/notebook-assist) scoped to the active DataFrame/columns, returns pandas or PySpark code, and inserts it into the bound notebook cell (or shows an approval diff before insert).
- [x] Wire real-time preview of the generated/suggested transform before apply — apply the candidate code against a sampled DataFrame via the existing Livy session (/api/lakehouse/table-stats plumbing) and render the resulting preview rows in DeltaPreviewGrid before commit.
- [x] Add parity doc updates to docs/fiab/parity/lakehouse-preview.md (or a new data-wrangler-ai.md) once built, per ui-parity.md's per-surface deliverable requirement.

**Notes:** This gap has a real dependency on gap G2 (AI Functions at scale / table batch endpoint) which is also still open per the same appendix and independently confirmed by grep of ai-functions-client.ts (5 single-text ops only, no DataFrame batch). Building G4 cleanly implies building at least the table-batch slice of G2 first, which is why effort is L rather than M despite G4 alone reading as a single UI tab — there is no existing batch AI backend to hang it off of. The Copilot orchestrator, personas, AOAI resolution, and approval-diff/proposed-change plumbing referenced elsewhere in the appendix (copilot-orchestrator.ts, proposed-change.ts, apply-change.ts) ARE real and reusable, which keeps this from being XL.

## G5 — **MISSING** (effort L)

G5 — Prep data for AI / Verified Answers (semantic-model editor): AI data schema (expose/hide tables+columns to AI), AI instructions, and Verified Answers (curated NL->DAX pairs), persisted on the model item and consumed by data-agent-client.ts DAX routing.

**Evidence (current code):**
- apps/fiab-console/lib/azure/data-agent-execute.ts:108-115 — semantic-model source ALWAYS returns { executed:false, gate:'Semantic-model (DAX) execution needs an XMLA endpoint ... Not wired in this deployment' } — DAX is never actually run, so there is no way for a Verified Answer to be 'consumed' even if one existed.
- apps/fiab-console/lib/azure/data-agent-client.ts:37-70 — DataAgentSource.examples (few-shot NL->query pairs) is the only analogous mechanism, and its own doc comment restricts it to 'lakehouse/warehouse/kql/graph/ai-search only' — semantic-model is explicitly excluded from few-shot/Verified-Answer style grounding.
- apps/fiab-console/lib/editors/phase4-editors.tsx:4308, apps/fiab-console/lib/editors/data-agent-config-copilot.tsx:187, apps/fiab-console/lib/copilot/agent-config-tools.ts:105, apps/fiab-console/lib/editors/_family-utils.ts:565 — all four references to 'Prep for AI'/'Verified Answers' in the codebase are comments telling the USER to go use Power BI's own Prep-for-AI feature instead ('Semantic models use Power BI "Prep for AI" Verified Answers instead of example queries') — i.e. Loom explicitly punts this to real Power BI/Fabric rather than building an Azure-native equivalent, which is itself a no-fabric-dependency.md violation on top of being unbuilt.
- apps/fiab-console/lib/editors/phase3-editors.tsx:11379-13746 (SemanticModelEditor) — tab union has no 'prep-for-ai'/'verified-answers' tab; the only AI-adjacent tab is 'copilot' (SemanticModelCopilotPane, line 11165) which is a model-STRUCTURE edit assistant (rename measures, suggest relationships) via /api/items/semantic-model/[id]/copilot-structure — unrelated to G5's schema-exposure/instructions/verified-answers concept.
- apps/fiab-console/lib/editors/phase3-editors.tsx:12822 — the only per-column 'isHidden' toggle found is the standard tabular-model 'hide in report view' property (Switch bound to colPatch.isHidden), not an AI-specific expose/hide-from-agent flag.
- find apps/fiab-console/app/api/items/semantic-model -type f — no prep-for-ai, verified-answers, or ai-schema route exists among the 13 semantic-model API routes (model, measures, datasource, copilot-structure, direct-lake, roles, refresh*, embed-token, ingest, take-over, build, workspace-pane, aas-databases).
- grep -rniE 'prepForAi|aiSchema|verifiedAnswers|aiInstructions' apps/fiab-console/lib/types apps/fiab-console/lib/catalog — zero hits; no persisted schema field for any of the three G5 sub-items exists on the item type.

**Remaining work:**
- [x] Add persisted fields on the semantic-model item content (lib/types + Cosmos schema): aiSchema (per-table/per-column exposed:boolean flags distinct from report-visibility isHidden), aiInstructions (string), verifiedAnswers (Array<{id, question, dax, lastVerifiedAt, lastVerifiedResult}>).
- [x] New BFF route apps/fiab-console/app/api/items/semantic-model/[id]/prep-for-ai/route.ts — GET/PATCH the three fields above; PATCH validates via synapse-sql-client (or AAS XMLA client) before persisting a verified answer, per no-vaporware.md (run-to-verify requirement from the appendix's own acceptance criteria).
- [x] New editor tab/panel 'Prep for AI' in apps/fiab-console/lib/editors/phase3-editors.tsx SemanticModelEditor (add to the tab union near line 11401, render block near line 13746): dropdown table/column expose-to-AI toggles (reuse SmTable/SmColumn types from lib/editors/phase3-editors.tsx ~line 10405), AI-instructions Textarea, and a Verified-Answer list editor (NL question + Monaco DAX input + 'Run to verify' button calling the new route).
- [x] Wire DataAgentSource / DataAgentConfig in apps/fiab-console/lib/azure/data-agent-client.ts to load a semantic-model source's verifiedAnswers into the `examples` few-shot array (currently explicitly excluded per the comment at line 53) so composeSystemPrompt actually surfaces curated NL->DAX pairs to the model.
- [x] Implement real DAX execution in apps/fiab-console/lib/azure/data-agent-execute.ts case 'semantic-model' (currently always gates at lines 108-115) against Azure Analysis Services / Synapse-backed tabular via an XMLA or DAX REST execute call, so a matching question actually runs the verified DAX instead of just displaying the query.
- [x] Replace the four 'use Power BI Prep for AI instead' punt-comments (phase4-editors.tsx:4308, data-agent-config-copilot.tsx:187, agent-config-tools.ts:105, _family-utils.ts:565) with real Loom-native Verified-Answers wiring to close the no-fabric-dependency.md violation those comments currently represent.

**Notes:** This is a P1 Phase-1 gap per the appendix's own phasing (grouped with G1/G2 as hitting the 'Copilot builder on every design UI' + AI-Functions value prop), but it is 0% built, not partially built — every sub-item (AI schema exposure, AI instructions, Verified Answers store, DAX execution/consumption) is absent from both the UI and backend. Notably the four existing code comments that DO mention 'Prep for AI'/'Verified Answers' actively tell the user to go do this in real Power BI instead of Loom, which is itself a no-fabric-dependency.md rule violation layered on top of the missing feature — worth flagging to the operator as a double-fix item, not just a build item. The appendix's claim that 'the wiring point already exists in data-agent-client.ts semantic-model routing' is technically true (semantic-model is a routing type) but misleading: the routing target is permanently gated off (data-agent-execute.ts never executes DAX), so there is no live consumption path to hook Verified Answers into without also building real DAX/XMLA execution.

## G6 — **PARTIAL** (effort M)

G6 — Deepen agentic publish depth (M365 Agent Store rich publish dialog + Copilot Studio connected-agent wiring with User vs Agent-author auth)

**Evidence (current code):**
- apps/fiab-console/app/api/items/data-agent/[id]/m365-copilot/route.ts:1-250 — real, wired GET/POST route: GET lists Power Platform/Dataverse environments (listEnvironments/resolvePublishEnvId), POST upserts+publishes a Copilot Studio agent via publishToM365Copilot(), persists the publish receipt to the Cosmos item, and returns an honest 501/503 infra-gate (LOOM_COPILOT_STUDIO_ENVIRONMENT_ID / LOOM_DATAVERSE_*) when Power Platform isn't configured — this part is NOT vaporware.
- apps/fiab-console/lib/azure/copilot-studio-client.ts:848-940 — publishToM365Copilot() makes real Dataverse REST calls (upsertAgentByName, addKnowledgeSource, publishAgent, listChannels/publishToChannel against msdyn_botchannels) — genuine backend, not mocked.
- apps/fiab-console/lib/editors/phase4-editors.tsx:4083-4126,4471,4531-4556 — publish dialog only has: env dropdown (m365Envs/m365EnvId), a reused shared `state.description` Textarea (same field used for the Foundry agent description, not a distinct AI-discovery description_for_model), and a single 'available in M365 Copilot' checkbox (m365Available) wired to availableInM365Copilot. No separate rich-description field, no deliver-as-is instruction control.
- grep for 'description_for_model' across apps/fiab-console returns zero hits (confirmed via background grep b70zfy8dv) — the specific field named in the gap definition does not exist anywhere in the codebase.
- grep for 'deliver-as-is'/'deliverAsIs' across apps/fiab-console returns zero hits — no deliver-as-is instruction toggle exists.
- grep for 'connected-agent'/'ConnectedAgent'/'connectedAgent' across apps/fiab-console returns only an unrelated hit in lib/apps/content-bundles/app-sovereign-ai-agents.ts (a content-bundle app description, not Copilot Studio wiring) — no Copilot Studio connected-agent construct exists.
- grep for agent-author/end-user auth terms (agentAuthor, endUserAuth, onBehalfOfUser, User vs Agent-author) across apps/fiab-console returns zero hits outside an unrelated MSAL library file in node_modules — there is no User-vs-Agent-author auth mode selector anywhere in the data-agent publish/deploy routes or client.
- apps/fiab-console/app/api/items/data-agent/[id]/publish/route.ts and deploy/route.ts — no authMode/onBehalfOf/agent-author concept present (grep clean).

**Remaining work:**
- [x] Add a distinct 'AI discoverability' field to the M365 Copilot publish dialog (phase4-editors.tsx ~4471-4556): a second Textarea for description_for_model (LLM-facing capability description, separate from the user-facing state.description), persisted as state.m365Copilot.descriptionForModel and sent in the POST body to app/api/items/data-agent/[id]/m365-copilot/route.ts.
- [x] Wire description_for_model through to Dataverse: extend M365PublishInput in lib/azure/copilot-studio-client.ts (currently name/description/instructions/modelDeployment/knowledge/availableInM365Copilot) to carry a descriptionForModel value into the msdyn_bot upsert payload (or the agent's semantic-description field Copilot Studio/Agent-Store discovery reads), and thread it from the route handler.
- [x] Add an explicit 'deliver-as-is' instruction toggle to the publish dialog + route + composeCopilotInstructions() in m365-copilot/route.ts, so the generated Copilot Studio instructions include a deliver-as-is (answer verbatim, no re-synthesis) directive when the operator enables it, distinct from the generic instructions text already composed.
- [x] Rename/clarify the existing m365Available checkbox as the literal 'Agent Store' listing toggle (UI label + persisted key) so the sub-claim 'Agent Store toggle' is unambiguous rather than reusing the generic availableInM365Copilot flag semantics.
- [x] Build Copilot Studio connected-agent wiring: new capability in lib/azure/copilot-studio-client.ts to register the published Foundry/Loom data agent as a Dataverse 'connected agent' (msdyn_connectedagent or equivalent Copilot Studio child-agent construct) under the Copilot Studio agent created by publishToM365Copilot, rather than only attaching it as a URL knowledge reference (current knowledge.push() in m365-copilot/route.ts:184-191 is a weak deep-link, not a connected-agent).
- [x] Add a User-vs-Agent-author auth selector to the publish dialog and backend: a control (dropdown/radio) persisted on the data-agent item state (e.g. state.m365Copilot.authMode: 'user' | 'agent-author') that determines whether the Copilot Studio connected-agent calls downstream Foundry/Azure resources on-behalf-of the invoking M365 user (OBO) vs. under the agent-author's own service identity; wire this into the publish POST body, into copilot-studio-client's agent/channel config, and (if OBO) into the chat/route.ts or deploy/route.ts auth path so runtime calls actually honor the selected mode.
- [x] Add/extend Vitest coverage in app/api/items/data-agent/__tests__/routes.test.ts for the new descriptionForModel, deliver-as-is, and authMode fields on the m365-copilot POST route (currently no tests reference these fields since they don't exist).

**Notes:** The appendix's ⚠️ framing ('publish-to-Agent-Store flow shallow') holds up under current-code verification, not just as stale-doc noise. The core plumbing (env picker, real Dataverse publish, honest infra-gate, receipt persistence) is genuinely built and non-vaporware — this is NOT a MISSING/F-grade surface. But the three named depth sub-items (description_for_model, deliver-as-is, connected-agent + User-vs-Agent-author auth) are all absent with zero string hits anywhere in apps/fiab-console, confirmed by direct grep rather than inference from the stale appendix. Grade PARTIAL, not BUILT.
