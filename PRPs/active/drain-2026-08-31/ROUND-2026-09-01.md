# ROUND — 2026-09-01, drain continuation

Companion to `PRP.md` / `LEDGER.md`. This round is recorded here rather than as
ledger rows because `LEDGER.md` is generated from the 2026-08-31 snapshot by
`gen-ledger.py` and must not be hand-edited. Fold these into the register on the
next regeneration.

---

## 1. The safety chain — a BLOCKING merge-order constraint

This is the most important output of the round. It is a sequencing constraint,
not a code change, and it must survive into the next session.

**Today the estate is protected from a one-click, unrecoverable data loss only
by an accident.** Nothing carries `loom-estate-id`, so `performDisposition`
returns `withheld-ownership` (no button renders) and `guardOwnership` refuses
server-side with a 409. Neither is a designed protection. Both are side effects
of the missing tag — which two in-flight PRs exist to supply.

Live confirmation, receipt run `33544639696`: the Brain reports `owns (0)` and
`observed (0)` across 109 nodes; only `configured (18)` contributes edges.

The chain that opens once tagging lands, measured by the #4260 review:

> primary Perform button on the card ranked top by derived saving → a
> `destructive · scale-to-zero` badge → server-side staging → **a confirm dialog
> saying the app "stays deployed and scales back up on demand"** → the typed
> resource name → all seven guards **pass** → ARM PATCH → a green receipt
> showing `before {minReplicas:1} / after {minReplicas:0}` and nothing about
> what was destroyed.

The top subject is `loom-risingwave`, whose own deploy says
(`loom-risingwave-aca.bicep:93-95`): **"CANNOT scale to zero: a scaled-to-zero
replica loses every MV definition and its progress."** It is on the list because
of a **false positive** — `LOOM_RISINGWAVE_URL` appears nowhere in `lib/brain`,
so the extractor discards a real declared wire before resolution (#4258).

### Required order

| # | PR / issue | Must be true before the next one merges |
|---|---|---|
| 1 | **#4261** `guardScalableToZero` | Guard **fails closed**. Currently fails OPEN — see §2. |
| 2 | **#4279** unguarded scaling route | Second door closed; reuses #4261's derivation. |
| 3 | **#4260** Perform UI | Reversibility claim removed from the confirm dialog. |
| 4 | **#4274** tag stamping | — |
| 5 | **#4267** ownership backfill | — |

Holds posted on #4274 and #4267 stating this. **Do not merge either on a green.**

---

## 2. New issues filed this round

| # | Title (short) | Class | Notes |
|---|---|---|---|
| **4275** | drift **reporter** walks arrays by index, so one inserted node reports as "20 differing fields" | REAL · S · CI/brain | **Retitled and rescoped after a correction — see §2a.** Diagnosability defect, not fleet serialization. Fix = diff node arrays by identity, not index. |
| **4282** | REQUIRED lane checks three integers; only the ADVISORY lane checks the graph | REAL · M · CI/brain | A **renamed** scanned file keeps the census at 379 and merges a stale artifact into the shipped console image. #4128 inverted. |
| **4276** | `loom-docs-reindex` declares a `workflow_call` with zero callers | REAL · S · CI | Console roll never invokes it; last 8 runs all `schedule`. Index freshness is incidental, so the eval gate is a coin flip. Underlies #3472. |
| **4277** | eval delta-baseline fetch is `set +e` / `exit 0` | REAL · S · CI | An unfetchable baseline silently disables the delta half of the gate. Distinguish "no prior run" (proceed, disclose) from "prior run unreadable" (fail). |
| **4278** | Brain tabs have no URL representation | REAL · S · W1 · brain | Structurally blocks the G1 receipt for 3 of 4 views; Recommendations has no address. |
| **4279** | container-apps scaling route accepts `minReplicas:0` unguarded | REAL · M · brain/api | **A second door to the RisingWave loss.** Independent of the Brain path; #4261 alone does not close it. |
| **4280** | Brain legend overprinted by provenance chips; stray Ctrl-K chip | REAL · S · W1 · brain/ux | Both themes, default width. `Unreachable + always-on` — the most destructive class — is the label made illegible. |

---

## 2a. A correction, recorded because the wrong premise is contagious

**#4275 was filed on a mechanism I got wrong**, and the error is worth keeping
rather than quietly overwriting, because the same mistake is easy to repeat.

I opened it on a `--check` output reporting *"20 differing field(s)"* of the form
`console:member:2791 -> console:member:2842`, and concluded the artifact keyed
node identity to a **positional ordinal**, making any scanned-file addition
re-stale every open PR.

Measured on PR #4273's regeneration (job `99971781647`): the artifact's **git
diff is a clean insertion — 49 insertions, 7 deletions, no cascade.** The 20-field
cascade exists **only in the comparator's output**, because
`_artifact-drift.mjs` walks arrays **by index**. One node inserted mid-array
makes every later position report as differing.

So the ids were never the problem, and "one merge re-stales every open PR" is
false at the file level. The three PRs that triggered this each had **genuine,
PR-local** drift from their own added `.mjs` under `scripts/**`.

**The generalisable lesson:** a diff tool's output is a claim about *how it
walked*, not only about the data. `deploy-integrity.md` R7 applies to
diagnostics as much as to errors — *"20 differing fields (there may be more)"*
asserted widespread corruption when the truth was one added node, and it cost
several steps of investigation down the wrong path.

**Secondary lesson on evidence:** I treated a *count* (20 fields) as a measure
of *extent*. It was a measure of the walk. Prefer evidence that names the
subject — "which node?" — over evidence that counts positions.

---

## 3. Review verdicts — seven PRs, all green in CI, **all seven CHANGES REQUIRED**

| PR | Verdict | Headline finding |
|---|---|---|
| #4260 | CHANGES REQUIRED | Confirm dialog promises reversibility it cannot know. R7 on copy the operator acts on. |
| #4261 | CHANGES REQUIRED | **Guard fails OPEN.** `resolveDlzTemplateInline()` swallows all errors, caches `null` at module scope; empty map ⇒ allow-everything across all three enforcement points (common mode, not independent). Detector half has zero production callers. |
| #4262 | CHANGES REQUIRED | `svc-adf` requiredSettings fully disjoint from its predicate; the guard meant to catch it compared the registry to a hand table in its own file. |
| #4263 | CHANGES REQUIRED | Widening admits `from === to` self-edges — a false negative, the same lie inverted. |
| #4266 | CHANGES REQUIRED | Merge commit fires three workflows; two take the same ACR lease with disjoint concurrency ⇒ one hard-fails after 25 min. R1. |
| #4271 | CHANGES REQUIRED | New predicate cannot match the string its own target produces (`monitor-arm.ts:89` discards `error.code`). Dead code for every warm target. |
| #4272 | CHANGES REQUIRED (2 should-fix) | RG fallback fires exactly when the name has external provenance; 404 repeats ARM's "does not exist" without disclosing the substitution. |

**Method note worth carrying:** the recurring cause across five of these is *a
type-correct fixture cannot reach a lie to the compiler*. The productive question
is **"what input SHAPE has no fixture?"**, not "what mutation?".

---

## 4. Landed this round

- **#4262** — guard rewritten to derive env vars from the real predicate instead of
  a hand table. Now flags **12 violations across 6 sites** (3 more than the review
  found). Left deliberately RED and honest. Its companion "derivation reached all
  16 sites" assertion caught a bug in its own first cut (a return-type annotation
  mistaken for a function body, silently missing 8 of 16 sites).
- **#4274, #4269** — security-graph artifacts re-derived and pushed; both graph
  checks green. Cause was PR-local (`scripts/ci/` is in scan scope), **not** base
  drift — `update-branch` reported both already up to date.
- **#4229** — unblocked. `changelog parser can read every commit message` was a
  required context that had never run: 11 bot runs parked at `action_required`.
  Approved all 11 rather than admin-merging past the one check most relevant to a
  release PR.
- **G1 receipt** captured for `/admin/brain` (run `33544639696`) — light + dark,
  live estate at `71c5bf2426`. Found #4280.

---

## 5. Still owed

- **G1 receipt for the Recommendations tab** — blocked on #4278; there is no route
  that reaches it. Re-capture at `/admin/brain?tab=recommendations`, full and
  narrow, both themes.
- **Live pause→resume round trip** with the read-warmer running.
- **Iceberg catalog** — #4256 is **deployed** (estate at `71c5bf2426`), **not
  verified**. Route answers a structured 401; end-to-end needs a session.
- **GCC (#4259)** — five secrets in the GCC Entra tenant. Its failing
  `check-workflow-lane-states` is CORRECT; do not work around it.
- **Operator question, non-blocking:** are `forzelite` and `sentinel` deliberately
  sharing these subscriptions? Default: theirs-but-not-Loom's (observed bucket).

---

## 6. Operational hazards observed

- **~200 accumulated worktrees** from dead agents hold branch locks. A `git checkout`
  fails with RC=128 while a following `git pull --ff-only origin <branch>` still
  runs — which moved local `main` onto a PR head once this session. Recovered, nothing
  pushed. **Check the RC of every checkout before the next git command.**
- **Reviewer probe files left in scan scope** (`zzreview-cae-head.ts` in
  `lib/brain/graph/extractors/`) silently pollute an artifact regeneration (#4216).
  Regenerate only with `git status --porcelain` clean.
- **Agents die of context exhaustion** reading large files whole (3.9 MB
  `deploy-templates/main.json`, multi-thousand-line test files). Two died today.
  Brief them to grep and read narrow ranges.
- **Branch protection requires 1 approving review**, so every PR reads `BLOCKED`
  regardless of checks. Agent reviews post as comments, not approvals — hence the
  authorized `--admin` path, gated on `temp/merge-eligible.py`.
