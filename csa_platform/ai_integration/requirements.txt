# Dependencies managed in pyproject.toml [project.optional-dependencies.platform]
# This file is kept for standalone module deployment.
#
# AI Integration — CSA-in-a-Box
# Python dependencies for RAG, enrichment, and model serving patterns.

# Azure OpenAI and embeddings
openai>=1.35.0,<3.0.0

# Azure AI Language (NER, PII detection, sentiment)
azure-ai-textanalytics>=5.3.0,<6.0.0

# Azure AI Search (vector store)
azure-search-documents>=11.5.0,<12.0.0

# Azure Identity (DefaultAzureCredential, managed identity)
azure-identity>=1.17.0,<2.0.0

# Azure ML SDK v2 (model serving, endpoints)
azure-ai-ml>=1.16.0,<2.0.0

# Tokenizer for chunk size estimation
tiktoken>=0.7.0,<1.0.0

# Data validation and settings
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0

# YAML parsing for taxonomy and config files
pyyaml>=6.0,<7.0

# Semantic Kernel for multi-agent orchestration
# Semantic Kernel was previously listed here as a runtime dependency for
# csa_platform.ai_integration.semantic_kernel. That subpackage was removed on
# 2026-04-24 (see csa_platform/ai_integration/SEMANTIC_KERNEL_REMOVED.md) so
# the dependency is gone too. The examples/ai-agents tutorial code has its
# own pip install instructions; do not re-add semantic-kernel here unless a
# new platform module needs it.
# semantic-kernel[azure]>=1.41.0,<2.0.0

# MCP SDK for Model Context Protocol server
mcp>=1.0.0,<2.0.0

# GraphRAG for knowledge graph construction
graphrag>=2.0.0,<3.0.0

# Azure Cosmos DB (Gremlin API for graph, SQL API for memory)
gremlinpython>=3.7.0,<4.0.0
azure-cosmos>=4.7.0,<5.0.0

# Azure AI Foundry
azure-ai-projects>=1.0.0,<2.0.0

# Document Intelligence for PDF extraction
azure-ai-documentintelligence>=1.0.0,<2.0.0

# Azure Event Hubs for streaming
azure-eventhub>=5.11.0,<6.0.0

# Azure Data Explorer
azure-kusto-data>=4.4.0,<5.0.0

# PDF fallback
pypdf2>=3.0.0,<4.0.0

# Word documents
python-docx>=1.1.0,<2.0.0

# Geospatial (optional — for geoanalytics module)
# geopandas>=0.14.0
# h3>=4.0.0
# pyproj>=3.6.0
# shapely>=2.0.0
# geoalchemy2>=0.14.0
