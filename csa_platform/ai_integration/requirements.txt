# Dependencies managed in pyproject.toml [project.optional-dependencies.platform]
# This file is kept for standalone module deployment.
#
# AI Integration — CSA-in-a-Box
# Python dependencies for RAG, enrichment, and model serving patterns.

# Azure OpenAI and embeddings
openai>=1.35.0,<2.0.0

# Azure AI Language (NER, PII detection, sentiment)
azure-ai-textanalytics>=5.3.0,<6.0.0

# Azure AI Search (vector store)
azure-search-documents>=11.5.0,<12.0.0

# Azure Identity (DefaultAzureCredential, managed identity)
azure-identity>=1.17.0,<2.0.0

# Azure ML SDK v2 (model serving, endpoints)
azure-ai-ml>=1.16.0,<2.0.0

# Tokenizer for chunk size estimation
tiktoken>=0.7.0,<1.0.0

# Data validation and settings
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0

# YAML parsing for taxonomy and config files
pyyaml>=6.0,<7.0
