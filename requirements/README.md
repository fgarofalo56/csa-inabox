# Lock Files (`requirements/locks/*/requirements.txt`)

CSA-0032 — **reproducible builds**. Each `requirements/locks/*/requirements.txt` file is a fully-resolved,
hash-pinned snapshot of one `pyproject.toml` optional-dependency extra (plus
the base install). They are generated by [`pip-compile`][pip-compile] directly
from `pyproject.toml` via `--extra=<name>` (pip-tools >=7 treats
`pyproject.toml` as a first-class input — no intermediate `*.in` files are
required).

## What consumes them — and what does not

- **SBOM generation** (`sbom.yml`) — one CycloneDX + SPDX pair per non-empty extra.
- **Trivy filesystem CVE scans** (`trivy.yml`) — the CRITICAL gate.

They are **not** installed by any Dockerfile: all 37 tracked Dockerfiles were
enumerated and none references `requirements/locks`. The shipped portal backend
installs the *range* file `portal/shared/requirements.txt`. They are also
deliberately excluded from `test.yml`'s per-domain `requirements.txt` loop —
495 exact pins in that one shared environment would decide what the suite
measures (#2615, #3485).

An earlier version of this file claimed the locks were the source of truth for
"Docker image builds (portal backend/frontend, Azure Functions)" and "CI
dependency installs". Neither was true, and a control trusted for coverage it
does not have is worse than no control.

## Files

| Lock file | Extra | Purpose |
|-----------|-------|---------|
| `locks/base/requirements.txt` | *(none)* | Base install (bare `csa-inabox`; currently empty runtime deps) |
| `locks/dev/requirements.txt` | `dev` | ruff, pytest, mypy, bandit, pre-commit, type stubs |
| `locks/governance/requirements.txt` | `governance` | PyYAML, structlog, Azure Monitor ingestion |
| `locks/functions/requirements.txt` | `functions` | Azure Functions SDK + Azure service SDKs |
| `locks/platform/requirements.txt` | `platform` | AI integration, Databricks, Synapse, RAG loaders |
| `locks/portal/requirements.txt` | `portal` | FastAPI, Uvicorn, Pydantic, JWT, OpenTelemetry |
| `locks/bff/requirements.txt` | `bff` | MSAL, itsdangerous, redis (BFF auth mode) |
| `locks/postgres/requirements.txt` | `postgres` | SQLAlchemy, asyncpg, psycopg, alembic |
| `locks/copilot/requirements.txt` | `copilot` | pydantic-ai, OpenAI, AI Search, MCP, SSE |
| `locks/streaming/requirements.txt` | `streaming` | Event Hubs, Schema Registry, Event Grid, Cosmos |

### Why the path looks like that

These files used to be `requirements/<extra>.lock`, and **nothing scanned
them**. Trivy's pip analyzer and Syft's python cataloger both key on the
FILENAME: the Trivy filesystem job logged `Number of language-specific files
num=0` and every published Python SBOM had **0 components**, for as long as the
locks had existed (#3485). Measured 2026-08-15 on identical file contents:

| Path | Trivy fs | Syft |
|------|----------|------|
| `requirements/bff.lock` | not seen | 0 components |
| `requirements/bff.requirements.txt` | not seen | 13 components |
| `requirements/locks/bff/requirements.txt` | **seen** | **13 components** |

A file literally named `requirements.txt` is the only spelling both tools
recognise with no configuration — and a filename cannot silently regress the way
a `--file-patterns` config can. The extra `locks/` level is load-bearing too:
Trivy's default skip list is ROOT-ANCHORED and includes `dev`, so with the scan
rooted at `requirements/` a `requirements/dev/requirements.txt` was dropped
while its nine siblings were scanned. `scripts/ci/check-lock-scan-coverage.mjs`
asserts every lock is actually parsed, with a package count matching the pins it
declares, and is what caught that.

## Regenerating locks

```bash
# Regenerate every lock file (preferred — one command)
./scripts/update-locks.sh

# Or a single extra
./scripts/update-locks.sh portal

# Raising a floor that needs a transitive dependency to move with it
./scripts/update-locks.sh portal --upgrade-package cryptography --upgrade-package msal

# Show the exact commands without running them
./scripts/update-locks.sh --print-plan portal
```

### Why the script and not a bare `pip-compile`

The script compiles **inside a digest-pinned Linux container** (the same
`python:3.12-slim` digest `portal/kubernetes/docker/backend/Dockerfile` ships)
with a pinned pip-tools version. Docker is required; `--native` opts out and is
refused outright on a non-Linux host.

That is not ceremony. pip-tools has **no** universal/cross-platform resolve mode
— `uv pip compile --universal` is a different tool — so the resolve is a
property of the machine it runs on. The committed portal lock had been
compiled on Windows, and therefore pinned the win32-only `colorama` while
omitting `uvloop`, which `uvicorn[standard]` uses on Linux. Every consumer of
these locks (Docker builds, CI, SBOM, Trivy fs) is Linux, so the lock described
an install none of them perform. Measured on 2026-08-15: compiling `[portal]`
natively on Windows still adds `colorama==0.4.6` and drops `uvloop==0.22.1`
versus the container. See #3491.

### When `ResolutionImpossible` comes back

`pip-compile` reuses the existing lock as constraints, so a floor whose fix
needs a transitive dependency to move is reported as impossible when it is
merely *forbidden*. The script now cross-references the versions named in the
resolver error against the pins in the lock it just fed back, names the ones
that match, and prints the exact `--upgrade-package` re-run. If none of them is
a reused pin it says so rather than guessing — the conflict is then genuinely
between the declared requirements.

See [`docs/SUPPLY_CHAIN.md`](../docs/SUPPLY_CHAIN.md) for the full workflow,
SBOM publication details, and CVE incident-response procedure.

## Relationship to `pyproject.toml`

The top-level declaration stays in `pyproject.toml` (PEP 621) so that
`pip install -e .[portal]` continues to work for contributors. The lock
files in this directory are **derived mechanically** from those extras via
`pip-compile --extra=<name> pyproject.toml`. This keeps `pyproject.toml`
canonical and eliminates drift between the two.

## Do not hand-edit `requirements/locks/*/requirements.txt`

Edit `pyproject.toml`, then run `./scripts/update-locks.sh`. The lock
files are fully mechanical outputs — any manual change will be overwritten
on the next regeneration and will cause the SBOM/Trivy jobs to drift.

[pip-compile]: https://pip-tools.readthedocs.io/en/stable/
