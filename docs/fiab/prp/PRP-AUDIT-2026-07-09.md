# CSA Loom — PRP reconciliation audit (2026-07-09)

**Date:** 2026-07-09
**Audited-at:** `main` @ `835d2145` (through PR #1797)
**Method:** per-item **grep/read verification against the code on `main`** — NOT the
PRP or MASTER-ROLLOUT status headers (which are stale in most PRPs; the repo has
raced ahead of the phased plan). Each item was checked against its named files,
routes, components, env vars, and bicep. Status reflects **current code**.

This document reconciles every active PRP under `PRPs/active/next-waves/` (plus the
`public-release`, `enterprise-hardening`, and `fabric-parity` reference dirs)
against the code, records the true "leftovers" (partial/missing items with **no**
MASTER-ROLLOUT phase owner), and captures every anomaly (built-but-listed-forward,
stale headers, half-landed items, dedup misses).

> No live estate identifiers appear here — only PR numbers, release revs, and the
> audited git sha (placeholders otherwise), per docs-hygiene CI.

---

## 1. Rollup

| PRP | total | built | partial | missing | scheduled |
|-----|------:|------:|--------:|--------:|----------:|
| fabric-gap-closure (FGC-01…31) | 31 | 16 | 7 | 8 | 0 |
| ai-foundry (AIF-1…18) | 18 | 16 | 1 | 0 | 1 |
| service-integrations (SVC-1…11) | 11 | 2 | 2 | 0 | 7 |
| databricks-parity (DBX-1…14) | 14 | 7 | 3 | 0 | 4 |
| surface-max (W1…22) | 22 | 8 | 5 | 0 | 9 |
| copilot-cts (CTS-01…17) | 17 | 5 | 1 | 0 | 11 |
| data-product (DP-1…17) | 17 | 0 | 3 | 0 | 14 |
| perf-scale (PSR-1…20) | 20 | 2 | 10 | 5 | 3 |
| hyperscale (HYP-1…16) | 16 | 0 | 0 | 0 | 16 |
| ux-baseline-g16 (U0/SC/U1-13/G1-6) | 31 | 14 | 5 | 1 | 11 |
| other-active-dirs (public-release/EH/FP/docs) | 13 | 4 | 5 | 2 | 2 |
| **TOTAL** | **210** | **74** | **42** | **16** | **78** |

Legend: **built** = shipped end-to-end on main; **partial** = half-landed (some
named deliverables present, others absent); **missing** = no meaningful impl but a
phase owns it; **scheduled** = correctly-unbuilt forward work with a phase owner.

**Headline findings**

- **74/210 items are built on main** — but the plan documents (MASTER-ROLLOUT + per-PRP
  headers) still enumerate ~40 of them as unbuilt forward work. The plan is stale, not the code.
- **Only two items are true leftovers with no phase owner**: `EH-P1-OBO`
  (per-user OBO data-plane path) and `EH-P1-MANIFEST` (item-type manifest registry),
  both from the enterprise-hardening PRP. Everything else maps cleanly to a MASTER-ROLLOUT phase.
- **42 half-landed items** — foundation/UI present, the item's full backend or its
  die-hard posture (default-ON, cross-replica, real-backend) not yet met.

---

## 2. Per-PRP detail

### 2.1 Fabric feature-gap closure (FGC-01…FGC-31)

Source: `PRPs/active/next-waves/PRP-fabric-gap-closure.md`

| ID | Title (short) | Status | Evidence / gap | Phase |
|----|---------------|--------|----------------|-------|
| FGC-01 | T-SQL Notebook (Spark-free) | missing | No `tsql-notebook` item type; zero files. Not in catalog/registry. | 9 (Wave 14) |
| FGC-02 | Python single-node Notebook | missing | `compute-picker.tsx` kinds = synapse-spark/databricks/dedicated-sql/serverless-sql; no DuckDB/Polars ACA single-node python-kernel mode. | 9 (Wave 14) |
| FGC-03 | Materialized Lake Views — incremental + multi-schedule + cross-ws lineage | partial | `materialized-lake-view-engine.ts` full-refresh only; incremental MERGE, multi-schedule, cross-ws lineage not built. | 9 (Wave 14) |
| FGC-04 | Semantic Model Refresh pipeline activity | missing | No `SemanticModelRefresh`/AnalysisServices key in `activity-catalog.ts`. | 9 (Wave 14) |
| FGC-05 | Airflow native Loom-item operators | partial | `airflow-job-editor.tsx` shows a parsed `operator` column but no first-class LoomNotebook/PipelineOperator picker or provider package. | 9 (Wave 14) |
| FGC-06 | Dataflow Gen2 Fast Copy path | missing | No `fastCopy`/bulk path in `dataflow-gen2-editor`. | 11 (Wave 17) |
| FGC-07 | OneLake cross-workspace security-role mgmt | partial | `onelake-security-rules.ts` has no `crossWorkspace`/multi-ws batch; single-item/single-ws only. | 11 (Wave 17) |
| FGC-08 | Spark consumption billing + max-spend cap | partial | Autoscale sizing exists; no `maxSpendCap`/`consumption-client`/billing selector. | 9 (Wave 14) |
| FGC-09 | Native Execution Engine honest-gate + Photon opt-in | missing | `spark-environment-editor.tsx` Runtime tab has no acceleration toggle. | 11 (Wave 17) |
| FGC-10 | High-concurrency Spark session pooling | partial | Warm pool built (`spark-session-pool.ts`) but one-lease-per-user; no shared/high-concurrency toggle. | 11 (Wave 17); coord PSR-3 |
| FGC-11 | `loom-cli` + VS Code extension | partial | `apps/loom-cli/` + publish workflow + parity doc shipped; VS Code extension (P3) absent. | 9 (Wave 14) |
| FGC-12 | Digital Twin Builder (ADX-native default) | built | `digital-twin-builder-editor.tsx` + `digital-twin-model.ts` + registry + BFF; ADT opt-in gated. | 5 (Wave 7) |
| FGC-13 | Activator trigger-model depth | built | `activator-trigger-model.ts` + tests + `activator/[id]/rules/route.ts`. | 5 (Wave 7) |
| FGC-14 | Real-Time hub new source connectors + samples | built | `source-catalog.ts` (+mongodb/oracle CDC, http-source) + `sample-streams.ts`. | 5 (Wave 7) |
| FGC-15 | Eventstream DeltaFlow CDC transform | built | `deltaflow-transform.ts` + tests + parity doc + editor wiring. | 5 (Wave 7) |
| FGC-16 | Data Wrangler in-notebook | built | `data-wrangler-panel.tsx`, `wrangler-ai.ts`, `wrangler-ai-tab.tsx`, `notebook/wrangler/route.ts`. | 6 (Wave 9) |
| FGC-17 | Semantic link / SemPy (LoomDataFrame) | built | `loom-semantic-link.py` + `semantic-model/[id]/semantic-link/route.ts`. | 6 (Wave 9) |
| FGC-18 | Batch model scoring (PREDICT-equiv) | built | `ml-model/[id]/predict/route.ts` + `/status` + `/history`. | 6 (Wave 9) |
| FGC-19 | AI Functions breadth + model-tier selector | built | `ai-functions-helper.tsx` Fast/Advanced tier + reasoningEffort on AOAI. | 3 (Wave 4) |
| FGC-20 | Azure SQL Database PITR / restore | built | `azure-sql-database/[id]/restore/route.ts` + `sql-restore-panel.tsx`. | 5 (Wave 8) |
| FGC-21 | Standalone DAX query view | built | `dax-query-view.tsx` + `semantic-model/[id]/dax-query/route.ts` + parity doc. | 6 (Wave 9) |
| FGC-22 | Copilot model-health scan + apply-fix | built | `model-health.ts` + `model-health-pane.tsx` + route + `apply-change.ts`. | 6 (Wave 9) |
| FGC-23 | Mirrored-database native CDC copy-job | built | `mirror-engine.ts` `backend:'azure-native-cdc'`/`engine:'adf-cdc'`. | 11 (Wave 17) |
| FGC-24 | Variable-library-aware promotion | built | PRP + MASTER-ROLLOUT both ✅ DONE (Wave 8); excluded from forward count. | done (excl.) |
| FGC-25 | Capacity surge protection (admission control) | built | `capacity-guardrails.ts` + `surge-protection-panel.tsx` + guardrails route; enforced in notebook run. | 5 (Wave 8) |
| FGC-26 | Capacity overage toggle | missing | No `overage`/`allowOverage` in guardrails/surge panel. | 9 (Wave 15) |
| FGC-27 | Capacity health + timepoint summary/detail | missing | `/admin/capacity` has cost+utilization but no timepoint heatmap/drill-down. | 9 (Wave 15) |
| FGC-28 | Chargeback report page | built | `admin/chargeback/page.tsx` + routes + `/attribution`. | 5 (Wave 8) |
| FGC-29 | Copilot capacity (AOAI) designation | missing | No capacity-designation in `copilot-config.ts`. | 9 (Wave 15) |
| FGC-30 | External (cross-tenant) data sharing | built | `external-share-*` model/client/panel + `api/external-shares/*`. | 5 (Wave 8) |
| FGC-31 | Workspace create wizard + settings flyout | partial | `panes/workspaces.tsx` create still single-step Dialog (added capacity/domain fields); no wizard/Graph picker/license step. | 9 (Wave 14) |

**Anomalies:** 16 FGC items BUILT on main but MASTER-ROLLOUT (07-09) still lists them forward in Phases 5/6 (FGC-12/13/14/15/20/25/28/30 + 16/17/18/21/22 + 19 in Phase 3). PRP status header stale (lists built items as MISSING/PARTIAL). No orphans — every FGC id maps to a phase. Half-landed: FGC-11 (CLI w/o VS Code ext), FGC-08 (sizing w/o billing cap), FGC-10 (warm-pool w/o concurrent sharing), FGC-31 (dialog w/o wizard). No UI-without-backend defects in built items spot-checked.

### 2.2 Azure AI Foundry integration (AIF-1…AIF-18)

Source: `PRPs/active/PRP-azure-ai-foundry-integration.md`

| ID | Title (short) | Status | Evidence / gap | Phase |
|----|---------------|--------|----------------|-------|
| AIF-1 | Knowledge Sources + Knowledge Bases | built | `aisearch-knowledge.ts` + routes + `knowledge-bases-panel.tsx` + `knowledge-tools.ts`; orchestrator wired. | Wave 3 |
| AIF-2 | Embedding client + integrated-vectorization | built | `embeddings-client.ts`, `vectorizer-consistency.ts`, `skillset-chain.ts`; designers in `foundry-sub-editors.tsx`. | Wave 3 |
| AIF-3 | Index-my-lakehouse/warehouse/ADX wizard | built | `index-my-data-wizard.tsx` + `index-my-data.ts` + prepare/run/sources routes. | Wave 4 |
| AIF-4 | Connected-agent (multi-agent) composition | built | `connected-agents.ts` + editor; wired in `agent-flow-canvas.tsx` + `data-agent-editor.tsx`. | Wave 5 |
| AIF-5 | Typed agent tool catalog | built | `agent-tool-catalog.ts` + editor + `agent-tool-kinds.ts`; freeform box removed. | Wave 5 |
| AIF-6 | Visual multi-agent workflow canvas | built | `agent-flow-canvas.tsx` (`@xyflow/react` + node-kit); FlowDag serialize/run. | Wave 5 |
| AIF-7 | `ai-enrichment` workflow item (batch LLM) | built | `ai-enrichment-client.ts` + editor + item type + routes. | Wave 4 |
| AIF-8 | MAF 1.0 OSS runtime tier (Gov) | built | `apps/copilot-maf/` + `agent-runtime-tier.ts` + `maf.bicep`. Deferred tail — see anomalies. | Wave 5 |
| AIF-9 | Foundry Connections CRUD | built | `foundry/connections/route.ts` POST/PATCH/DELETE, KV-ref creds. | Wave 3 |
| AIF-10 | Indexer scheduling + history + mappings + reset | built | `search-index-client.ts` + panels + routes. | Wave 3 |
| AIF-11 | PTU + Batch deployment types | built | `deployment-types.ts`; hub `DeployModelDialog` PTU/Batch; batch routes. | Wave 7 |
| AIF-12 | Model Router + Loom-native tier router | built | `model-tier-router.ts` wired into orchestrator + admin copilot-config. | Wave 9 |
| AIF-13 | AgentOps: eval-linked tracing + per-agent rollup | partial | Rollup BUILT (`agentops.ts` + `/agents/rollup`); MISSING eval-row→trace deep-link + App Insights `GROUP BY agent_name` tab. | Wave 7 |
| AIF-14 | Durable cross-session agent memory | built | `agent-memory-client.ts` + Cosmos no-TTL + threads route. Deferred: vector retrieval + discrete `memory` tool-kind. | Wave 5 |
| AIF-15 | AI Red Teaming Agent (PyRIT) | scheduled | Zero impl; grep hits false-positive. Truly not built. | Wave 11 |
| AIF-16 | Scoring-profile / analyzer / CORS / CMK designers | built | `search-index-designers.ts` + `index-designers.tsx`. | Wave 3 |
| AIF-17 | AI Search service administration in-editor | built | `aisearch-admin.ts` + `ai-search-service-panel.tsx` + routes. | Wave 3 |
| AIF-18 | Browser-automation tool type (Playwright ACA) | built | `browser-tool-client.ts` + `browser-tool.bicep` + runner + status route. Deferred: admin-plane bicep wiring + async. | Wave 5 |

**Anomalies:** PRP work-item matrix (lines 30–47) stale vs its own build-status footers + code (AIF-3/10/11/13/14/18). AIF-13 half-landed (rollup yes, trace deep-link + AI tab no). AIF-8 deferred Gov tail (connected-agent fan-out on MAF, streaming, multi-turn thread reuse, Cosmos agent authoring). AIF-14 spec deviation (discrete `memory` tool-kind + vector retrieval deferred; recency top-K today). AIF-18 ACA-Job not wired into admin-plane bicep (256-param ceiling); HTTP-runner synchronous only. AIF-15 is the sole unbuilt item, correctly forward (Wave 11). No orphans.

### 2.3 Azure service integrations (SVC-1…SVC-11)

Source: `PRPs/active/next-waves/PRP-azure-service-integrations.md`

| ID | Title (short) | Status | Evidence / gap | Phase |
|----|---------------|--------|----------------|-------|
| SVC-1 | AI-enrichment pipeline activities | built | 4 clients (doc-intel/vision/language/translator) + 4 activity-catalog entries + preview BFF + forms + bicep + env + test. | Phase 3/4 · Wave 4 |
| SVC-2 | AI Search cognitive skillsets | partial | Only debug-session lifecycle in `aisearch-client.ts`. Missing skillset CRUD, skill-chain builder tab, skillset BFF route. | Phase 3/4 · Wave 4 |
| SVC-3 | Azure Health Data Services (FHIR/DICOM/de-id) | scheduled | Zero backend; healthcare bundle still CSV-shim + manual Safe-Harbor + Power BI framing. | Phase 8 · Wave 13 |
| SVC-4 | Content Understanding | scheduled | No client/analyzer/activity. | Phase 8 · Wave 13 |
| SVC-5 | Azure Batch app wiring | partial | `batch.bicep` provisions account only; no client/pool editor/`BatchExecute`/BFF. Capacity nothing drives. | Phase 8 · Wave 13 |
| SVC-6 | Azure AI Video Indexer | scheduled | No client/activity/media item/bicep. | Phase 8 · Wave 13 |
| SVC-7 | Planetary Computer + Open Datasets shortcuts | scheduled | No STAC/open-datasets entries in `shortcut-engines.ts`. | Phase 8 · Wave 13 |
| SVC-8 | Content Safety pipeline activity | built | `ModerateText` activity in catalog; handled in ai-enrich preview route. | Phase 3/4 · Wave 4 |
| SVC-9 | Azure Confidential Ledger | scheduled | No client/bicep/tab/BFF. Governance audit Cosmos-only. | Phase 11 · Wave 17-18 |
| SVC-10 | Microsoft Graph Data Connect | scheduled | No client/linked-service/BFF. | Phase 11 |
| SVC-11 | Chaos Studio reliability panel | scheduled | No client/bicep/panel. Canonical chaos owner (PSR-20 defers here). | Phase 11 (REF PSR-20) |

**Anomalies:** SVC-1/SVC-8 fully built (Wave 4) yet still forward in MASTER-ROLLOUT Phase 4. SVC-5 half-landed (bicep account nothing drives; comment claims a nonexistent navigator). SVC-2 partial straggler inside shipped Wave 4. SVC-3 vaporware smell still open (CSV shim + manual HIPAA disclaimer + Power BI framing = the `no-fabric-dependency` concern it was written to fix), deferred Wave 13. Naming collision AIF-7 vs SVC-1 rename-pass unresolved. No orphans.

### 2.4 Databricks parity (DBX-1…DBX-14)

Source: `PRPs/active/next-waves/PRP-databricks-parity.md`

| ID | Title (short) | Status | Evidence / gap | Phase |
|----|---------------|--------|----------------|-------|
| DBX-1 | loom-app-runtime (hosted ACA apps) | built | Item + editor + `loom-apps-client.ts` + 5 templates + BFF + tests; real ACR build + ACA deploy. #1745. | Phase 1 |
| DBX-2 | Custom agent hosting | partial | Runtime foundation ships; no Agent/FastAPI template + `DA_SOURCE_TYPES` has no agent source (no compose-back). | Phase 4 |
| DBX-3 | databricks-pipeline (DLT visual editor) | built | `pipeline-editor.tsx` + BFF + client. #1757. | Phase 6 |
| DBX-4 | lakebase-postgres (serverless Postgres OLTP) | built | Item + `postgres-flex-client.ts` + opt-in databricks client + editor + tests. #1756. | Phase 6 |
| DBX-5 | Data Agent Genie deltas | built | metric-view grounding + "Open in Databricks Genie" deep link. #1758. | Phase 6 |
| DBX-6 | UC Metric Views | built | `metric-view-builders.ts` + builder + Metrics tab. #1758. | Phase 6 |
| DBX-7 | Streaming tables + MVs in SQL editor | built | Streaming Tables / MV tree nodes + builders + refresh scheduling. #1757. | Phase 6 |
| DBX-8 | Clean Rooms create + task CRUD | partial | Read-only only (`listCleanRooms`/`getCleanRoom`/`listCleanRoomAssets`); no create, no task CRUD/run. | Phase 4 |
| DBX-9 | Publish Data Agent as MCP | missing→scheduled | publish route Foundry-only; no publish-mcp route/toggle/`ask_<agent>` tool. | Phase 4 |
| DBX-10 | MLflow 3.x GenAI tracing + Prompt Registry | partial | Classical MLflow tracking present; no Traces tab (run_id-joined) + no Prompt Registry. | Phase 6 |
| DBX-11 | Managed Iceberg + UniForm | built | Table-format dropdown + deletion-vector/row-lineage → real TBLPROPERTIES. #1758. | Phase 6 |
| DBX-12 | Lakeflow Connect sink = UC managed table | missing→scheduled | No UC-managed-table sink on mirror wizard; Azure mirror covers default value. | Phase 4 |
| DBX-13 | Catalog Federation (cross-account) | missing→scheduled | No UC cross-account federation nor honest-gate. | Phase 4 |
| DBX-14 | Feature Store + Online Tables | missing→scheduled | No feature-store/online-table item/client/editor. | Phase 4 |

**Anomalies:** DBX-3/4/5/6/7/11 BUILT (Wave 10 #1756/#1757/#1758) but MASTER-ROLLOUT Phase 6 still lists them forward. DBX-1 correctly reconciled (#1745 Phase 1). Half-landed: DBX-2 (foundation only), DBX-8 (read-only), DBX-10 (classical-only). No orphans — DBX-9/12/13/14 scheduled Phase 4; DBX-10 trace store shared w/ AIF-13/CTS-14 per dedup.

### 2.5 Surface-max enhancements (W1…W22)

Source: `PRPs/active/next-waves/PRP-surface-max-enhancements.md`

| ID | Title (short) | Status | Evidence / gap | Phase |
|----|---------------|--------|----------------|-------|
| W1 | Undo/redo on every canvas | built | `use-canvas-history.ts` wired into 5 canvases. | Phase 1 (#1728) |
| W2 | Copy/paste + duplicate node | built | `canvas-shortcuts.ts` + `canvas.tsx`. | Phase 2 |
| W3 | Align/distribute toolbar | built | `canvas-align.ts` + `canvas-power-toolbar.tsx`. | Phase 2 |
| W4 | Canvas comments / sticky notes | scheduled | No `CanvasComment`/sticky-note. | Phase 8 (Wave 12) |
| W5 | Real-time co-authoring (presence) | scheduled | No webPubSub/yjs/presence. | Phase 8 (Wave 12) |
| W6 | Version-history timeline + diff | built | `version-history-drawer.tsx` + `item-version-store.ts` + diff. | Phase 2 |
| W7 | Ambient inline Copilot ghost-node | partial | `GhostNextStepNode` chrome renders; NO AOAI suggestion engine (manual add-next-step). | Phase 8 |
| W8 | Cross-catalog impact analysis | built | `impact-analysis.ts` + panel + `[type]/[id]/impact/route.ts`. | Phase 2 |
| W9 | Agent Flow Designer (chains MCP tools) | partial | AIF-6 canvas reused; MISSING standalone `agent-flow` item + Durable Functions + run route. | Phase 7 |
| W10 | Data Contract item type + gate | partial | Designer + validators exist EMBEDDED in data-products; MISSING standalone item type + BR-CONTRACT-GATE publish-block. | Phase 7 |
| W11 | Data Quality Rule Engine | partial | Real engine (`/api/dq/*` + client + 2 pages); MISSING catalog item-type + kql-dashboard scorecard tile. | Phase 7 |
| W12 | Synthetic Data Generator | scheduled | No `synthetic-data` slug/editor/Function. | Phase 7 |
| W13 | Incident / Runbook item | scheduled | No incident/runbook item type. | Phase 9 |
| W14 | FinOps what-if cost simulator | scheduled | No what-if/RetailPrices simulator. | Phase 9 |
| W15 | Use-case app clone/fork | scheduled | No clone/fork. | Phase 9 |
| W16 | App version-upgrade path | scheduled | No bundleVersion diff. | Phase 9 |
| W17 | Report mobile/phone layout | scheduled | No mobile-layout. | Phase 9 |
| W18 | Marketplace analytics + SLA webhooks | partial | `analytics/route.ts` + `sla-check/route.ts` BUILT; publisher-analytics UI tab unverified; direct webhook fanout (not Event Grid / W11-DQ). | Phase 7 |
| W19 | Cross-item "Explain this" Copilot | built | `explain-this` + `[type]/[id]/explain/route.ts`. | Phase 2 |
| W20 | Canvas shortcut cheat-sheet overlay | built | `canvas-shortcut-dialog.tsx`. | Phase 2 |
| W21 | Command-palette canvas coverage | built | `canvas-command-registry.ts` + `command-palette.tsx`. | Phase 2 |
| W22 | Learning Hub interactive sandbox labs | scheduled | No sandbox provisioning under `app/learn`. | Phase 8 |

**Anomalies:** W6/W8/W19 built but forward in Phase 2. W11 DQ engine + W18 analytics/SLA live but scheduled Phase 7; W18 sla-check ships without waiting on W11 despite "W11 must precede W18". W7 half-landed (chrome present, AOAI backend absent). W10/W11 built as data-product/governance surfaces not standalone item types; W10 publish gate owed. No orphans.

### 2.6 Copilot transparency / skills / memory (CTS-01…CTS-17)

Source: `PRPs/active/next-waves/PRP-copilot-transparency-skills-memory.md`

| ID | Title (short) | Status | Evidence / gap | Phase |
|----|---------------|--------|----------------|-------|
| CTS-01 | Per-message status bar (Tier 1) | built | `message-metadata-bar.tsx`; orchestrator computes latency/cost/tokens; rendered in transcript. | 3 |
| CTS-02 | Per-message detail badge (Tier 2) | built | `turnDetail` per-tool table (`turn-detail-panel.tsx`). | 3 |
| CTS-03 | Admin deep debug/trace panel (Tier 3) | scheduled | No per-turn phase timing, no trace route, no debug panel. | 3 |
| CTS-04 | Sources/grounding attribution | built | `tool-citations.ts` + `turnCitations`; CitationChips render. | 3 |
| CTS-05 | Context-window meter | built | `context-usage.ts` + panel + one `context_usage` SSE step. | 3 |
| CTS-06 | Dump conversation to memory | scheduled | Explicitly omitted (deferred to CTS-08); only Copy Report present. | 3 |
| CTS-07 | Skills library + management | scheduled | No skills container/registry/resolver/studio/toggle. | 3 |
| CTS-08 | Long-term memory brain | scheduled | No `copilot-memory` container / `getLayeredContext` / injection. | 3 |
| CTS-09 | MCP visibility in chat | partial | Per-call `via <server>` badge BUILT; MISSING per-conversation `connectedMcp[]` panel. | 3 |
| CTS-10 | Transparency+meter on every AI surface | built | Bar+meter reused in scoped panes + global editor. Caveat: inline builder routes carry neither. | 3 |
| CTS-11 | Skills self-evolution | scheduled | No `skill-learner`; rides CTS-07. | 6 |
| CTS-12 | Memory-write security guard | scheduled | No `memory-write-guard`; dep of CTS-08. | 3 |
| CTS-13 | Nightly memory consolidation | scheduled | No `memory-consolidate` job; rides CTS-08. | 3 |
| CTS-14 | Copilot replay → eval harness | scheduled | No replay/save-from-turn route; shares AIF-13 store. | 5 |
| CTS-15 | Proactive/ambient context injection | scheduled | No `proactive-context`; feeds BR-AMBIENT-FEED. | 11 |
| CTS-16 | Circuit breaker + learned routing | scheduled | breaker+learning absent (tier router is AIF-12/FGC-19). | 6 |
| CTS-17 | AI spend burn-rate projection | scheduled | No `spend-projection`; extends FGC-28→BR-COSTATTR. | 9 |

**Anomalies:** CTS-01/02/04/05/10 fully built but still forward in Phase 3. CTS-06 half-landed (context panel stubs "Dump to Memory" pending CTS-08). CTS-10 caveat (inline builder routes dax/notebook-assist/complete carry no bar/meter). Entire memory+skills family (CTS-06/07/08/11/12/13) unbuilt — `agent-memory-client.ts` is AIF-14, not CTS-08. No orphans.

### 2.7 Data product ultimate (DP-1…DP-17)

Source: `PRPs/active/next-waves/PRP-data-product-ultimate.md`

| ID | Title (short) | Status | Evidence / gap | Phase |
|----|---------------|--------|----------------|-------|
| DP-1 | Unify data-product model | scheduled | route.ts reads 3 unsynced fields; no `lifecycle.ts`. | 7 |
| DP-2 | Item-type taxonomy cleanup | scheduled | template/instance lack `coreSurface`/`hiddenFromGallery`; 3 look-alike `/new` cards. | 7 |
| DP-17 | Fix 3 freeform-config violations | scheduled | comma-separated owners `<Input>`, apim free-text, generic custom-attr input; no people-picker. | 7 |
| DP-3 | Guided creation wizard | partial | 4-step wizard + real preview route; missing template/sources+ports/access-policy/review steps, draft-on-step-1, skip-with-defaults. | 10 |
| DP-4 | Template gallery + provenance | scheduled | only `federated-mesh` stamps governed product; no previews/provenance. | 10 |
| DP-5 | Certification pipeline | scheduled | No `certification.ts`/route/state; two unsynced booleans. | 10 |
| DP-6 | Walkthroughs + LearnPopovers | scheduled | No completion checklist. | 10 |
| DP-7 | Copilot data-product builder | scheduled | No `<CopilotBuilderPane>` on DP editors; no copilot routes. | 10 |
| DP-8 | Input/output/management ports | scheduled | No `ports` model/route/tab. | 10 |
| DP-9 | Versioning + deprecation | scheduled | No versions/deprecate route; overwrite-on-save. | 10 |
| DP-10 | Subscription approval + fulfillment | scheduled | Base tiered-approval exists; no `fulfillSubscription`/auto-grant/attestations. | 10 |
| DP-11 | Feedback / ratings / usage | scheduled | No ratings/comments/feedback route (W18 analytics rides usage, not DP-11 glue). | 10 |
| DP-12 | Sample data + starter notebook | scheduled | No sample/drift/starter-notebook route. | 10 |
| DP-13 | Live SLO monitoring | partial | W18 freshness check + breach webhook; missing availability/latency/completeness, time-series, gauge, Monitor alert. | 10 |
| DP-14 | Value metrics (OKRs+CDEs+cost) | partial | Real OKR CRUD + Purview CDE read (F10); missing Value tab UI, cost/value from chargeback, KPI rollup. | 10 |
| DP-15 | Governed ↔ infra cross-linking | scheduled | No graph route/back-pointers. | 10 |
| DP-16 | Editable/consumable/shareable end state | scheduled | No landing/view-split/share/consumer-view route. | 10 |

**Anomalies:** DP-14 PRP "none (verified)" understates — `okrs/route.ts` + `cdes/route.ts` (F10) already real. DP-13 PRP "declarative-only" understates — `sla-check/route.ts` + `lib/marketplace/sla.ts` give real freshness check (W18-owned). DP-11 usage partly present via W18 analytics (#1744); only ratings/comments absent. DP-3 wizard drifted 3→4 steps since PRP; short of 7-step spec. All 17 roadmapped (Phase 7: DP-1/2/17; Phase 10: DP-3-16), none built to spec — no orphans, no done-but-forward.

### 2.8 Performance, scale & reliability (PSR-1…PSR-20)

Source: `PRPs/active/next-waves/PRP-performance-scale-parity.md`

| ID | Title (short) | Status | Evidence / gap | Phase |
|----|---------------|--------|----------------|-------|
| PSR-1 | Benchmark harness + `/admin/performance` + trend | built | `lib/perf/*` + probes + page + editor + route + Cosmos container + DCR bicep + scripts. | Phase 1 (PSR-A) |
| PSR-2 | Perf gate in CI + regression budget | built | `perf-gate.yml` + `perf-budgets.json` + `compare-budgets.ts` + docs. | Phase 1 (PSR-A) |
| PSR-3 | Warm Spark pool DEFAULT-ON + cross-replica lease | partial | Full lease model but DEFAULT-OFF; no cross-replica store, no kill-switch. | Phase 4 (PSR-B) |
| PSR-4 | Databricks / AML warm fast-path | partial | Pool supports databricks kind but rides off-default PSR-3; AML CI not wired; unmeasured. | Phase 4 (PSR-B) |
| PSR-5 | AAS warm-cache + result-cache tuning | partial | `query-result-cache.ts` exists; no AAS model-warm/prime, Cosmos tier not default-on, no hit-rate telemetry. | Phase 4 (PSR-B) |
| PSR-6 | ADX result-cache + cache headers + row-cap paging | missing | None of the 3 deliverables exist (live-query only, MAX_ROWS 5000). | Phase 4 (PSR-B) |
| PSR-7 | Dashboard tile parallelization + skeletons + SWR | partial | Editor + tile routes poll; no fan-out/skeletons/SWR. | Phase 4 (PSR-B) |
| PSR-8 | Copilot turn latency SLO + streaming budget + router | partial | PSR-1 measures first-token+full-turn; no SLO doc/target, no router tuning. | Phase 4 (PSR-B) |
| PSR-9 | Route-level code-splitting / TTI budget | partial | `next/dynamic` in a few surfaces; not the named heavy editors; no TTI budget. | Phase 4 (PSR-B) |
| PSR-10 | Cosmos RU/partition autoscale advisory + audit | missing | No RU sampling / cross-partition audit / advisory card. | Phase 6 |
| PSR-11 | ACA autoscale — HTTP tuning + KEDA per-workload | partial | bicep per-app `scaleRules` override; no KEDA queue/CPU scalers, no load-derived tuning. | Phase 6 |
| PSR-12 | Front Door caching for static/immutable assets | missing | No `/_next/static/*` caching / rules-engine rule. | Phase 9 |
| PSR-13 | Session-store hardening + refresh latency budget | partial | Cosmos-backed MSAL cache correct; no measured refresh budget, no LRU fast-path. | Phase 9 |
| PSR-14 | Concurrent-user load tests (k6/JMeter) | missing | No load-test config anywhere. | Phase 6 |
| PSR-15 | Quota preflight advisor | partial | Real route+evaluator+wizard but vCPU-only; not on `/admin/performance`. | Phase 6 |
| PSR-16 | SLO definitions + burn-rate alerts | missing | No `slo.md`, no burn-rate rules, no SLO panel. | Phase 11 |
| PSR-17 | Synthetic probes — UAT harness as continuous canary | partial | UAT harness exists per-roll; not promoted to scheduled ACA-Job canary feeding SLOs. | Phase 11 |
| PSR-18 | Blue-green console rolls | scheduled | REF BR-BLUEGREEN (unshipped); in-place PATCH only. | Phase 11 (REF) |
| PSR-19 | Control-plane DR active-passive drill | scheduled | REF BR-CONTROLPLANE-DR (unshipped). | Phase 11 (REF) |
| PSR-20 | Chaos experiments benchmark-under-fault | scheduled | REF SVC-11 (unshipped). | Phase 11 (REF) |

**Anomalies:** PSR-1+PSR-2 built+green on main but MASTER-ROLLOUT/PRP still label PSR-A pending (Phase-12 gate effectively met). PSR-15 half-landed (real backend beyond PRP claim, but vCPU-only + not surfaced). PSR-3 half-landed (backend committed DEFAULT-OFF — die-hard default-ON unmet). PSR-8 measurement outran spec (only SLO-definition half missing). PSR-6 marked PARTIAL in PRP but effectively MISSING on main. No orphans — REF 18/19/20 correctly dedup'd to BR-BLUEGREEN/BR-CONTROLPLANE-DR/SVC-11.

### 2.9 Loom Hyperscale custom components (HYP-1…HYP-16)

Source: `PRPs/active/next-waves/PRP-loom-hyperscale-custom-components.md` (Status: **proposed**)

Entire epic **unbuilt/scheduled** — Phase 12 (LAST), hard-gated on PSR-A green. Three NEW deployable services; none exist. Seed substrate libraries (adls/shortcut/onelake-security/catalog, cost-attribution, capacity-guardrails, surge-protection, query-cache, spark-session-pool DEFAULT-OFF, aas/tabular-eval) all present = documented starting point, not partial epic delivery.

| ID | Title (short) | Status | Evidence / gap | Phase |
|----|---------------|--------|----------------|-------|
| HYP-1 | Loom OneLake namespace/catalog service skeleton | scheduled | No service/`loom-onelake-app.bicep`/`loom://` resolver/env. | 12 (H1) |
| HYP-2 | Shortcut engine as a service | scheduled | `shortcut-engines.ts` in-proc only; not extracted. | 12 (H1) |
| HYP-3 | OneLake-security enforcement service | scheduled | Lib exists; no `POST /security/reconcile` service. | 12 (H1) |
| HYP-4 | OneLake 7 residual UI/BFF gaps | scheduled | None of 7 residual surfaces built. | 12 (H1) |
| HYP-5 | Loom Direct Lake columnar service skeleton (Rust) | scheduled | Zero delta-rs/DuckDB/DataFusion/arrow-rs; no service/bicep/env. | 12 (H2) |
| HYP-6 | Segment-residency cache + Redis coherence | scheduled | No columnar service; no Redis residency index. | 12 (H2) |
| HYP-7 | DAX-lite → Arrow/SQL compiler + backend selector | scheduled | Selector infra exists (2 values); 3rd `loom-columnar-cache` value + compiler absent. | 12 (H2) |
| HYP-8 | DirectQuery-class cold fallback | scheduled | Synapse-serverless backend exists; no Direct-Lake→cold semantics. | 12 (H2) |
| HYP-9 | Loom Capacity Broker skeleton (`POST /admit`) | scheduled | No `/admit`/broker/bicep/env. | 12 (H3) |
| HYP-10 | Smoothing/bursting math + 4-stage throttle | scheduled | Static hourly cap only; no timepoint/smoothing. | 12 (H3) |
| HYP-11 | Choke-point wiring — submit paths call `/admit` | scheduled | No `/admit` client at any submit path. | 12 (H3) |
| HYP-12 | Capacity-Metrics admin page reading broker ledger | scheduled | `/admin/capacity` exists; no broker-ledger Timepoint tab. | 12 (H3) |
| HYP-13 | Broker throttle events → Event Grid → Activator | scheduled | Generic eventgrid client; no broker topic/wiring. | 12 (H3) |
| HYP-14 | Warm-Pool Keepalive service (ref PSR-3) | scheduled | `spark-session-pool.ts` DEFAULT-OFF, per-replica. Owner PSR-3. | 12 (rides PSR-3) |
| HYP-15 | Shared Result-Cache service (ref PSR-5/6) | scheduled | In-proc LRU + Cosmos tier; no Redis service. Owner PSR-5/6. | 12 (rides PSR-5/6) |
| HYP-16 | Cross-cutting `modules/compute/` bicep + UAMIs + gates | scheduled | `modules/compute/` dir absent; nothing in admin-plane apps[]. | 12 (H1) |

**Anomalies:** No done-but-forward. Every HYP item genuinely unbuilt; all 16 in Phase 12. HYP-7 partially pre-satisfied (selector exists, 2 backends). HYP-12 not greenfield UI (page exists; gap is broker-ledger data). No half-landed (nothing started; seed libs are complete substrate). PSR-A green state not confirmed here (out of scope).

### 2.10 UX baseline program + Copilot-AI G1-G6 (U0/SC/U1-13/G1-6)

Sources: `PRPs/active/next-waves/PRP-ux-baseline-program.md`, `copilot-ai-G1-G6-verification`

**Shared components (U0 / SC-1…SC-10): all BUILT.** `canvas-node-kit.tsx` + history/clipboard/align/toolbar/command-registry/shortcut-dialog/anatomy; `details-panel`, `docked-inspector`, `guided-empty-state`, `preview-table`, `teaching-toast`, `explorer-tree`, `item-tab-strip`, `command-search`, `entity-diagram`. Bonus: shared `copilot-builder-pane.tsx` (the G1 "never built" primitive) now exists, adopted by 8 surfaces. **CAP-R1/CAP-R2 (Fabric capture): built.**

**Per-surface adopt-shared waves (U1-U13, 170 items):** only ~18 of 170 target surfaces import `components/shared/*`. U1-U10/U13 scheduled; U11 partial (~7/18 adopted early: lakehouse/materialized-lake-view/eventhouse/kql-database/semantic-model/warehouse/notebook; activator/digital-twin/ontology/graph-model/tapestry/data-agent/plan not yet); U12 partial (~4/27: ml-experiment/mirrored-database/graph/automl; rest untouched).

| ID | Title (short) | Status | Evidence / gap | Phase |
|----|---------------|--------|----------------|-------|
| G1 | Copilot-builder primitive on every design surface | built | Shared `copilot-builder-pane.tsx` + per-item assist routes + generic catch-all. | P2 |
| G2 | AI Functions at scale (table/T-SQL/Dataflow/multimodal) | built | `add-ai-column-dialog.tsx` wired into the Delta preview grid + Dataflow Gen2's AI step; `POST /api/ai-functions/table` batch endpoint (`lib/azure/ai-functions-client.ts`); multimodal image/document input gated on `LOOM_AOAI_VISION_DEPLOYMENT` (#1946). | P3 |
| G3 | Operations Agent → Azure Monitor + Logic App | partial | Deploys to Foundry Agent Service not `scheduledQueryRule`; no rule/condition/action canvas. | P3 |
| G4 | Data Wrangler AI tab | missing | `delta-preview-grid.tsx` read-only; no AI tab/suggestions/NL-to-code (blocked on G2). | P3 |
| G5 | Semantic-model Prep-for-AI / Verified Answers | built | `prep-for-ai/route.ts` + model + store; editor wired. | P2/P3 |
| G6 | Agentic-publish depth (Agent Store + connected-agent + auth mode) | partial | `descriptionForModel`/`deliverAsIs`/`connectedAgent`/`authMode` absent from m365-copilot route + copilot-studio-client. | P3 |

**Anomalies:** G1 DONE but PARTIAL in the G1-G6 doc and forward in Phase 2. G5 DONE but MISSING in doc / listed Phase-2 forward. U0/SC-1..10 + CAP-R2 fully built but Phase 1 still flags U0 "genuinely-unbuilt Phase-1 root." ~18/170 U-surfaces adopted ahead of schedule; remaining U-waves correctly scheduled. G1 stream-analytics uses item-slug + generic catch-all route (non-obvious, not a gap).

### 2.11 Public-release + other active dirs (public-release / enterprise-hardening / fabric-parity / docs-sweep)

Sources: `PRPs/active/public-release/`, `enterprise-hardening/`, `fabric-parity/`, docs-sweep

The 108-item public-release PRP (`rel-T`) is declared **Waves 0-7 LANDED** (rev 0000211 / 0.60.0), spot-verified built across every wave. Extra-scope dirs are where dangling work lives.

| ID | Title (short) | Status | Evidence / gap | Phase |
|----|---------------|--------|----------------|-------|
| PR-PRP | public-release PRP — 108 rel-T (Waves 0-7) | built | Declared LANDED; spot-checks all present (rel-T87/T09d/T82/T81/T83/T86/T20/T16). | Shipped (excl.) |
| PR-README | public-release README status header | partial | Content correct but STALE: still "NOT READY … ~13 blockers gate the tag" though all shipped. | — |
| EH-P0 | enterprise-hardening Phase 0 — refactor foundation | built | `aoai-chat-client.ts`, `rls-compiler.ts`, editor-split, refresh + deep-health routes. | Subsumed (shipped) |
| EH-P1-ACL | Phase 1 — multi-domain ACL / PDP | built | `pdp/{evaluate,enforce}.ts` + tid-partition + per-resource ACL (rel-T11/T20). | Subsumed (shipped) |
| **EH-P1-OBO** | Phase 1 — OBO data-plane (per-user tokens) | **partial** | `data-access-mode.ts` + `user-token-store.ts` exist BUT report `[id]/query` has no `accessMode==='user'` branch; `adls-user-client.ts`/`storage-user-token-store.ts`/`kusto-user-token-store.ts`/`user-pool-registry.ts` MISSING. Read path still service/UAMI. | **Not owned by any MASTER phase — dangling** |
| **EH-P1-MANIFEST** | Phase 1 — item-type manifest registry | **missing** | `lib/items/manifest/` does not exist; no `ItemManifest`/`FabricItemType→LoomItemType`. Deferred in EH Phase-0 STATUS. | **Not in any MASTER phase — dangling** |
| EH-P2 | Phase 2 — scale tier | partial | rate-limiter + query-cache base; MISSING middleware.ts/result-cache-client/concurrency-governor/Cosmos migration/PTU gateway. | Subsumed → PSR band (scheduled) |
| EH-P3 | Phase 3 — BCDR + cost-governance | scheduled | enforcement-engine/domain-cost-client/geo-failover MISSING. | Subsumed → FGC-28 / BR-CONTROLPLANE-DR |
| EH-P4 | Phase 4 — Ops SLO / load-test | scheduled | slo-catalog/monitor-slo/k6 MISSING. | Subsumed → PSR-16 / PSR-14 |
| FP-TrackA | fabric-parity Track A — fix-the-broken | built | Activator ADX-native runtime, UDF host, config-ignoring Copilot 503, item-sharing. | Ph0 shipped |
| FP-TrackB | fabric-parity Track B — ~110 new-parity gaps | partial | ~half built (AI fns 9/9, warehouse time-travel, Data Wrangler, Airflow, protection/OLS/managed-PE, estate search); rest → forward FGC/DBX/SVC/BR bands. | Subsumed → FGC/DBX/SVC (scheduled) |
| FP-SCORECARD | fabric-parity README §3 scorecard currency | partial | STALE (~06-26): Activator/UDF/Data-Wrangler/warehouse-time-travel/protection/item-sharing still D/❌ though built; rel-T95 re-baseline not applied here. | — |
| DOCS-SWEEP | docs-sweep — docs/help/copilot program | partial | Batch-0 done; per-category sweep of ~118 editors + 28 admin pages ongoing, not phase-counted. | Partly DOC-1..6 (Ph1 shipped); per-editor sweep ongoing |

**Anomalies:** PR-README status stale ("NOT READY / 13 blockers" though all shipped); PRP.md has no completion header. fabric-parity §3 scorecard stale (~06-26); rel-T95 ledger re-baseline never applied here. Dedup miss: FGC-16 Data Wrangler + G4 AI-tab listed forward (Phase 6/3) but already built via rel-T83. Two dangling EH foundations with NO forward-phase owner (OBO per-user data-plane; item-type manifest registry). EH Phases 2-4 are orphaned design docs — scope now lives under PSR/FGC/BR ids. enterprise-hardening + fabric-parity dirs are reference specs, not live trackers.

---

## 3. ACTION LIST — true leftovers & anomalies

### 3.1 True leftovers (partial/missing items with **NO** MASTER-ROLLOUT phase owner)

These are the only work items that fall through the cracks between the shipped
public-release program and the forward MASTER-ROLLOUT backlog. **Everything else in
this audit maps cleanly to a phase.**

1. **EH-P1-OBO** (partial) — Phase-1 OBO per-user data-plane path. `data-access-mode.ts`
   + `pbi/sql/user-token-store.ts` exist, but the flagship migration is not done: report
   `[id]/query/route.ts` has no `accessMode==='user'` branch; `adls-user-client.ts`,
   `storage-user-token-store.ts`, `kusto-user-token-store.ts`, `user-pool-registry.ts`
   are all absent. Data reads still run as service/UAMI, not the signed-in user. This is
   the enterprise-hardening PRP's **first migration target** and is represented nowhere in
   MASTER-ROLLOUT. **Needs a phase owner.**
2. **EH-P1-MANIFEST** (missing) — item-type manifest registry. `lib/items/manifest/`
   never created; no `ItemManifest` / `FabricItemType→LoomItemType` framework. Deferred in
   EH Phase-0 STATUS ("needs operator steer") and picked up by no next-wave. **Needs a phase owner.**

> Watch-list (leftover-adjacent, no clean phase but doc/program in nature, not feature build):
> **PR-README** stale status header, **FP-SCORECARD** stale parity ledger (rel-T95 re-baseline
> not applied), **DOCS-SWEEP** per-editor doc/Help currency program (ongoing, not phase-counted).
> These are documentation-currency debts, tracked below as anomalies rather than build leftovers.

### 3.2 Anomalies (consolidated)

**A. Stale plan — built-but-listed-as-forward-work.** The single largest anomaly class:
~40 items are built on main yet still enumerated as forward work.

- FGC: 16 items built but forward in Phases 5/6 (FGC-12/13/14/15/16/17/18/20/21/22/25/28/30 + 19 in Phase 3).
- DBX: DBX-3/4/5/6/7/11 built (Wave 10 #1756/#1757/#1758) but forward in Phase 6.
- SVC: SVC-1 + SVC-8 built (Wave 4) but forward in Phase 4.
- Surface-max: W6/W8/W19 built but forward in Phase 2; W11/W18 live but scheduled Phase 7.
- CTS: CTS-01/02/04/05/10 built but forward in Phase 3.
- G-series: G1 + G5 built but forward in Phase 2 (and stale in the G1-G6 doc).
- PSR: PSR-1 + PSR-2 built + green on main but PSR-A still labeled pending (Phase-12 gate met).
- UX: U0/SC-1..10 + CAP-R2 built but Phase 1 still flags U0 "genuinely-unbuilt root."

**B. Stale status headers / ledgers.** FGC PRP header; AIF work-item matrix (lines 30-47);
public-release README "NOT READY / ~13 blockers"; fabric-parity README §3 scorecard (~06-26)
— rel-T95 ledger re-baseline declared LANDED but not applied to this file.

**C. Dedup miss — Data Wrangler double-listed.** FGC-16 Data Wrangler (Phase 6) and G4
Data-Wrangler-AI-tab (Phase 3) are listed forward but Data Wrangler is already built via
rel-T83 (`data-wrangler-panel.tsx` + `wrangler-ai-tab.tsx` + `notebook/wrangler/route.ts`).
Strike from forward count or mark ✅ like FGC-24.

**D. Half-landed items (foundation/UI present, full backend or die-hard posture absent).**
- FGC-08 (autoscale sizing, no billing/max-spend cap), FGC-10 (warm-pool, no concurrent sharing),
  FGC-11 (CLI, no VS Code ext), FGC-31 (dialog, no wizard/Graph picker/license step).
- DBX-2 (foundation, no Agent template + no compose-back), DBX-8 (read-only Clean Rooms, no create/task CRUD), DBX-10 (classical MLflow, no Traces tab + no Prompt Registry).
- SVC-5 (batch.bicep account nothing drives — bicep-without-app).
- W7 (ghost-node chrome, no AOAI suggestion engine), W9 (canvas reused, no standalone item + Durable Functions), W10 (embedded contract, no standalone item + publish gate).
- CTS-06 (context panel stubs "Dump to Memory"), CTS-09 (per-call badge, no per-conversation panel).
- PSR-3 (warm-pool DEFAULT-OFF, no cross-replica store/kill-switch — die-hard default-ON unmet), PSR-15 (real backend but vCPU-only + not on `/admin/performance`).
- AIF-13 (rollup, no trace deep-link + AI tab), AIF-8 (MAF core, no Gov connected-agent fan-out/streaming/Cosmos authoring), AIF-14 (run-path memory, no discrete tool-kind + vector retrieval), AIF-18 (ACA-Job not in admin-plane bicep; sync HTTP only).
- G2/G3/G6 (partial AI-at-scale / ops-agent-to-Monitor / agentic-publish depth), G4 (missing, blocked on G2).
- DP-3 (4-step wizard vs 7-step spec), DP-13 (freshness only), DP-14 (OKR/CDE backends, no Value tab).

**E. Vaporware smell still open (deferred, not fixed).** SVC-3 — healthcare bundle keeps
synthetic-CSV shim + manual HIPAA Safe-Harbor disclaimer + Power BI dashboard framing on the
default path (a `no-fabric-dependency` concern), unfixed until Wave 13.

**F. Naming / ordering nits.** AIF-7 (`ai-enrichment` item) vs SVC-1 (AI-enrichment pipeline
activities) rename-pass unresolved; SVC-1 shipped without it. W18 sla-check ships without
waiting on W11 despite MASTER-ROLLOUT's "W11 must precede W18." PSR-6 marked PARTIAL in the
PRP but effectively MISSING on main.

**G. Orphan check.** Zero orphans beyond the two §3.1 leftovers — every FGC/AIF/SVC/DBX/W/CTS/DP/PSR/HYP/U/G
item maps to a MASTER-ROLLOUT phase (or is dedup'd to a named owner). The REF items
(PSR-18/19/20) correctly point at BR-BLUEGREEN / BR-CONTROLPLANE-DR / SVC-11, none shipped —
the REF glue is blocked on those owners.

---

## 4. Recommendations

1. **Assign the two §3.1 leftovers a phase.** EH-P1-OBO (per-user OBO data-plane) is a
   defensible-security foundation; EH-P1-MANIFEST is a 60k-scale foundation. Both should be
   given an explicit MASTER-ROLLOUT owner rather than left dangling in the enterprise-hardening
   reference dir.
2. **Re-baseline MASTER-ROLLOUT against this audit** — strike the ~40 built-but-forward items
   (Anomaly A) from the forward count so the remaining backlog reflects real work.
3. **Refresh stale headers/ledgers** (Anomaly B): public-release README, fabric-parity §3
   scorecard (apply the rel-T95 re-baseline), FGC PRP header, AIF work-item matrix.
4. **Close the dedup miss** (Anomaly C): mark FGC-16 / G4 Data Wrangler ✅ done.
5. **Track the half-landed items** (Anomaly D) as explicit "finish the second half" tickets in
   their owning phases rather than counting them as fully forward.
