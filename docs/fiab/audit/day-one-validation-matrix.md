# CSA Loom — Day-One 100% Validation Matrix (HONEST)

**Ship gate:** every item, function, capability, app, and control-plane surface must be validated **end-to-end** (backend + **frontend** + Azure services + APIs) and proven to work — or have an honest, documented limitation — before day-one release.

**Rules (non-negotiable, set by the operator):**
1. **No PASS without a FRONTEND observation.** A passing API/route check is NOT a PASS. A row is PASS only when the actual UI action was driven and the real result was observed in the browser.
2. **No fake/missing datasets.** If an app bundle references a dataset, that dataset must be **hosted in the repo** and **actually loaded/installed** by the flow. External/example URLs that 404 or reference non-existent workspaces are FAILs.
3. **No vaporware.** A UI that renders but doesn't perform its claimed function is a FAIL.
4. **Honest status only.** Untested = `NOT-TESTED`. Do not infer PASS.

**Status legend:** `PASS` (frontend-verified) · `FAIL` (frontend-verified broken) · `FIXING` (fix in progress) · `NOT-TESTED` (not yet driven in the UI — assume nothing).

---

## A. Verified in the FRONTEND so far (this is the ONLY trustworthy section)

| Surface | Status | Frontend evidence |
|---|---|---|
| AutoML (create→wizard→submit) | PASS* | Drove wizard in browser; real AML job submitted, visible in Runs w/ Studio link. *Required a fix: AmlCompute cluster provisioned + concurrency≤nodes. |
| Notebook (cell run) | PASS | Run cell → real Synapse Spark Livy session 29 on loompool (observed). |
| App install — FinOps Cost Optimizer | PASS | Installed in CSA Loom Demo; 3 items provisioned w/ real Azure ids (observed). |
| CoE report viewer render | PASS | Report tab rendered 7 visuals (observed earlier); live-data path = separate PR. |
| **App-bundle dataset load (repoDataset → tenant ADLS)** | **PASS** | **v0.11, 2026-06-16:** fresh install of Lakehouse Inspector into a clean workspace. Report: *"Shortcut retail-orders-public: uploaded repo dataset `samples/app-data/lakehouse-inspector/retail-orders-public.csv` → `…/Files/_shortcuts/retail-orders-public/retail-orders-public.csv` (38751 bytes)"*, *"registered 'active' (self-contained, in-tenant)"*, *"Shortcuts: 1 active, 0 pending, 0 failed (of 1 declared)"*. Proves #1434 — the CSV ships in the image (`/app/samples`) and is uploaded to tenant ADLS. 38751 B = the repo file size. |
| **Shortcuts — register from bundle** | **PASS** | **v0.11:** bundle declares 1 repoDataset shortcut; report = *"1 active, 0 pending, 0 failed (of 1 declared)"*. (An earlier "2 external pending shortcuts" report in the CSA Loom Demo workspace was a STALE prior-session install — items "existed", not re-provisioned with v0.11 code.) Parser #1429 + datasets #1433/#1434 landed. Right-click context menu still NOT-TESTED. |
| **App install — Lakehouse Inspector (lakehouse+notebook+medallion seed)** | **PASS** | **v0.11:** both items `created`; Azure-native ADLS Gen2 lakehouse materialised — 10 table folders (10 seeded), 10 Synapse serverless views, shortcut active. Real `abfss://landing@srloomdlk6mvh5sm6z7do…`. |
| **Lakehouse Inspector — serverless SQL endpoint (T-SQL query)** | **PASS** | **2026-06-17, fresh centralus estate:** after granting the Console UAMI the **Synapse SQL Administrator** Synapse-RBAC role on the DLZ workspace, Retry → `synapse-serverless-sql-pool` step goes green `created` (`syn-loom-default-centralus-ondemand.sql.azuresynapse.net`); install toast *"All items installed and provisioned."* **Root cause:** "Login failed for token-identified principal" on CREATE DATABASE = UAMI lacked CONTROL SERVER. The ARM `administrators/activeDirectory` AAD-admin route does NOT create a working serverless login for an MI (documented Graph-fetch limitation); Artifact Publisher + Compute Operator don't confer SQL admin. Fix = Synapse SQL Administrator RBAC (roleId `7af0c69a-…`). **Durable bicep fix still owed** — synapse.bicep must grant SQL Administrator at deploy (must pass appId since the deploy identity is an SPI; PR #1440's ARM-admin sid=appId is insufficient). |
| Right-click context menu (lakehouse) | NOT-TESTED | Operator reports broken; not yet reproduced in UI. |
| **Warehouse (create → editor)** | **PASS** | **2026-06-17:** New → Data Warehouse → `val-warehouse` created (item `abaf48b5`); rich editor renders — ribbon (New SQL query, Run, Save as table, Open in Excel, Ask Copilot, Explain, Optimize, Model view, Manage relationships, Principals, Source control, Query statistics, Query acceleration, Alerts, Column & Row security), schema tree (Schemas/Views/Stored procedures/Functions), Query/Model/Monitoring/Migrate tabs. Backing = Synapse Dedicated SQL `loompool` (DW100c, **Online** per ARM). |
| **Warehouse — compute status badge** | **PASS ✓ (fix confirmed live)** | **2026-06-17:** #1445 merged + the live console rolled to SHA `94daabbe`. Re-verified: status pill now reads **Online** (loompool / DW100c), matching ARM — was "Unknown". Root cause (PR #1445): the ARM status probe (`synapse-pool-arm.ts`) mixed `LOOM_SYNAPSE_SUB` (admin sub) with `LOOM_DLZ_RG` → 404 → "Unknown". Fix = self-heal via **Azure Resource Graph discovery by workspace name**; schema route now distinguishes Paused (409) from probe-failure (502). |
| **Warehouse — Alerts dialog auto-opens & blocks query editor** | **PASS ✓ (fix confirmed live)** | **2026-06-17:** #1445 merged + rolled live. Re-verified: the Alerts dialog **no longer auto-opens**; query editor is clean on load. Root cause (PR #1445): the "New alert" editor `Dialog` was nested inside the list Dialog's `DialogSurface`; fix = render as a sibling (fixes phase3 + databricks warehouse editors). |
| **Warehouse — T-SQL query execution (dedicated pool)** | **PASS** | **2026-06-17, rolled image:** ran `SELECT 1 AS smoke, DB_NAME() AS db;` on loompool → **1 row · 303 ms**, `smoke=1, db=loompool`. Proves the dedicated-SQL TDS/AAD path end-to-end (UAMI holds Synapse SQL Administrator). **Day-one bug found:** the editor's default **seed query** used `SUSER_NAME()`, unsupported on Synapse Dedicated SQL → out-of-box Run fails `'SUSER_NAME' is not a recognized built-in function · EREQUEST`. Fix → **PR #1448** (`SUSER_NAME()`→`SYSTEM_USER` in the two dedicated-pool seeds; `SYSDATETIMEOFFSET()` confirmed supported, left as-is). |
| **Data pipeline (create → editor)** | **PASS (UI/parity)** | **2026-06-17:** New → Data Factory → Data pipeline `val-pipeline` (item `e4040db0`); rich ADF designer — activity palette (Copy data, Dataflow Gen2, Mapping data flow, Lookup, Get metadata, Delete, Notebook, Spark Job Def, Invoke pipeline, Script, Stored procedure…), canvas, Parameters/Variables/Settings tabs, full ribbon (Validate, Publish, Run, Debug, Schedule, Add trigger, Export/Import, Templates). NOTE: canvas drag-drop + backend Publish/Run not exercised — synthetic HTML5-DnD doesn't fire on the canvas (browser-automation limit, not a confirmed product bug); needs real-mouse or a click-to-add affordance to fully validate. |
| **Eventhouse (create → editor)** | **PASS** | **2026-06-17:** New → Real-Time Intelligence → Eventhouse `val-eventhouse` (item `b36b6af7`) created against the **real shared ADX cluster** `adx-csa-loom-z52x3p.centralus.kusto.windows.net`. System-overview shows live telemetry (Storage 10.7 MB orig / 1.9 MB compressed @5.7×, 100% ingestion success, real **schema-change log**: ADD-DATABASE / ALTER-DATABASE-CACHING-POLICY / ADD-DATABASE-ADMINS on `loomdb-default`). Databases tab lists the real `loomdb-default` KQL DB. Full RTI ribbon present. |
| **KQL query execution (ADX)** | **PASS** | **2026-06-17:** ran `print smoke="ok", server_time=now(), current_user=current_principal()` → **1 row · 38 ms** against the real ADX cluster; result `current_user = aadapp=0638e31f-…` = the console UAMI authenticating to ADX. Proves the RTI/ADX query path end-to-end. Minor cosmetic finding: the per-DB **"Query"** action routes to a `kql-database/new` editor showing a misleading **"cluster not configured"** badge, yet the query still executes against the real cluster (badge/route only). |
| **Eventstream (create → editor)** | **PASS** | **2026-06-17:** New → RTI → Eventstream `val-eventstream` (item `6e403e38`); visual designer canvas renders a real **eventhub → kusto** topology, full ribbon (Add source/transform/destination, Filter, Aggregate, Group by, KQL Database, Lakehouse (ADLS), Event Hub, Activator, Add alert), and Visual designer / SQL operator / JSON tabs. |
| **Eventstream — Event Hubs env-wiring (dlz-attach gap)** | **FIXING** | **2026-06-17:** editor showed honest gate *"Event Hubs namespace not configured — set LOOM_EVENTHUB_NAMESPACE…"*. Root cause = **systemic dlz-attach env-wiring gap**: the namespace `evhns-loom-default-centralus` IS deployed in the DLZ (rg-csa-loom-dlz-default-centralus / sub 363ef5d1), but the console env had `LOOM_EVENTHUB_NAMESPACE=""` and `LOOM_EVENTHUB_RG/SUB` pointed at the **admin** plane. PR #1441's hub-console-dlz-env module wires ADLS+Synapse but NOT Event Hubs. **Fixed live:** granted the console UAMI *Azure Event Hubs Data Owner* + *Contributor* on the namespace and set `LOOM_EVENTHUB_NAMESPACE/RG/SUB` to the DLZ values (revision 0000005 healthy). Browser re-verify of the cleared gate is **pending operator re-auth** (the long-running MSAL session expired ~mid-sweep — `SESSION_SECRET` is a stable KV secretref so it's token expiry, not the revision roll). Durable bicep fix: extend the dlz-attach console-env module to include EH (+ audit ASA/ADX/other DLZ coords), or adopt the #1445 Resource-Graph-by-name self-heal pattern for all DLZ clients. |
| **Eventstream — ASA job stale region** | **FAIL (minor)** | **2026-06-17:** the Stream Analytics job is named `asa-loom-default-eastus2` on a **centralus** estate — a stale region carried from config; should derive from the deployment region. |
| **App install — RAG Builder (4 items)** | **PASS (install) + honest gates** | **2026-06-17:** New → Apps → RAG Builder → Val2. All 4 items **created** (RAG Corpus, RAG Basic Q&A, RAG Quality 7-metric, Walkthrough). Two honest infra-gates (correct per no-vaporware): **ai-search-index** — *"AI Search service not configured; set LOOM_AI_SEARCH_SERVICE"* — **verified: NO AI Search service is deployed anywhere in the estate** (the clean centralus deploy didn't enable it); it IS deployable via `modules/admin-plane/ai-search.bicep`, so this is an acceptable honest gate, not vaporware. **prompt-flow** — *"No AI Foundry project configured"* (the `aifndry-loom-centralus` AIServices account exists; no project/env wired). **Day-one note:** enable AI Search + create a Foundry project in the default deploy params so AI-class apps (RAG Builder, Sovereign AI Agents, Supercharge) are functional out-of-box; otherwise they ship gated. |
| **Live console image roll (latest fixes)** | **PASS** | **2026-06-17:** rebuilt origin/main `94daabbe` into `acrloomk6mvh5sm6z7do/loom-console:v0.1` (ACR public-toggle for the ACR-Tasks push, re-locked after), rolled `loom-console` to the new digest → revision 0000006 Healthy. `/api/version` + `/build-marker.txt` both confirm `sha=94daabbe`. The merged fixes #1445 (Warehouse status/alerts + Synapse-pool self-heal) and #1447 (generalized DLZ ARM self-heal) are now LIVE. |

---

## B. NOT-TESTED — the full surface still owed a real frontend validation

Assume nothing here works until driven in the UI.

- **Item types (~104):** every New→item create flow + its primary action (run/query/render/deploy), in the UI. Only AutoML + Notebook + Lakehouse (partial) + Map (shell) driven so far.
- **Apps (29):** install → provision → **load real data** → use it, per bundle. Only FinOps (PASS) + Lakehouse Inspector (PASS, incl. serverless T-SQL after the SQL-Admin RBAC fix) driven.
- **Feature surfaces (243 leaves from the Wave-2 code audit):** Real-Time Hub, RTI catalog, Activators, Mirroring (all source types), APIs (APIM/GraphQL/DAB/UDF), Warp/Weave, Workload Hub, Connections, Business Events, Event Hubs — drive each in the UI.
- **Control planes:** every Admin Portal page + action (Tenant settings, Capacity/Scale, Runtime config, API Mgmt, Domains, Deployment planner, Add landing zone, Security & governance, Feature permissions, Batch labeling, Embed codes, Org visuals, Audit logs, Usage, Copilot usage, DSPM, Users, Network & DNS, Updates) + the Setup Wizard + DLZ attach/visualize.
- **Datasets:** audit ALL 29 bundles → every referenced dataset hosted in-repo + load verified.

---

## C. Execution method (so it's trustworthy, not "claimed")
1. Drive each surface in the authenticated browser; record PASS/FAIL + the observed evidence here.
2. For each FAIL: fix (live + bicep), re-drive in the UI, flip to PASS only on observation.
3. Datasets: host real sample data in the repo (`samples/` or `docs/fiab/sample-data/`), wire installers to upload/load it, verify the data is queryable in the UI.
4. Report progress as honest counts (N driven / M PASS / K FAIL / rest NOT-TESTED). Never "100%" until the matrix is actually all-PASS.

_This is a large, attended, multi-session program. Progress is tracked here, honestly, with frontend evidence per row._
